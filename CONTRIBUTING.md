# HUAKAI 贡献指南

## 1. 事实来源

HUAKAI 以最新主线生产源码、数据库迁移和部署接线为事实基线。文档与源码冲突时，先核实
源码和真实行为，再通过 PR 修正文档或实现，不得用旧计划覆盖当前真码。

开始前依次阅读：

1. `AGENTS.md`：所有参与者的硬规则。
2. `docs/RULES.md`：项目开发、风险、clean-room 和发布规则。
3. `docs/HUAKAI项目与架构白皮书.md`：产品、架构、三身份、模块和完整链路。
4. `docs/源码责任索引.md`：全部第一方源码文件、声明、功能归属和保守影响半径。
5. 与本目标直接相关的 OpenAPI、schema、白皮书稳定决策和运维手册。

## 2. 单目标、单分支、单 PR

一个目标同一时间只有一个写入负责人和一个 PR。其他参与者可以只读调查、复现和审查，
不能在另一个工作树同时修改同一目标。

```text
同步 origin/main
  -> 创建目标分支和独立工作树
  -> 核实真码与外部行为合同
  -> 形成完整影响面
  -> 实现和判别式测试
  -> 独立审查
  -> PR
  -> Owner 同意
  -> 合并
  -> 最新主线再验证
```

主线只接收 PR，不直接修改。未经 Owner 明确同意不得合并。

## 3. 缺陷记录

普通缺陷使用 GitHub Issue，修改使用 PR；不要为每个缺陷在 `docs/process` 新建 Markdown。

Issue 必须包含：

- 实际现象、期望行为和最小复现。
- 提交、迁移版本、部署方式和脱敏证据。
- 整条业务链和兄弟模块的影响范围。
- 资金、鉴权、schema、凭据和生产风险。
- 能证明关闭的测试与主线验证条件。

PR 必须说明：

- 根因，不只描述表面错误。
- 修改文件和行为差异。
- 正常、失败、并发和恢复验证。
- 不能由现有测试证明的部分。
- 发布、回滚或前向修复方式。

## 4. 源码与文档

- 生产代码和测试注释使用中文；技术标识符保留英文。
- 文件和包按内聚职责组织，不把新功能继续塞进超预算大包。
- OpenAPI、schema、配置和运维动作变化必须在同一 PR 同步。
- 只有重要、复杂、高风险或易变化的构件进入白皮书；全部文件覆盖由源码责任索引、包结构、
  中文注释和 review 共同保证。
- 变更第一方源码后必须在同一 PR 更新源码责任索引。当前主线没有专用生成器，维护者必须重新枚举
  主线路径、校验无缺失和重复，并人工复核功能编号、运行层级与影响半径，不得提交陈旧索引或编造
  不存在的生成命令。
- 已完成计划、旧状态快照和一次性调查报告在有效事实迁移后删除，由 Git 保存历史。

## 5. 测试与合并

验证范围必须匹配变更范围。局部单测不能证明整条链可上线；模拟测试不能冒充真实账号验证。
涉及鉴权、资金、配额、schema、凭据或跨模块接线时，必须覆盖拒绝、并发、幂等、失败恢复
和运维处置。

PR 通过所需检查后仍需 Owner 明确批准。合并后在最新主线重跑关联门；只有主线证据才是最终
完成证据。
