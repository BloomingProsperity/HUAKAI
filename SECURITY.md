# HUAKAI 安全报告政策

## 1. 私密报告

请不要在公开 Issue、讨论、PR 或日志附件中披露可利用漏洞、明文凭据、Cookie、访问令牌、
私钥、个人信息或生产地址。

优先使用 GitHub 仓库 `Security` 页面提供的私密漏洞报告渠道：

<https://github.com/BloomingProsperity/HUAKAI/security>

如果该渠道暂不可用，请通过 Owner 已验证的私密联系方式报告；不要为了获得响应而公开利用细节。

## 2. 报告内容

报告应尽量包含：

- 受影响提交、版本、部署模式和端点。
- 攻击前提、最小复现和实际影响。
- 是否涉及跨租户、权限提升、资金、凭据、数据泄露或远程执行。
- 已采取的临时缓解措施。
- 不含真实秘密的日志、请求或测试证据。

## 3. 处理原则

安全问题按以下顺序处理：

1. 限制继续扩散并保护凭据和数据。
2. 在最新主线复现并确认真实攻击路径。
3. 沿身份、租户、路由、凭据、钱路、日志和恢复检查同类影响。
4. 编写能区分修复前后行为的回归测试。
5. 通过独立安全审查和必要的迁移、轮转、恢复验证。
6. 由 Owner 批准后合并、发布和通知受影响部署者。

重大安全事故需要独立 RCA；普通缺陷留在私密报告、修复 PR 和发布记录中，不额外堆积文档。

## 4. 支持边界

HUAKAI 不接受以下行为：

- 使用不属于报告者的真实账号、凭据或数据进行测试。
- 对非授权部署实施扫描、流量压测、社会工程或拒绝服务。
- 在修复发布前公开可直接复现的利用链或秘密。

本政策不改变 `LICENSE` 和 `LEGAL.md` 中的许可、责任与合规边界。
