This file is agent-facing and authoritative.

# Risk Register

## Purpose

Track risks that can affect implementation method, rollout, testing, or release readiness. Risks must not be used to silently drop features.

## Risk Template

| Risk ID | Area | Severity | Risk | Impact | Mitigation | Feature Impact | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-TBD | TBD | TBD | TBD | TBD | TBD | No deletion allowed. | TBD | Open |

## Initial Risks

| Risk ID | Area | Severity | Risk | Impact | Mitigation | Feature Impact | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R-LIC-001 | License | TBD | Two of three primary references (New API, All API Hub) are AGPL-3.0; one (Sub2API) is LGPL-3.0. AGPL is triggered by network distribution. | Copying any protected detail risks forcing the entire platform under AGPL when offered as a service. | Decided in [DR-000](process/decisions/DR-000-clean-room-methodology.md): Option B (two-lane separation) default + Option C carve-out for billing ledger, account-pool routing, provider failover/account-health. Lane definitions in [05_CLEAN_ROOM_POLICY.md](05_CLEAN_ROOM_POLICY.md) and [12_AGENT_WORKFLOW.md](12_AGENT_WORKFLOW.md). Spec-leakage review required before specs leave the specifier lane. MIT anchor reference (one-api) is the safe source-level study target. | Use safe equivalent or independent implementation; no feature deletion. | Claude | Mitigated |
| R-SEC-001 | Security | TBD | Admin operations can expose secrets or dangerous controls. | Credential leak or unauthorized changes. | Redaction, RBAC, audit logs, confirmations. | Gate or stage, do not delete. | Claude | Open |
| R-BILL-001 | Billing | TBD | Usage and cost accounting can drift. | Revenue loss or incorrect charges. | Acceptance tests and reconciliation views. | Preserve billing feature with stronger checks. | Codex | Open |
| R-OPS-001 | Operations | TBD | UI hides important provider/account state. | Operators cannot resolve incidents. | Scenario-driven dashboard contracts. | Improve UI parity. | Gemini | Open |
| R-REL-001 | Reliability | TBD | Failover masks provider problems. | Silent degradation or cost spikes. | Health states, alerts, fallback logs. | Add visibility and controls. | Claude | Open |
| R-TRUST-001 | Trust chain / signer key | HIGH | Trust signer private key leaks or is copied outside the intended file/KMS boundary. | Attacker can mint receipts that verify until the fingerprint is revoked; user trust in historic receipts is weakened. | Production key path is `HUAKAI_AUDIT_PRIVATE_KEY_PATH`; publish CRL overlay through `HUAKAI_TRUST_REVOKED_KEYS_FILE/JSON`; keep 90-day rotation registry and immediate revoke runbook. | Preserve signed receipts; compromised keys become revoked/unverified rather than deleting trust-chain feature. | Codex | Open |
| R-TRUST-002 | Trust chain / client anchor | MED | TOFU first fetch can be intercepted before the client pins the public key. | Client may cache an attacker key and reject the real key later, or accept forged receipts during first use. | Require HTTPS; `huakai-verify` records first-use cache under `~/.huakai/known_keys`; first-use UX must prompt or require explicit Owner silent-trust acceptance in packaged clients. | Preserve TOFU; stronger pinning can be added without removing offline verify. | Codex | Open |
| R-TRUST-003 | Trust chain / DLQ recovery | HIGH | `audit_ledger_dlq` or trust-ledger deferred queue accumulates after fail-open responses. | Users receive successful responses with `unverified` trust state for too long; replay backlog can hide signer/ledger incidents. | Monitor DLQ depth/age; Prometheus alert on stale/depth thresholds; manual replay runbook; preserve `X-HUAKAI-Ledger-DLQ-Ref` so affected requests are traceable. | Preserve fail-open availability while making recovery observable. | Codex | Open |
| R-TRUST-004 | Trust chain / canonical bytes | HIGH | Canonical receipt bytes drift across versions, languages, or money precision changes. | Old receipts stop verifying or two clients disagree about the signed facts. | HUAKAI `trust.receipt.v1` fixed-order writer; golden bytes/hash tests; reject float money; use integer cost units including micro-USD where billing needs sub-cent precision. | Preserve verification with schema-versioned canonical evolution. | Codex | Mitigated |
| R-TRUST-005 | Trust chain / OpenAPI | MED | Public verify/well-known routes drift from OpenAPI contract. | SDKs and CLI call stale payload/response schemas; contract tests miss user-visible verification breakage. | Keep `openapi_consistency_test` as release gate; TRUST-B-5 required command is `GOCACHE=/tmp/go-build go test ./cmd/gateway/ -count=1 -timeout 60s`. | Preserve public verify API; contract drift blocks release until fixed. | Codex | Open |
| R-TRUST-006 | Trust chain / public verify DoS | MED | Public unauthenticated verify endpoint can be abused with large bodies or high request rate. | CPU/memory pressure on Ed25519 verification and JSON canonicalization path. | Body cap 10 KiB; anonymous per-IP rate limit 60/min; no auth by Owner D-B-verify-authn but bounded resource usage. | Preserve public no-auth verification with resource caps. | Codex | Mitigated |
| R-TRUST-007 | Trust chain / revocation config | HIGH | Revocation file is oversized, malformed, or points to the wrong operational data. | Revoked keys may stay trusted, or all verification paths may behave unpredictably during incident response. | Revocation file cap 1 MiB; file env takes precedence; fail-closed on oversize/parse error; tests cover file-env precedence and oversized file rejection. | Preserve CRL overlay; bad config fails closed instead of silently accepting revoked keys. | Codex | Mitigated |
| RR-W4-001 | Billing / Trust ledger | HIGH | Decision date: TBD (post-W4). 当 production money-path settle 被 `ValidateMoneyPathAuditRef` 拒绝时,W4c 走 D3 fallback（无 DLQ enqueue,仅 structured ERROR 日志）,需要建立 mandatory reconciliation 机制覆盖该路径。当前依赖 ops 通过日志告警发现并手动复理,自动 reconciliation 机制本体推后切片。 | 拒绝事件无系统化追踪渠道,ops 必须监控 `audit_ref_missing` 日志告警面。 | structured ERROR 字段含 request_id/tenant_id/claim_id/route_id；后续切片建 durable reconciliation table 或 DLQ enqueue API 扩展支持 settle-only intent。 | Mandatory Roadmap；不删除 settlement/audit-ref 功能。 | TBD (post-W4 scheduling) | Open |
| RR-W5-001 | Audit / DLQ replay | MED | W5 D2 锁定 ordinary admin / credential / channelhealth audit fail 不进 `audit_ledger_entry` DLQ (会与 W4 PreparedEntry replay 语义冲突);也不新增 EventKind。fail-closed semantic 是直接 reject mutation。后续若 ops 需要 durable replay 走独立队列+独立 worker, 不复用 trust ledger DLQ。 | audit DB 抖动时 W5 fail-closed = mutation reject = 短暂 admin/credential/channel-state UI 报错;ops 必须建监控告警 audit_write_failed 计数;复理走手工 admin operation 重试。 | structured ERROR 日志含 outcome/request_id/tenant_id;后续切片可建独立 audit_failure_replay 队列(不 reuse audit_ledger_entry);per-commit review 查无新 EventKind 注入 `audit_ledger_entry`。 | Mandatory Roadmap;不降级到无审计 mutation;不复用 trust ledger DLQ。 | TBD (post-W5 scheduling) | Open |
| RR-W5-002 | Audit / Antigravity OAuth refresh | HIGH | W5 synthesis C-03 「antigravity refresh audit fail-closed」原定改 `backend/internal/auth/antigravity_token_provider.go`, 但生产链路 review (2026-05-23 codex per-commit review) 证实 `AntigravityTokenProvider` 是 F-AUTH-005 v0.1 skeleton, **整个 cmd/gateway 没有构造它**;真实生产 refresh 路径走 `credentialworker.Scheduler.recordAudit` (`backend/internal/credentialworker/audit.go:15-44`) + `dbAuditWriter` (`audit.go:46-69`)。当前 `recordAudit`:audit insert + ledger Append 独立 `errors.Join` 非同事务;`dbAuditWriter` nil queries silent return nil(不 fail-closed)。fail-closed 接入点矫正给后续切片。 | 当 audit DB 抖动,生产 antigravity refresh 仍轮换 token + 填 cache, audit ledger 缺失,违 W5 D1/D4 既定决策;trust ledger 链可能掉条目。 | (1) `credentialworker.Scheduler.recordAudit` 改成 BeginTx-wrapped:audit insert + auditledger.AppendInTransaction 同 tx, 失败 rollback;(2) `dbAuditWriter.WriteRefreshAudit` queries nil production 报 ErrAuditWriterMissing;(3) `cmd/gateway/wiring.go` credentialScheduler 装配点 production-required gate 验 authQueries + auditLedger 非 nil;(4) 派生测试:audit insert 失败 / ledger append 失败 各一条 fail-closed 判别测试。 | 不删 antigravity refresh; fail-closed by mutation reject, ops 重试。 | TBD (W5 续切片或 W6 前置) | Open |
| RR-W5-003 | Audit / Receipt owner legacy backfill | MED | P0-1 receipt owner 隔离 sidecar 方案 (synthesis 2026-05-23,Owner 已批 D-001) 实施时,user_cost_receipt_owners 新 receipt 写入 100% NOT NULL,但 0028 既有 user_cost_receipts 历史行无 sidecar 映射,user endpoint 永久 404 (Owner D-002 批)。后续切片需建 backfill job 从 `billing_events.audit_request_id` (0029 加列) → audit_ledger_entries → billing_ledger_claims.user_id 派生 owner_source=backfill_join。 | 历史 receipt 用户永久 404 直至 backfill 完成;ops 暂时手工查 by request_id。 | (1) 写 backfill job 读 user_cost_receipts join `billing_events(tenant_id, audit_request_id)` → claim_id → billing_ledger_claims.user_id;(2) 批量 INSERT user_cost_receipt_owners owner_source='backfill_join';(3) ambiguous claim (多 user) 不写,记 metric;(4) dry-run 验证 matched/missing/ambiguous count。 | 不重写 receipt;不绕 append-only trigger。 | TBD (P0-1 收尾后) | Open |
| RR-W5-004 | Audit / Receipt owner DB-enforced user-claim 一致性 | MED | P0-1 C1 实施时 codex review 抓 HIGH:sidecar 单 FK `(tenant_id, claim_id) → billing_ledger_claims(tenant_id, id)` 不验 user_id 等。Owner D-010 2026-05-23 schema gate 已批 `(tenant_id, id, user_id)` superset UNIQUE + composite FK 加固,**batch 进 C1 migration 0052**。 | (resolved) DB 层防御已上,backfill/ingest 错绑被 DB reject。 | Owner D-010 批 + C1 0052 落地。 | 不降级 owner 隔离。 | C1 commit 时 close | **Closed by D-010 / C1** |
| RR-W5-005 | Audit / Receipt owner request-to-claim DB-enforced 关系 | LOW | P0-1 C1 codex review 第六轮抓 HIGH:sidecar composite FK 已验 (claim_id, user_id) 一致 (D-010),但**没验 (request_id, receipt_sequence) ↔ claim_id** 关系。坏 backfill 可绑 user B 的合法 claim 到 user A 的 receipt。当前 settler.go single-claim derive 应用层防御 + sidecar 走 settler 唯一写入路径 = application invariant 已守;backfill_join 切片 (RR-W5-003) 实现时 join 单一路径派生 = 不可错绑。DB 防线在 future ingest path 出现时补。 | application invariant 已防 settle / cache_hit / backfill_join 三个 owner_source 错绑;但 DB 层不强,future ingest path 若绕过 settler/backfill 可错绑。 | (1) 若 future ingest path 出现,加 billing_events (tenant_id, audit_request_id) UNIQUE + sidecar composite FK 到 billing_events 派生关系;(2) Integration test 验 settler.go 同 claim 派 receipt 不跨 claim。 | application invariant 守,DB 加固按需。 | TBD (future ingest path) | Open |
| RR-W5-006 | Audit / Receipt admin path | MED | P0-1 D-002 锁定「admin endpoint 可看 legacy + 跨 user receipt」,但 C4 实施时 `NewAdminCostReceiptGetHandler` (cost_receipt_handler.go) 缺 admin operator auth 依赖 + 未挂路由,留 future foot-gun(任何 caller 直接调即绕 owner 隔离),**C4 review 抓 HIGH 后删除转后续切片**实施时再带 admin.OperatorAuth 一起做。 | 老 receipt 用户暂时无 admin 复查 endpoint, ops 走 DB 直查 by request_id;P0-1 C2-C4 已落 storage.GetReceiptForAdmin 后端接口,前端入口缺。 | (1) 后续切片实施 admin endpoint 时:接 d.AdminAuth admin.OperatorAuthenticator;(2) 强制 platform-admin 或 tenant-operator scope;(3) routes.go 挂到 /admin/v1/receipts/...;(4) 测试覆盖 admin 强制 + scope 越权;(5) 复用 C2 已加的 GetReceiptForAdmin 后端 method。 | 不删 admin 复审 capability, 后续切片闭环。 | TBD (P0-1 收尾后) | Open |
| RR-W5-007 | Audit / Receipt verify owner-bound strict enforce | MED | P0-1 C4 实施时 codex review 抓 HIGH:verify handler `if valid && d.Receipts != nil` (cost_receipt_handler.go:275) 走 owner check, 但 Receipts nil 时 fail-open 走老 detached signature-only verify path。按 D-004 D-005 strict enforce 应该 Receipts nil → 503 dependency missing。改 strict 会 break 6+ 既有 detached verify tests (014/014b/014c/014d/029/043), 扩 P0-1 scope。**P0-1 接受应用层 caller wire (cmd/gateway/routes.go 已装 Receipts) 保证生产 Receipts !=  nil**;detached verify 测试假定 caller 配错时不应做 production. RR 转后续切片 strict enforce + 废弃 detached verify path 或改用 explicit fail-open flag。 | 生产 caller 装 Receipts 后正确;若 future code path forget装 Receipts, verify 仍走老 detached signature-only path 不 fail-closed。 | (1) 后续切片删既有 detached verify tests 或改装 Receipts stub;(2) handler 入口 nil Receipts → 503;(3) cmd/gateway/routes.go verify 装 wire 加 production-required gate (类似 channelhealth signer required pattern,W5 C2 已落)。 | 不降级 owner 隔离;生产配 Receipts 正确即可。 | TBD (P0-1 收尾后 + AGENTS.md per-commit review 加 verify dep production-required 项) | Open |
| RR-W5-008 | Money path / Post-delivery settle durable recovery DLQ persist 自身失败 | LOW | P2/P3 切片(synthesis 2026-05-24,Owner D-4 已批"只 alert 不加 disk spool"+ D-A 全 synthesis 实施 commit fc64a1f → f91c8ea)6 个 commit 把"流式响应已发给客户端 + Tx2 settle 失败"漏洞转成 durable DLQ 重放 + 三证 proof 防重复扣费,但当 DLQ persist 自身也失败(主 DB 完全不可写)时,settleCompletionWithRecovery 仅 slog P0 alert,不再嵌套 disk spool 二次兜底(Owner D-4)。三个借鉴项目 (sub2api/new-api/litellm) 在同等场景都只 log+人工,HUAKAI alert 是跟平。**(resolved by current slice + Owner D-4)** 进 RR 跟踪「再加 local disk spool」未来 Owner gate。 | (current slice resolves main P2/P3) 主 DB 完全不可写场景仍灰区,但 alert + grafana 抓后 ops 介入;概率极低(DB 全挂时多数请求都失败)。 | (1) 当前切片已落:0053 schema + settlementrecovery 包(payload/enqueue/handler/proof)+ gatewayhttp settleCompletionWithRecovery + eventbus billing handler 自定义 DLQ payload + cmd/gateway wire;(2) 未来若 disk spool 决策启动,需 Owner 单独 schema gate(新文件系统目录 / crash safety / 启动 replay 子系统)。 | 主漏洞已堵 + Owner D-4 拒升级 disk spool;后续若 Grafana alert 频繁触发再 Owner 重新 review。 | (closed by current slice for main P2/P3) Open for "DLQ persist 双环灰区" follow-up。 | **Main P2/P3 closed by commits fc64a1f → f91c8ea**; D-4 disk spool follow-up Open |
| R-SEC-002 | Rust data plane / Control plane transport | HIGH | RoutePlan now carries per-attempt upstream auth material; if the Rust data plane connects to the Go control plane over an unprotected network transport, upstream credentials can leak in transit. | Credential disclosure before vendor request, cross-tenant blast radius if the control-plane channel is sniffed or proxied. | Before mainline R-E接入, require mTLS, Unix domain socket, or equivalent local-only authenticated transport; keep exploratory mock as local-test only. | Preserve resolved auth material feature; gate production connection until transport protection is implemented. | Codex | Open |
| R-POOL-001 | Pooling / Legal | TBD | Upstream providers (OpenAI, Anthropic, etc.) detect quota-pooling and ban accounts. Sub2API community has reported this concretely. | Operator loses inventory mid-deployment; SaaS Edition risk is acute (one ban affects many tenants). | Per-Account fingerprint hygiene (don't burst the same Account from many IPs simultaneously), Provider Account affinity pinning ([F-POOL-AFFINITY-001](03_FEATURE_PARITY_MATRIX.md)), per-Account concurrency caps ([F-CONC-001](03_FEATURE_PARITY_MATRIX.md)), Account-health probes ([F-CH-002](03_FEATURE_PARITY_MATRIX.md)) with soft-disable instead of immediate hard-fail, operator-visible "burn rate" view per Account. Operator carries the legal risk; document this in Personal-Edition install README. | Preserve pooling capability with safety controls; do not delete F-POOL-001. | Claude | Open |
| R-BILL-002 | Billing / Pooling | TBD | A single request that retries or fails over crosses two Provider Accounts with different pricing; operator must decide which Account's price applies and how to attribute split usage. | Revenue leakage or end-user disputes; reconciliation drift compounds over time. | Versioned pricing context per Account at attempt time; Usage Record carries the per-attempt Account list; Billing Ledger entry references the ATTEMPT that succeeded plus an attribution policy ("first-tried", "succeeded-on", "dollar-weighted"); operator-visible reconciliation dashboard. Decide attribution policy in Phase 6 billing design. | Preserve pooling + retry + billing simultaneously with explicit attribution; no feature deletion. | Codex | Open |
| R-OPS-002 | Operations / Pooling | TBD | Sticky session pins a long conversation to Account A; A goes unhealthy; next request fails over to B; the upstream conversation context held by A is lost; user sees broken multi-turn. Also: skewed sticky-session distribution creates per-Account hot spots. | User-visible breakage on long sessions; uneven Account exhaustion. | Operator-visible per-Account session count + hot-spot diagnostic ([F-OPS-003](03_FEATURE_PARITY_MATRIX.md)); on sticky-pin loss, surface "context-may-be-lost" signal to client (not silent re-route); optional client-side context resubmission protocol. Phase 5 routing scope. | Preserve sticky-session capability with explicit context-loss surfacing; no feature deletion. | Codex | Open |
| R-LIC-002 | License / Specifier-lane drift | TBD | Option B (per [DR-000](process/decisions/DR-000-clean-room-methodology.md)) prevents intra-session contamination but does not address cumulative cross-session exposure: the same agent across many specifier sessions reads many AGPL/LGPL/GPL sources; over time the agent (or its config-cached patterns) carries upstream design knowledge into implementer sessions even with fresh context. | Subtle contamination invisible per-session; surfaces as "we somehow ended up matching upstream structure" months later. | Per-session contamination ledger: every session that read non-MIT source records source list + session id + retention policy; Phase 2 schema-lock includes a "contamination cool-off" rule (an agent that read Sub2API source in Session 47 cannot be the implementer for F-POOL-001 in Session 48 even with fresh context); spec-leakage review (CL-001..010) is the structural defense; `R-LIC-001` plus this row jointly cover both axes. | Preserve clean-room defense across time; no feature deletion. | Claude | Open |
| R-TRANSPORT-001 | Transport | HIGH | Exact TLS mimicry may need a patch or unsafe adapter around the selected TLS backend. | Production fingerprint mismatch can cause upstream/WAF rejection; crypto-adjacent patching also increases security review burden. | Keep exact transport behind feature flag, local capture gate, Owner real-upstream gate, and audit trail; no production dispatch until exact PASS. | Preserve mimicry capability as gated exact backend; no silent fallback or feature deletion. | Codex | Open |
| R-LIC-003 | License | HIGH | Browser/TLS mimicry preset utilities may carry LGPL/GPL terms even when adjacent core crates are permissive. | Accidentally adding a GPL/LGPL runtime dependency can contaminate the MIT distribution path. | Reject `wreq-util` and `rquest-util` for production use; run dependency license audit before adding any runtime transport dependency. | Preserve preset-like outcomes via HUAKAI-owned templates or permissive dependencies; no feature deletion. | Codex | Open |
| R-LIC-004 | License | LOW | `boring`, `boring-sys`, and `tokio-boring` package metadata/docs show permissive Apache-2.0 or MIT/Apache-2.0 licensing, not GPL/LGPL/AGPL. | Low residual notice/compliance work when adding the BoringSSL Rust transport path. | Accepted in [DR-009](process/decisions/DR-009-boring-license-clear.md); keep license evidence to package metadata/docs only and rerun dependency license checks once the registry index is reachable. | Preserve R-2-B Boring transport path; no feature deletion. | Codex | Mitigated |
| R-LIC-005 | License / Vendored dependency | LOW | HUAKAI now vendors the Apache-2.0 `boring` crate and MIT `boring-sys` crate for the R-3-A-fix local fork path; future local patches must preserve attribution and modification records. | Missing NOTICE or patch ledger updates could create avoidable Apache-2.0 §4 compliance gaps during release review. | Keep `exploratory/rust-core-gateway/vendor/boring/NOTICE` and `MODIFICATIONS.md` with source lock, license split, and per-wave patch history; do not silently patch vendored source without updating the ledger. | Preserve exact TLS mimicry fork path with notice discipline; no feature deletion. | Codex | Open |
| R-DEP-001 | Rust transport dependency | HIGH | `rquest` 5.x resolves only to yanked versions in the local crates.io index, and sandbox network cannot refresh crates.io. | Adding the requested dependency would make the Rust workspace fail dependency resolution before any code-level verification can run. | Do not write yanked `rquest` into Cargo manifests. Owner must choose a non-yanked replacement, approve a pinned locked/yanked exception, or authorize a HUAKAI-owned transport patch path. | Preserve R-2 exact BoringSSL/JA3 objective as blocked pending dependency decision; no feature deletion. | Codex | Open |
| R-DEP-002 | Rust transport dependency | MED | `boring` / `tokio-boring` depend on registry availability and a native build toolchain for BoringSSL-related crates, including bindgen/libclang and cmake. | `mimicry-boring` may fail at dependency resolution or native build before R-2-B logic can be compiled. | Sandbox 2026-05-17 fix: `apt install clang cmake libclang-dev` + `LIBCLANG_PATH=/usr/lib/llvm-21/lib`; baseline `cargo check -p core_gateway` PASS, `cargo check --features mimicry-boring` PASS (boring-sys 5.1.0 compiled in 1m51s via bindgen + clang-sys + cmake). Owner local + CI must replicate toolchain before R-8 release gate. | Preserve R-2-B exact mimicry path behind feature flag; no silent fallback. | Codex | Mitigated |
| R-REL-002 | Reliability | MED | OpenSSL fingerprint behavior can drift across OS distributions, package versions, or vendored build revisions. | A profile that passed capture in one environment may fail after base-image or library upgrade. | Pin build/runtime OpenSSL surface for exact profiles; capture artifact records OpenSSL version and crate checksum; recapture on every OpenSSL or image upgrade. | Preserve OpenSSL backend with explicit upgrade gate. | Codex | Open |
| R-TEST-001 | Testing | MED | Local capture PASS does not prove real provider or WAF acceptance. | Release could pass localhost packet checks but fail against the real upstream path because of endpoint, proxy, ALPN, or network variance. | R-D Owner real-upstream gate remains mandatory; local-only evidence cannot release production dispatch. | Preserve local capture as necessary but not sufficient evidence. | Codex | Open |
| R-MAINT-001 | Maintenance | MED | A HUAKAI-owned crypto or transport patch creates long-term rebase and CVE response burden. | Security updates may require urgent patch rebases; stale patches can block release or weaken transport safety. | Maintain patch ledger, keep scope minimal, verify upstream HEAD before rebases, and rerun capture tests on every rebase. | Preserve exact mimicry through maintained patch discipline rather than dropping the backend. | Codex | Open |

## Rule

Each high or release-blocking risk must map to mitigation, test coverage, and release gate status.

## 2026-05-15 Triage Notes

3 HIGH risk 已做 retrospective triage（codex γ lane + Claude 平行 review）：

- **R-SEC-002** (Rust 数据面 / 控制面 transport, HIGH)：保留 Open，前置条件已并入 `docs/process/plans/2026-05-15-r-3-r-e-mainline-codex.md` 的 R-E 切换闸门（必须先实施 mTLS / UDS / 本地认证 transport）。Last triage 2026-05-15。下一步:UDS vs TCP+mTLS 设计决策点在 R-E plan OCAW D1。
- **R-TRANSPORT-001** (Exact TLS mimicry patch burden, HIGH)：保留 Open。L2-A5 系列已落 feature flag + KnownGap 严格 fail-fast + provenance 记录；未来 OpenSSL upstream 升级时仍需打 patch 的风险无法零。Last triage 2026-05-15。下一步:补 OpenSSL patch policy 文 + 真上游回放回归定时任务（R-E ramp Phase 2 之前必须就位）。
- **R-LIC-003** (GPL/LGPL runtime dep creep, HIGH)：保留 Open。Round 2-A deny 修复已确认 `webpki-roots 1.0.7` 使用 SPDX 标识 `CDLA-Permissive-2.0`；该许可证按 permissive 数据许可加入 `deny.toml` allow-list，不触发 GPL/LGPL/AGPL/copyleft 路径。`protobuf 2.28.0` 命中 `RUSTSEC-2024-0437`，cargo-deny 给出的修复线为 `>=3.7.2`；当前由 `prometheus 0.13.4` 默认 feature 拉入且 `cargo update -p protobuf --offline` 无法升出 2.x，处置为关闭 `prometheus` 默认 feature，保留文本 scrape 指标并移除 vulnerable transitive dependency，未添加 advisory ignore。Last triage 2026-05-15。下一步:保持 `cargo deny check` 作为 R-LIC-003 gate；`wreq-util` / `rquest-util` ban 不放松。

证据收敛文档：
- 详细 mitigation 收敛 + 命令清单：[docs/process/reviews/2026-05-15-high-risks-mitigation-codex.md](process/reviews/2026-05-15-high-risks-mitigation-codex.md)
- Claude 平行视角：[docs/process/reviews/2026-05-15-high-risks-mitigation-claude.md](process/reviews/2026-05-15-high-risks-mitigation-claude.md)

未删除、未降级任何 risk 行；只在表后追加 triage 记录。

## 2026-05-25 Triage Notes (账号转 API 主线 8 vendor + auth_expired S1+S2+S3)

今天工作累积识别 6 项新 risks（其中 2 项已修,4 项 DEFERRED）。

- **R-SEC-003 (Vendor refresher SSRF / token exfil, S0, FIXED)**：Cursor + Windsurf refresher.go 从 credential JSON 读 `oauth_token_endpoint/token_endpoint/client_id/scope` → 攻击者改 credential → refresh token 发到攻击者 URL → token 外泄。Owner 直接发现。修复:endpoint/client/scope **仅** 从 admin operator config (a.TokenURL/a.ClientID/a.Scope) 读,credential JSON 仅 refresh_token 数据源。SSRF 防御 unit test (credential 注 evil URL + a.TokenURL 空 → fail-closed)。所有 8 vendor refresher (anthropic/copilot/cursor/windsurf/openai_codex/kiro/gemini/antigravity) 同模式 hardened。Last triage 2026-05-25 commit `cd8a5f4` (Cursor) + `707d556` (Windsurf) + 后续 5 vendor 同模式。

- **R-AUDIT-001 (Admin UI severity mapping for 4 new outcomes, S2, FIXED)**：observability.sql severity case 漏 auth_expired/rate_limit_exceeded/risk_control_triggered/account_disabled → admin UI warning/error 过滤漏报。修复:severity case 新加 auth_expired/risk_control_triggered/account_disabled → error / rate_limit_exceeded → warning。Last triage 2026-05-25 commit `cd8a5f4`,DEFERRED-audit-outcome-severity-mapping.md [CLOSED]。

- **R-SIDECAR-001 (boring raw sigalgs, RESOLVED 2026-06-02)**：已修。vendored boring 增 raw setter,`boring_ctx.rs:44` 用 `set_raw_verify_algorithm_prefs(&profile.signature_algorithms)` 发完整 26 个数字 sigalg ID(`profile.rs:19`),wire 测试 `boring_signature_algorithms_profile_controls_wire_bytes`(`boring_ctx.rs:382`)断言 26 个全匹配且删空 mutation-red。commit `8cfc5467`。

- **R-SIDECAR-002 (H2 ALPN raw tunnel 边界, RESOLVED 2026-06-02)**：已按选项 B 修。`connect.rs:118-119` 当 `selected_alpn==Some(b"h2")` 时走 `start_profile_h2_connection`(Rust 端 own H2 framing),不再返 raw TLS;`h2_bridge.rs` 做 IPC HTTP/1.1↔上游 H2 桥接 + Stage-2 流式 + 防请求走私(拒重复 Content-Length/Host)。commits `c0fe5231`/`76f31ea7`/`d3d18933`,cargo test 36/0。**残留(非本票)**:anthropic profile 的 H2 SETTINGS 指纹值未抓(`profile.rs:38-42` `[profile.h2_settings]` 空 + `TODO(Phase 3 real capture)`,`profile.rs:390` 测试断言空),启用生产 sidecar 前抓一次即补全 H2 层指纹。

- **R-VEND-001 (6 vendor operator-config OAuth endpoint 缺, S1, OPEN-PENDING-OWNER)**：cursor/windsurf/openai_codex/kiro/gemini/antigravity 6 vendor RefreshAdapter operator-config endpoint/client/scope 缺。Owner 真账号抓包后才能填入 `HUAKAI_<VENDOR>_OAUTH_{AUTH_URL,TOKEN_URL,CLIENT_ID,CLIENT_SECRET,SCOPE}` env。**当前 fail-closed**:TokenURL 空 → `WithVendorRefresher` 不注入 → vendor 自动 skip,不 crash。Owner-side action:per [[feedback_owner_local_verification]] Owner 本机抓 cursor/windsurf/codex CLI/kiro/gemini Advanced/antigravity OAuth flow。Last triage 2026-05-25 commit `e1af326`。

- **R-TEST-002 (Sandbox TCP bind 限制 20+ tests #[ignore], S3, DEFERRED)**：core_gateway 多个集成测试 `TcpListener::bind("127.0.0.1:0")` 在 sandbox 报 `Operation not permitted`。attempt_reporter_test (commit `ecc9a5c`) 8 用例 `#[ignore]` + listener_test (commit `c58afcb`) 2 用例 `#[ignore]` + route_client_test spillover 3 用例。修复方式:in-memory mock + `#[ignore]` 双轨;in-memory 保留 mutation discriminating 断言,TCP 集成在 bind-allowed env 单独跑。CI 需 bind-allowed env 跑 `--ignored` profile 才完整覆盖。Last triage 2026-05-25。下一步:Owner / CI infra 提供 bind-allowed test runner 覆盖 ignored cases。

新 DEFERRED 票汇总 (docs/process/reviews/):
- DEFERRED-audit-outcome-severity-mapping.md [CLOSED 2026-05-24 by S2 commit cd8a5f4]
- DEFERRED-windsurf-storage-handler.md (S2)
- DEFERRED-windsurf-refresher-audit-outcome.md [CLOSED 2026-05-24 by S2/Cursor SSRF fix]
- DEFERRED-sidecar-raw-tunnel-h2-alpn.md (S2)

待 Owner 决策:
- AE-D3 admin UI detailed cause (8 D 决策 §F docs/process/plans/2026-05-25-ae-d3-admin-ui-detailed-cause-synthesis.md, 推 Mixed D = B target + 可选 C 加速)
- BS45-SD1..SD5 Phase 4-5 ECH+PQ (5 D 决策 §F docs/process/plans/2026-05-24-boringssl-phase-4-5-synthesis.md)
- R-3-A-fix-6 命名 + 实施时机 (Phase 5C pre-production gate)

## 2026-05-27 Triage Notes (ChatGPT OAuth CHG-1..4 commit 7a34ae7)

CHG-1..4 真接通 openai/chatgpt_oauth OAuth 流 (双源验证 ClientID `app_EMo...` from CLIProxyAPI@21fad9d8 + openai-codex Apache-2.0; PKCE-only 不需要 ClientSecret + redirect_uri 双模式 allowlist + 3 OpenAI 特定 query params + offline_access scope + ChatGPT metadata persist + endpoint pin 不读 cred + refresh_token fail-closed + session_token sync after refresh)。Owner 2026-05-27 决策 D-1=C/D-3=A/D-5=A/D-4=A; defaults D-2=A/D-6=A/D-7=A。

新增 4 项 ChatGPT 相关 risks (2 deferred Mandatory Roadmap, 2 mitigated by current commit):

- **R-CHG-MIMICRY-001 (ChatGPT codex_cli mimicry sidecar 未启用, S2 → Mandatory Roadmap, DEFERRED)**: D-4=A Owner 决策本轮只接 OAuth, 不启用 codex_cli mimicry profile + Rust runtime preflight。HUAKAI W11-F §14b codex_cli wire ja3 已 PASS ([[project_huakai_codex_mimicry_verified]]), 但 Rust 生产 dispatch builder 对 Pending fail-closed (Rust review d55fa24 S1-2 Gemini production builder 同款问题, 等 Rust 分支实现者修 F-2.3a runtime preflight)。**Status: Deferred 2026-05-27 commit 7a34ae7; 等 OCAW 或 Rust §14b runtime preflight 接通**。

- **R-CHG-REVOKE-001 (ChatGPT OAuth revoke endpoint 未实施, S2 → Mandatory Roadmap, DEFERRED)**: openai-codex@HEAD:codex-rs/login/src/auth/manager.rs:95 声明 REVOKE_TOKEN_URL = "https://auth.openai.com/oauth/revoke" 用于显式 OAuth token revocation。HUAKAI 本切片 D-7=A 不实施 — credential lifecycle 当前覆盖 acquisition + refresh, 不覆盖 explicit revoke (账号删除走 admin DELETE endpoint, 但不 emit OAuth revoke 给 OpenAI)。**Status: Deferred 2026-05-27 commit 7a34ae7; 后续 credential lifecycle 切片 (revoke + key rotation) 时 close**。

- **R-CHG-SESSION-TOKEN-SYNC-001 (ChatGPT refresh 后 session_token 同步, S1, MITIGATED by R1 fix)**: R1 codex review 抓出 chatgpt_oauth credential payload acquisition 时 access_token 同步写到 session_token (与 anthropic / gemini 一致 runtime material 解析), 但 ChatGPTRefresh 只更新 access_token 不更新 session_token。credentialstore 解 runtime material 时优先看 session_token → refresh 报成功但仍用旧过期 access_token = 真 prod bug。**Status: Mitigated 2026-05-27 commit 7a34ae7 (R1 fix); TestChatGPTRefreshSyncsSessionTokenFromAccessToken 守门 (mutation: 不写 session_token 同步 → 红)**。

- **R-CHG-ADMIN-FLOW-ID-001 (Admin OAuth redirect flow_id 保留, S1, MITIGATED by R2 verify inline fix)**: R2 verify codex 抓出 admin HTTPS allowlist 启用后, authorize URL 把静态 admin callback 作 redirect_uri, OpenAI 完成 OAuth 后 redirect 只带 state+code, 不带 flow_id → mounted `/admin/v1/credentials/oauth-callback` 调 `d.Sessions.Get("")` 失败。修法: authorize URL builder 在 admin 模式时把 flow_id append 到 redirect_uri query, OpenAI redirect 后 mounted handler 从 query 读 flow_id 成功 lookup session (loopback 模式 user agent 直接 preserve query 不需要修)。**Status: Mitigated 2026-05-27 commit 7a34ae7 (R2 verify inline fix); TestChatGPTAuthorizeURLPreservesFlowIDInAdminMode 守门**。**注: gemini admin allowlist 同款路径默认空未启用, 未来 wiring 启用时同样需要 flow_id preserve — 记 R-GEM-FLOW-ID-001 follow-up**。

借鉴项目对照 (CLAUDE.md #15, 详细 evidence rows in [docs/07 §Behavior Evidence — ChatGPT OAuth 借鉴对照](07_REFERENCE_EVIDENCE_LEDGER.md)):
- CLIProxyAPI@21fad9d8:internal/auth/codex/openai_auth.go:24-27,72,180 = ClientID + endpoints + PKCE-only + token 文件持久化 token_uri 反例 (HUAKAI 不抄)
- openai-codex@2026-05-12-HEAD:codex-rs/login/src/auth/manager.rs:921,93-95 = 双源验证 ClientID + endpoint pin + revoke endpoint + ChatGPT metadata 字段

后续 (Mandatory Roadmap):
- R-CHG-MIMICRY-001 解锁 = Rust §14b runtime preflight 接通 + OCAW codex_cli mimicry 启用
- R-CHG-REVOKE-001 解锁 = credential lifecycle 切片 (revoke + rotation 同时做)
- R-GEM-FLOW-ID-001 follow-up = gemini admin allowlist 启用时同步加 flow_id preserve (与 ChatGPT 同款修)

## 2026-05-27 Triage Notes (Gemini OAuth GEM-1+2+3 commit 89947e0 + 78a010a)

GEM-1+2+3 真接通 gemini/code_assist + gemini/google_one 真实 OAuth 流 (PKCE + Google CLI 公开 ClientID + env var ClientSecret + redirect_uri 双模式 allowlist + offline_access/consent + 4 SSRF-D 攻击面闭合 + cred 永不持久化 secret)。Owner 2026-05-27 决策依据: D-1=A 内置 ClientID + env var ClientSecret, D-3=C redirect_uri 双模式 (loopback + HTTPS admin allowlist), D-4=A 三项 scope, D-5=A mimicry Mandatory Roadmap。

新增 3 项 Gemini 相关 risks (1 mitigated, 2 deferred Mandatory Roadmap):

- **R-GEM-FALLBACK-001 (Gemini cross-client fallback explicit disabled, S2, MITIGATED via Owner D-2=A)**: GeminiRefresh.AllowCrossClientFallback 在 code_assist/google_one wiring 显式 = false。原因: Google desktop CLI 设计本意是单一 ClientID 跨 code_assist/google_one/ai_studio 三家 — fallback 不能靠切 ClientID 实现 (从 cred 读 fallback_client_id 是 S1-D 攻击面, Owner D-2=A 撤回 cred 取信)。HUAKAI 自维护 approvedGeminiCrossClientID family→ClientID mapping 现返同一公开 ClientID, primary 与 fallback 永远等值 → 守卫永远跳过 fallback。改成 explicit `AllowCrossClientFallback: false` + 注释说明 + 留 mapping 函数给未来 application-level fallback (例: 切 scope / 切 token shape) 用。**Status: Mitigated 2026-05-27 commit 78a010a; TestGeminiRefreshFallbackExplicitlyDisabledForGoogleCLI 守门**。

- **R-GEM-MIMICRY-001 (Gemini Chrome/CLI mimicry sidecar 未做, S2 → Mandatory Roadmap, DEFERRED)**: D-5=A Owner 决策本轮只接 OAuth, 不做 Gemini CLI / Gemini Advanced / Code Assist 的 mimicry sidecar profile + 真 fingerprint capture。Rust 分支 W11-F §14b Chrome impersonation (4 vendor profile) **生产 dispatch 还未启用** (Rust review d55fa24 S1-2: Gemini production builder 对 Pending fail-closed, F-2.3a runtime preflight 未完成)。Antigravity 共用 Gemini 设备指纹 (gemini-advanced 模板不可直接当 Code Assist 用, R-GEM-MIMICRY-001 风险已在 GEM synthesis 拍出)。**Status: Deferred 2026-05-27, 等 OCAW 或 Rust W11-F §14b runtime preflight 接通后单独切片实施**。

- **R-GEM-ANTIGRAVITY-PAUSED-001 (Gemini/antigravity refresh 临时 fail-closed, S1 → Mandatory Roadmap, DEFERRED)**: GEM-3 R2 verify 抓出 antigravity 共用 GeminiRefresh adapter 但 mode_refresh.go:81 没注入字段 — GeminiRefresh 内部 fallback 把 legacy antigravity credential silently posted to Google Gemini endpoint with public CLI client = **token leak 风险** + 现有 antigravity 用户 refresh 全坏。Owner 2026-05-27 决策 (R2 verify Option 1): gemini/antigravity 注册改为 geminiAntigravityPausedAdapter, refresh 直接返 `antigravity OAuth wiring pending Owner reactivation: %w ErrFeatureDisabled`, 不发 HTTP 请求 (mock 0 calls 守门)。现有 antigravity 用户在 OCAW 重启前不能 refresh 但也不会 token leak。**Status: Deferred 2026-05-27 commit 78a010a; TestGeminiAntigravityRefreshIsFailClosedUntilReactivation 守门; 后续 OCAW 切片重启 antigravity OAuth wiring 时 close**。

借鉴项目对照 (CLAUDE.md #15, 详细 evidence rows 在 [docs/07_REFERENCE_EVIDENCE_LEDGER.md](07_REFERENCE_EVIDENCE_LEDGER.md) §"Behavior Evidence — Gemini OAuth ClientSecret 借鉴对照"):
- CLIProxyAPI@21fad9d:internal/auth/gemini/gemini_auth.go:32+180 = ClientSecret 硬编 + 持久化 token 文件 (HUAKAI 不抄, 信任链 invariant)
- litellm@HEAD:litellm/proxy/management_endpoints/ui_sso.py:2082-2084 = env var GOOGLE_CLIENT_SECRET + missing_vars fail-fast (HUAKAI Option 1 同源)
- llmgateway@HEAD:apps/api/src/auth/config.ts:613 + .env.example:92 = env var only + docker/helm 集成 (HUAKAI Option 1 同源)

OCAW 重启 Gemini 路径 (Owner 后续切片) 必须:
1. 决定 antigravity 是否启用 (R-GEM-ANTIGRAVITY-PAUSED-001 解锁)
2. 决定 mimicry sidecar 是否本切片做 (R-GEM-MIMICRY-001 解锁)
3. 决定是否启用 HTTPS admin callback (GEM-1+2 默认 allowlist 空, 仅 loopback 接 — 后续 wiring 需要静态注入 admin URL allowlist)
