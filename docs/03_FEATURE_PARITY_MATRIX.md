This file is agent-facing and authoritative.

# Feature Parity Matrix

## Purpose

This matrix tracks reference-derived features and ensures full feature parity or better. It is a living control document, not a wishlist.

## Valid Dispositions

| Disposition | Meaning |
| --- | --- |
| `Implemented` | The feature exists with equivalent behavior. |
| `Implemented Better` | The feature exists with materially stronger behavior, safety, UX, or operability. |
| `Merged Equivalent` | Multiple reference features are covered by one broader capability. |
| `Safe Equivalent` | The same user outcome is delivered with a safer clean-room design. |
| `Plugin` | The feature is supported through a plugin boundary. |
| `Feature Flag` | The feature exists but is gated for rollout, risk, or deployment policy. |
| `Mandatory Roadmap` | The feature is not implemented yet but is required before parity closure. |

Invalid dispositions: `Dropped`, `Ignored`, `Not Needed`, `Too Risky`, `License Risk`, `Out of Scope`.

## Disposition vs Status

The **Disposition** column is the **target** disposition: the planned shape the feature will take when delivered at its target level (L1/L2/L3/L4 in [17_FEATURE_LEVEL_MATRIX.md](17_FEATURE_LEVEL_MATRIX.md)). It is set when the feature is mined and only changes when the design intent changes.

The **Status** column reflects the **current** implementation state: `Open` (not yet built), `Built`, `Released`, `Mitigated`, etc. A feature can have Disposition `Implemented Better` and Status `Open (L1 MVP)` simultaneously — that means "we plan to deliver this with stronger behavior than the reference, and we have not built it yet." Release-readiness gates check the Status column; design-intent audits check the Disposition column.

This separation is intentional. If we forced Disposition to track Status (e.g. all unbuilt rows must say `Mandatory Roadmap`), we would lose the design-intent metadata that distinguishes a planned `Plugin` from a planned `Implemented Better` from a planned `Safe Equivalent`. Instead, both axes are preserved.

## Mined Features — Phase 1 First Batch (one-api)

First mapping pass, 2026-04-28. Each row references one or more `E-OAI-*` evidence rows in [07_REFERENCE_EVIDENCE_LEDGER.md](07_REFERENCE_EVIDENCE_LEDGER.md). Capabilities map to [02_CAPABILITY_CONTRACT.md](02_CAPABILITY_CONTRACT.md). Test IDs are placeholders pending [11_ACCEPTANCE_TEST_MATRIX.md](11_ACCEPTANCE_TEST_MATRIX.md) population.

| Feature ID | Reference | Evidence ID | User Outcome | Risk | Disposition | Local Capability | Test ID | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-GW-001 | one-api | E-OAI-001 | Client sends one OpenAI-compatible request and reaches the right upstream provider. | Mis-routing leaks request to wrong account / provider. | Implemented | Gateway core: model-driven Route → Channel → Provider Account selection. | AT-GW-001 | Claude | Open (L1 MVP) |
| F-GW-002 | one-api | E-OAI-002 | Streaming and non-streaming requests both work with consistent usage accounting. | Streaming partial-usage drift; double-billing on retry (BUG-GW-001/002). | Implemented Better | Stream and non-stream paths share one accounting engine that records partial-token state separately from final settlement. **A25 升级** (Phase C, P0): adaptive buffer sized at historical P99×4 with memory-pressure clipping (AIMD). **A26 升级** (Phase C, P0): drain decision E[value]>E[cost] default-greedy with three-budget early-stop; drain reads token-usage metadata only (DR-009 Q5 privacy). | AT-GW-002-01..19 | Claude | **Spec Released 2026-04-28** ([docs/specs/streaming-forwarder.md](specs/streaming-forwarder.md)) — Codex APPROVE-WITH-FIXES; 10 fixes applied. Awaits Phase 2 schema. |
| F-AUTH-001 | one-api | E-OAI-003 | A user signs in via email or any of N OAuth providers. | Hardcoding providers blocks Phase 9 SSO additions. | Plugin | Pluggable auth provider abstraction; email + at least one OAuth provider in MVP; remaining providers added as plugins. | AT-AUTH-001 | Claude | Open (L1 MVP for email + 1 OAuth) |
| F-AUTH-002 | one-api | E-OAI-004 | Session survives server restart and works across multiple instances. | Default-generated secrets, secret reuse. | Implemented Better | Operator-supplied session secret required on first run (no default); horizontal-scale-ready session store. | AT-AUTH-002 | Claude | Open (L1 MVP) |
| F-KEY-001 | one-api | E-OAI-005 | Operator issues API keys to a user with expiration and per-key quota. | "Balance OK but key out" confusion. | Implemented Better | API Key lifecycle (issue / disable / rotate / expire) with per-key quota that is reconciled against account balance in a single Usage Record query path. | AT-KEY-001 | Claude | Open (L1 MVP) |
| F-BILL-001 | one-api | E-OAI-006 | Per-request cost reflects user group, model, and completion vs prompt token weighting. | Pricing drift; non-replayable historical cost. | Implemented Better | Versioned pricing context attached to every Usage Record; reprice operation operates on stored context, not live config. **A15 升级** (Phase A, P0): versioned pricing vector dot-product over reasoning/audio/image/tool token fields against Merkle-chained pricing snapshot. **A16a 升级** (Phase D, P1): drift reconciliation — post-hoc authoritative usage correction emits immutable adjustment pairs. **A16b 升级** (Phase E, P2): expression AST compile for pricing formula evaluation (50-200µs→1µs). | AT-BILL-001 | Codex | **Framing Released within F-OBS-001 spec** 2026-04-28; per-request cost engine details remain L2 deferred (atomic Tx1/Tx2 settlement is documented in [docs/specs/observability-billing.md](specs/observability-billing.md)). |
| F-BILL-002 | one-api | E-OAI-007 | Operator issues redemption vouchers; users redeem vouchers to top up balance. | Voucher reuse, voucher expiry, audit gaps. | Mandatory Roadmap | Voucher entity with one-time redemption, expiry, audit event, and per-issue cap. Phase 6+. | AT-BILL-002 | Codex | Open (L3 Phase 6+) |
| F-CH-001 | one-api | E-OAI-008 | Operator creates and configures channels; channels expose a model allow-list; bulk creation supported. | Bulk action without per-channel audit. | Implemented | Channel CRUD + per-channel model allow-list + bulk-create with one audit event per channel + one bulk-summary audit event. | AT-CH-001 | Claude | Open (L1 MVP) |
| F-CH-002 | one-api | E-OAI-009 | Channel health is probed periodically; channels can be auto-disabled below a success threshold. | Silent auto-disable hides upstream incidents; CF-blocking misclassified. | Implemented Better | Health probe + parser that distinguishes "upstream returned non-JSON" from "upstream down"; auto-disable raises an alert and requires operator-confirm-resume. | AT-CH-002 | Codex | Open (L2 deferred) |
| F-GROUP-001 | one-api | E-OAI-010 | User Group × Channel Group resolves to differential pricing and routing eligibility. | Pricing surprises from group cross-product. | Implemented | User Group + Channel Group as first-class entities with auditable resolution rules. | AT-GROUP-001 | Claude | Open (L2 deferred) |
| F-UI-001 | one-api | E-OAI-011, E-OAI-012 | Operator customizes branding (homepage, about, top-bar) and onboarding (announcements, recharge links, initial balance). | Iframe embed XSS; initial-balance abuse via mass registration. | Mandatory Roadmap | Branding fields with sandboxed iframe; initial-balance tied to identity-verification gate. Phase 7+. | AT-UI-001 | Gemini | Open (L3 Phase 7+) |
| F-OBS-001 | one-api | E-OAI-013 | Operator dashboard shows per-request quota detail and per-channel success rate; thresholds tunable. | Silent threshold-based disabling. | Implemented Better | Dashboard + alert-on-disable + operator-confirm-resume + atomic Tx2 settlement (carries F-BILL-001 framing correction: durable end-to-end billing pipeline, no lossy worker-pool tier). **A09 升级** (Phase B, P0): 2-phase quota reserve+settle — Tx1 reserve(scopes) + attempt records + Tx2 settle; billing attribution: client bill shows `succeeded_on`, analytics shows `dollar_weighted` (DR-009 Q2 + hard floor). **A10 升级** (Phase D, P1): quantile reserve estimator — tiered P99/P95/P90 by user tier; emits `X-Huakai-Quota-Headroom-Usd` (DR-009 Q4). **A27 升级** (Phase E, P2): stream-time quota dynamic reserve extension for long-running streams. | AT-OBS-001..022 | Claude | **Spec Released 2026-04-28** ([docs/specs/observability-billing.md](specs/observability-billing.md)) — Codex APPROVE-WITH-FIXES; 10 fixes applied including worker-pool gap correction + immutable Usage Record reconciliation. |
| F-SEC-001 | one-api | E-OAI-014 | CAPTCHA gates registration/login; per-IP rate limit gates abuse paths. | Default thresholds too loose for SaaS. | Plugin + Implemented | CAPTCHA as plugin (Turnstile + alternatives); rate limit always-on with operator-tunable thresholds; SaaS Edition tightens defaults. | AT-SEC-001 | Codex | Open (L1 MVP rate limit; L2 CAPTCHA plugin) |
| F-SEC-002 | one-api | E-OAI-015 | Privileged management API exists; first-run bootstrap credential is rotatable. | Default credentials (`root/123456`) antipattern. | Implemented Better | RBAC tier for management API; first-run bootstrap forces credential change before any other operation; never ships hardcoded password. | AT-SEC-002 | Codex | Open (L1 MVP) |
| F-GW-003 | LiteLLM, Helicone | E-LM-001, E-HLC-005 | Operator can assert and verify a per-request latency target AND a runtime resource budget (memory, binary size, cold start) in production. | Marketing-vs-production benchmark drift; vendor-claimed numbers under-represent real payload mix. | Implemented Better | Local SLO doc + automated p95/p99 latency measurement and resource-budget measurement under representative payload mix; SLO/budget violation triggers alert, not silent. **A17 升级** (Phase C, P0): capacity depletion forecast — EWMA + seasonal P95 → ETA until exhaustion with optional STL decomposition (P1 opt). **A18 升级** (Phase D, P1): bounded knapsack restock recommendation — selects optimal account restock mix across ≥5 account types. | AT-GW-003 | Codex | Open (L2) |
| F-GW-004 | LiteLLM, Portkey | E-LM-002, E-PK-001, E-PK-002 | Failed request transparently retries / falls over with exponential backoff and operator-visible reason on each fallback. | Retry / fallback amplifies cost; silent fallback hides upstream incidents. | Implemented | Per-request retry policy with backoff schedule; per-account spend cap; every fallback emits an operator-visible reason. **A11 升级** (Phase B, P0): bounded multi-attempt DAG planner with edge types (refresh/spill/model_subst/terminal); minimum expected-time path; all attempts write audit rows for dollar_weighted analytics (DR-009 Q2 hard floor). **A12a 升级** (Phase B, P0): stream-safe retry boundary FSM (BEFORE_FIRST_TOKEN/CONTENT_STARTED/TOOL_SIDE_EFFECT_STARTED) — blocks retry after first token to prevent double-billing (DR-009 hard floor). **A12b 升级** (Phase D, P1): retry decision matrix (error_class × attempt_n × budget) → action. | AT-GW-004 | Codex | Open (L1 MVP retry; L2 cross-deployment fallback) |
| F-TIMEOUT-001 | Portkey | E-PK-007 | Per-request timeout is operator-tunable per model. | Aggressive timeout cuts legitimate long completions. | Implemented | Per-request timeout policy with per-model defaults; operator-tunable; emitted on the Usage Record on cut. | AT-TIMEOUT-001 | Codex | Open (L1 MVP) |
| F-RBAC-001 | Portkey | E-PK-008 | RBAC roles per user / workspace / API key; revocation propagates instantly with audited "who lost access" diff. | Cascading permission changes surprise operators. | Implemented Better | RBAC engine with explicit revocation propagation guarantee; UI shows the diff after each role change. | AT-RBAC-001 | Codex | Open (L2) |
| F-CACHE-001 | Portkey | E-PK-005 | Responses can be cached, both by simple key and by semantic similarity. | Stale cache for time-sensitive queries. | Mandatory Roadmap | Simple cache at L2; semantic cache at L3+. Always with TTL and operator-invokable invalidation. | AT-CACHE-001 | Codex | Open (L2 simple; L3 semantic) |
| F-GUARD-001 | Portkey | E-PK-003 | Output is validated against operator-selected guardrail rule packs. | False positives reject valid output. | Plugin | Pluggable guardrail engine; operator chooses rule packs; bypass path requires operator role and audit. | AT-GUARD-001 | Codex | Open (L2 plugin shell; L3 first packs) |
| F-MM-001 | Portkey | E-PK-004 | Single OpenAI-compatible request shape supports text, vision, audio in/out, image generation. | Provider capability variance is large. | Mandatory Roadmap | Multi-modal request normalization. Phase 9+. Capability matrix per model required before claim. | AT-MM-001 | Codex | Open (L3 Phase 9+) |
| F-RT-001 | Portkey | E-PK-006 | Real-time WebSocket request surface is supported (OpenAI Realtime-style). | Mid-stream drop loses partial usage. | Mandatory Roadmap | WebSocket protocol surface with stream resume + partial usage settlement. Phase 9+. | AT-RT-001 | Codex | Open (L3 Phase 9+) |
| F-PROTO-001 | LiteLLM | E-LM-006 | Gateway bridges external agent/tool protocols (MCP, A2A) so any LLM can use them. | Tool execution surface = malicious-tool risk. | Plugin | External-protocol bridging behind a plugin boundary; tool execution sandboxed; protocol versions explicit. Phase 9+. | AT-PROTO-001 | Codex | Open (L3 Phase 9+) |
| F-AUTH-003 | LiteLLM | E-LM-005 | Operator can plug in enterprise SSO (SAML / OIDC) as an auth provider. | Single-tenant SSO behaves differently from SaaS multi-tenant SSO. | Plugin | SSO as additional auth-provider plugin under F-AUTH-001 abstraction; SaaS Edition (Phase 10+) defines tenant-scoped SSO. | AT-AUTH-003 | Codex | Open (L3 Personal; L4 SaaS) |
| F-TENANT-001 | LiteLLM, Portkey | E-LM-003, E-PK-008 | Per-tenant configuration travels with the credential: logging level, guardrail pack, caching policy. | Mis-scoped config leaks between tenants. | Implemented | Per-tenant config rows referenced by API Key resolution; cross-tenant tests required (DR-001 isolation tests). | AT-TENANT-001 | Codex | Open (L2 Personal default tenant; full at L4 SaaS) |
| F-SEC-003 | LiteLLM | E-LM-004 | Operator can verify the running container's image was signed by the project. | Verification optional in many deployments. | Mandatory Roadmap | Signed images + SBOM as Phase 8 production-hardening deliverable; required for SaaS Edition. | AT-SEC-003 | Codex | Open (L3 Phase 8) |
| F-BILL-003 | New API | E-NAI-001 | Cached-prompt tokens are billed at a different rate than fresh tokens; UI explains why a repeat prompt costs less. | Users surprised by repeat-prompt cost variance. | Implemented Better | Versioned pricing context already includes cache-hit field per F-BILL-001; UI surfaces cache-hit reason inline on Usage Record. | AT-BILL-003 | Codex | Open (L3 Phase 6+) |
| F-PROTO-002 | New API | E-NAI-003 | Single client request shape is translated across provider protocols (OpenAI ↔ Claude, Gemini → OpenAI), with explicit capability matrix exposing what is lost in translation. | Silent semantic loss (e.g. dropped function calls). | Implemented Better | Protocol translator with explicit per-pair capability matrix; conversion that loses capability emits operator-visible warning + records `protocol_loss` field on Usage Record. | AT-PROTO-002-01..16 | Codex | **Spec Released 2026-04-28** ([docs/specs/protocol-translation.md](specs/protocol-translation.md)) — Codex APPROVE-WITH-FIXES (after initial REJECT for F-PROTO-001 mislabel). |
| F-MODEL-001 | New API | E-NAI-004 | Client passes reasoning-effort parameter (high / medium / low) and per-request thinking-token budget; gateway records actual thinking-token spend. | Silent truncation of thinking phase. | Implemented | Reasoning-effort parameter flows through Usage Record with separate thinking-token field; truncation surfaces operator-visible signal. | AT-MODEL-001 | Codex | Open (L2) |
| F-MODEL-002 | New API | E-NAI-005 | Dedicated rerank-model surface (separate from chat/embedding/image) speaks the rerank response shape natively. | Wrong-shape responses if served via chat endpoint. | Mandatory Roadmap | Rerank as a distinct logical API surface; response shape compatibility tested against at least Cohere and Jina conventions. | AT-MODEL-002 | Codex | Open (L3 Phase 9+) |
| F-SEC-004 | New API | E-NAI-006 | Per-User × Per-Model rate limit with tiered ceilings (extends F-SEC-001 per-IP). | Tier misconfiguration starves low tier or lets high tier monopolize. | Implemented Better | Rate-limit policy is a (User-or-Group, Model, Window) tuple; operator-visible tier diff after each policy change. | AT-SEC-004 | Codex | Open (L2) |
| F-RATE-001 | Sub2API | E-S2A-DEEP-007 | Upstream-side rate-limit detection + cooldown computation: gateway parses upstream 429/529 + 401/403 + custom error codes, marks Provider Account in typed cooldown states (rate_limited, overloaded, temp_unschedulable, model_rate_limited, error, disabled), and clears state on recovery signals. Distinct from client-facing F-SEC-001/F-SEC-004 (those rate-limit the User). | Cooldown miscalculation drains operator credits (over-cooldown wastes capacity; under-cooldown burns provider quota). | Implemented Better | Multi-platform 429 reset extraction (header → body → default fallback) + Anthropic 5h/7d window selection + OpenAI Codex x-codex-* parsing + OAuth 401 force-refresh + per-(account, model) granular cooldown + transactional cascade clearing. HUAKAI adds tenant-level rate limits, jittered cooldowns, distributed coordination, configurable failover code list per Account/Pool. **A13 升级** (Phase A, P0): versioned provider error normalization rule table with provider override — deterministic error-class assignment from upstream codes. **A14 升级** (Phase D, P1): Retry-After harmonizer — harmonic jitter + cooldown spread to prevent thundering-herd storms. **A14b 升级** (Phase E, P3): misclassification feedback learning loop for error classification refinement. **A21 升级** (Phase C, P0): risk-weighted probe scheduler — multi-signal risk vector drives probe cadence; response: `X-Huakai-Account-Failover-Count`. **A22 升级** (Phase A, P0): health hysteresis FSM (normal/degraded/cooling/needs_refresh) with asymmetric thresholds + score decay; permanent_disable only on DR-009 Q1 iron-clad keywords (hard floor). | AT-RATE-001..020 | Claude | **Spec Released 2026-04-28** ([docs/specs/rate-limiting.md](specs/rate-limiting.md)) — Codex APPROVE-WITH-FIXES; 10 fixes applied. |
| F-I18N-001 | New API | E-NAI-002 | UI ships natively in 5+ languages without "English plus bolt-on translations" feel. | Drift across many locales; technical glossary varies per language. | Mandatory Roadmap | i18n as first-class concern with locked per-language glossary; Phase 7+ for ship-quality multi-language. Personal Edition L1 ships English + Simplified Chinese only. | AT-I18N-001 | Gemini | Open (L3 Phase 7+) |
| F-AUTH-004 | New API | E-NAI-007 | Operator can plug in community-platform OAuth identity sources (Discord, Telegram, LinuxDo, etc.) under the same auth-provider abstraction. | Account recovery depends on third-party platform availability. | Plugin | Identity sources implemented as plugins under F-AUTH-001 abstraction; each plugin must provide a fallback recovery path that does not require the third-party platform. | AT-AUTH-004 | Codex | Open (L2 plugin shell; L3 first plugins) |
| F-AUTH-005 | Sub2API | E-S2A-DEEP-001 | Upstream Provider Account credential management: OAuth token refresh + cache + version check + storm-prevention + leakage-safe logging + Claude Code mimicry as opt-in policy. **Distinct from F-AUTH-001..004 user-facing auth.** | Refresh storms across N expiring accounts; token leakage in error logs; mimicry without legal review. | Implemented Better | Provider-neutral refresh state machine + provider adapters (only HTTP details per provider); CAS on credential version; three-scope storm controls (account / provider-endpoint / global); OAuth error sanitizer at adapter boundary; mimicry behind opt-in flag + audit + legal review. **A06 升级** (Phase A, P0): credential lease state machine VALID/LEASED/STALE/REVOKED with version CAS + grace_max hold boundary. **A07 升级** (Phase A, P0): 3-scope refresh storm controller (account/endpoint/global) token bucket + singleflight deduplication (DR-009 hard floor). **A08a 升级** (Phase D, P1): stale-while-refresh admission guard — per request-class risk tiering for stale token use. **A08b 升级** (Phase D, P1): lease orphan sweep + heartbeat watermark for gateway-crash recovery. **A23 升级** (Phase B, P0): client identity priority detector — weighted signals + spoof penalty + TTL; emits `X-Huakai-Quota-Headroom-Usd` (DR-009 Q4). **A24 升级** (Phase D, P1): identity cache drift detector — detects stale priority assignments and revalidates. | AT-AUTH-005-001..017 | Claude | **Spec Released 2026-04-28** ([docs/specs/upstream-credential-management.md](specs/upstream-credential-management.md)) — Codex REJECT initial (mis-labeled F-AUTH-001) → fixes applied + ID corrected to F-AUTH-005. |
| F-OPS-001 | New API | E-NAI-008 | External tooling can query per-key remaining quota / usage in real time via a stable operator-facing API. | External tool dependency creates UX gap if abandoned. | Implemented | Stable operator-facing introspection API for quota / usage / key state; first-party UI covers the same data so external tools are convenience, not necessity. | AT-OPS-001 | Codex | Open (L2) |
| F-POOL-001 | Sub2API | E-S2A-001 | Operator pools multiple upstream subscription accounts into one logical capacity; end users see only platform API Keys, never individual upstream accounts. | Upstream ToS may forbid quota pooling; operator carries legal risk. | Implemented | Multi-account quota pooling as a first-class capability AND product identity (per [01_PROJECT_BRIEF.md](01_PROJECT_BRIEF.md)); pooling group is a configurable entity with operator-visible health and balance per pooled Provider Account. **A01 升级** (Phase A, P0): binding-aware scheduler — api_key→bindings→expand→filter→score pipeline with wait_or_fail(plan, ttl=min_recovery_eta) and candidate dedup via OrderedSet; emits `Retry-After` on fail path. **A02 升级** (Phase B, P0): Pareto band + Welford standardization — Welford+EWMA normalizes raw signals, Pareto band filters dominated candidates, risk_energy weighted selection from non-dominated set. **A19 升级** (Phase C, P0): cross-vendor min-residual graph with fault_domain abstraction — min-cut/min-residual to maintain maximum combined capacity across vendors (DR-009 hard floor). **A20 升级** (Phase D, P1): fault-domain spillover guard with exponential decay counter as residual input. | AT-POOL-001..019 | Claude | **Spec Released 2026-04-28** ([docs/specs/pool-routing.md](specs/pool-routing.md)) — first feature to cross the DR-008 Released gate; Codex APPROVE-WITH-FIXES verified. Awaits Phase 2 schema/OpenAPI lock. |
| F-SESSION-001 | Sub2API | E-S2A-002 | Same-session multi-turn requests pin to the same upstream Provider Account so conversation context is preserved. | Sticky routing creates uneven account load. | Implemented | Session-pin policy as opt-in routing rule; per-Account distribution view exposes hot-spotting. **A04 升级** (Phase C, P0): sticky migration loss function — context_loss/cache_loss/load/cred/cooldown_near weighted decision + migration manifest; emits `X-Huakai-Migration-Reason`, `X-Huakai-Migration-Cache-Lost`, `X-Huakai-Migration-Cost-Delta-Usd`, `X-Huakai-Migration-Action` response headers. **A05a 升级** (Phase D, P1): HRW Rendezvous Hashing for sticky account selection — sticky hit rate 60%→99% on account pool resize (DR-009 Q7). **A05b 升级** (Phase D, P1): hotspot rebalance — Gini coefficient-based redistribution when load skew exceeds threshold. Stream mid-session account swap gated by `Idempotent-Stream-Replay: true` opt-in (DR-009 Q8). | AT-SESSION-001 | Codex | Open (L2) |
| F-PAY-001 | Sub2API | E-S2A-003 | Operator enables one or more payment surfaces (Alipay / WeChat Pay / Stripe / aggregators) for end-user self-service top-up. | PCI compliance + fraud risk per enabled provider. | Plugin | Payment as plugin layer with operator-selected providers. **Per [DR-002 §Owner Refinement](decisions/DR-002-product-editions.md) 2026-04-28: payment is OPTIONAL but AVAILABLE in Personal Edition** so Owner can run Model 1 commercially. SaaS Edition adds multi-tenant payment orchestration (Owner takes a cut of each tenant's revenue) on top. | AT-PAY-001 | Claude | **Open (L2 Personal — payment plugin shell + 1 provider; L3 Personal — additional providers; L4 SaaS — multi-tenant orchestration)** — promoted from L4 Phase 10+ on 2026-04-28 per Owner business-model clarification |
| F-CONC-001 | Sub2API | E-S2A-004 | Two independent concurrency caps: per-User and per-Provider-Account. | Misconfigured stacking blocks legitimate spikes or under-protects upstream. | Implemented | Concurrency policy is a (User-or-Group, Provider-Account-or-Pool) tuple with explicit operator-visible diff after each change. | AT-CONC-001 | Codex | Open (L2) |
| F-MODE-001 | Sub2API | E-S2A-005 | Single configuration flag toggles between full SaaS-feature mode and a stripped-down internal-team mode. | Stripped mode invites unmetered consumption. | Implemented | Edition flag plumbing that turns Phase 10+ SaaS-only features off in Personal Edition deployments (concrete realization of DR-002). Even in Personal mode, cost-ceiling alert is mandatory. | AT-MODE-001 | Codex | Open (L1 MVP) |
| F-CB-001 | Sub2API | E-S2A-006 | Billing-system unavailability triggers a fail-closed circuit breaker; requests are denied rather than served untracked. | Closed circuit denies all users during outage. | Implemented Better | Circuit breaker with operator-visible state, explicit override role, and per-tenant scope. Fail-closed is default but configurable per User Group. | AT-CB-001 | Codex | Open (L2) |
| F-OPS-002 | Sub2API | E-S2A-007 | Operator triggers self-upgrade from inside the admin UI with rollback support, no terminal. | Update path is an attack surface. | Mandatory Roadmap | In-dashboard upgrade gated by operator role + audit + signed artifact (ties to F-SEC-003 image signing). Phase 8 production hardening. | AT-OPS-002 | Codex | Open (L3 Phase 8) |
| F-SEC-005 | Sub2API | E-S2A-008 | Operator-configurable allow/block list controls which upstream response headers are forwarded to clients; defaults block sensitive ones. | Permissive allowlists leak upstream auth / internals. | Implemented | Header-filter policy with secure defaults and operator-visible "currently exposing" diagnostic. | AT-SEC-005 | Codex | Open (L1 MVP defaults; L2 operator config) |
| F-OPS-003 | All API Hub | E-AAH-001, E-AAH-003 | Operator with multiple relay-station accounts has one dashboard showing balance / usage / health / price comparison across all sources. | Aggregating credentials creates high-value target. | Mandatory Roadmap | Operator multi-source dashboard as a separate surface (potentially a separate tool or admin UI plug-in). Phase 7+. | AT-OPS-003 | Gemini | Open (L3 Phase 7+) |
| F-OPS-004 | All API Hub | E-AAH-002 | Daily-bonus check-ins on supported sources are automated and audited per operator schedule. | Automation can look like abuse to upstream. | Plugin | Auto-check-in as plugin per source; operator opts in per source; rate-paced and audited. | AT-OPS-004 | Codex | Open (L3 Plugin shell at Phase 7+) |
| F-EXPORT-001 | All API Hub | E-AAH-004, E-AAH-005 | Operator exports a key directly into supported downstream tools and probes its health before promotion to production. | Export bypasses pause-points; probe bursts look like attacks. | Plugin | Tool-export plugin layer with explicit confirmation per export; key health-probe surface rate-limited and operator-only. | AT-EXPORT-001 | Gemini | Open (L3 Phase 7+) |
| F-SYNC-001 | All API Hub | E-AAH-006 | Cross-device operator config sync via WebDAV (optional encryption, selective rules). | WebDAV credential is long-lived token. | Plugin | Config sync as plugin with WebDAV (and S3, etc) backends; encryption mandatory; revocation-on-device-loss workflow. | AT-SYNC-001 | Codex | Open (L4 SaaS) |
| F-ROUTE-001 | Helicone | E-HLC-001 | Routing decision uses real-time observed latency / cost / reliability per provider, not only operator-set weights. | Cost-optimized routing drifts to undertested providers. | Implemented Better | Performance-aware routing extends F-GW-004 with observed-signal inputs; every chosen Provider Account carries a "routing reason" field on the Usage Record. | AT-ROUTE-001 | Codex | Open (L2) |
| F-CACHE-002 | Helicone | E-HLC-002 | Response cache backend pluggable: in-process / Redis / S3. | Multi-tenant isolation depends on key scheme. | Implemented | Cache backend abstraction with tenant_id mandatorily in key. Extends F-CACHE-001. | AT-CACHE-002 | Codex | Open (L2) |
| F-OBS-002 | Helicone | E-HLC-003 | Logs / metrics / traces export via OpenTelemetry, not a proprietary format. | OTel exporter choice affects ops flexibility. | Implemented Better | OpenTelemetry as the formal observability contract; exporter (Prometheus, Tempo, etc.) is operator-configurable. Extends F-OBS-001. | AT-OBS-002 | Codex | Open (L2) |
| F-SEC-006 | Helicone | E-HLC-004 | Rate / cost limits expressed at global / team / per-user scope, both as request-count and as dollar-amount. | Cost limits depend on accurate pricing context. | Implemented Better | Multi-scope limits with both count and money axes; cost-amount limits enforced against versioned pricing context (F-BILL-001). Extends F-SEC-004. | AT-SEC-006 | Codex | Open (L2) |
| F-CONFIG-001 | Helicone | E-HLC-006 | Per-endpoint routing strategies defined declaratively in YAML or via UI wizard. | YAML drift across deployments. | Implemented | Config-as-code routing with operator-audited reload. UI wizard generates the same artifact. **A03 升级** (Phase D, P1): explicit strategy enum (compat_priority_lru / round_robin / fill_first / risk_pareto) with operator visibility; active strategy emitted as `X-Huakai-Routing-Strategy` response header. | AT-CONFIG-001 | Codex | Open (L2) |
| F-DEPLOY-001 | Helicone | E-HLC-007 | One artifact deploys multiple ways: managed cloud, self-hosted Docker, or local npm/binary install. | Feature gaps between deployment targets cause drift. | Mandatory Roadmap | Single deployable artifact with feature-flag matrix per target. Phase 8+. | AT-DEPLOY-001 | Codex | Open (L3 Phase 8) |
| F-ARCH-001 | Envoy AI Gateway | E-EAG-001 | Operator can split the gateway into an outer-facing tier (auth + global limits) and an inner tier (model cluster + endpoint selection); single-tier deployment also supported. | Two tiers double operational complexity. | Mandatory Roadmap | Optional two-tier deployment topology; Personal Edition stays single-tier. SaaS Edition Phase 10+ unlocks two-tier for enterprise customers. | AT-ARCH-001 | Codex | Open (L4 SaaS) |
| F-ROUTE-002 | Envoy AI Gateway | E-EAG-002 | Within a Channel/Pool, an endpoint-picker policy selects among replica instances of the same model. | Misconfigured picker routes to suboptimal endpoint silently. | Implemented | Endpoint picker as a policy attached to Channel/Pool with operator-visible "why this endpoint" reason on Usage Record. | AT-ROUTE-002 | Codex | Open (L2) |
| F-DEPLOY-002 | Envoy AI Gateway | E-EAG-003 | Kubernetes-native CRD/operator-driven deployment is supported as a primary surface. | K8s requirement excludes simpler self-hosters. | Mandatory Roadmap | K8s deployment as additional surface alongside Docker / binary; Personal Edition must remain non-K8s-runnable. Phase 8+ for K8s parity. | AT-DEPLOY-002 | Codex | Open (L3 Phase 8) |

## Algorithm Upgrade Features (DR-009 Propagation, 2026-05-02)

New F-* rows introduced by the 30 A-ID algorithm upgrade synthesis. Phase labels follow DR-009 §Phase ordering (A/B/C/D/E).

| Feature ID | Reference | Evidence ID | User Outcome | Risk | Disposition | Local Capability | Test ID | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-MODEL-SUBSTITUTION-001 | DR-009 A29 | DR-009-§6.5 | When a requested model is unavailable or quota-exhausted, gateway substitutes a schema-compatible alternative model (same family) with explicit operator opt-in and client response-header disclosure. | Silent model swap degrades output quality without client awareness; double-billing risk on substitution retry. | Feature Flag | Model substitution engine: route_policy.allow_substitution gate + model_substitution_table(from, priority_list) + Substitution record emitted on Usage Record. Client receives `X-Huakai-Model-Substituted: <from>→<to>` + `X-Huakai-Substitution-Reason: quota_exhausted\|model_unavailable\|operator_route_policy`. Default disabled; operator enables per route policy. **A29 升级** (Phase D, P1): implements DR-009 Q3 explicit opt-in decision. | AT-MODEL-SUB-001 | Claude | Open (L2 Phase D) |

## MISSING_DISPOSITION 修补 — Codex Feature Parity Audit (2026-05-09)

来源：[docs/research/2026-05-09-codex-feature-parity-audit.md](research/2026-05-09-codex-feature-parity-audit.md) §6 — 8 条 HIGH `MISSING_DISPOSITION`，按 Feature Preservation Rule 不允许 silent drop，逐条分配 disposition row。三条 contentious 项标记 `DECISION-POINT-Q1/Q2/Q3` 等待 Owner 复核。

> **ID 命名说明**：Codex audit 草稿使用 `F-OBS-002 / F-OBS-003 / F-OBS-004` 命名 4-state 计费 / 14-handler 链 / DLQ-priority 三行；当前矩阵 `F-OBS-002` 已被 Helicone OTel 行占用，故对该批新行整体后移到 `F-OBS-003 / F-OBS-004 / F-OBS-005` 以避免 ID 撞库。

| Feature ID | Reference | Evidence ID | User Outcome | Risk | Disposition | Local Capability | Test ID | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-COMPAT-001 | sub2api | E-S2A-WARMUP-001 | 客户端 SDK 探针式 warm-up 请求会被网关识别并立即返合法的 mock 响应，不再回上游消耗凭证额度；此为被动拦截而非主动预热。 | 默认开启会让真实首条请求被误判成 warm-up；语义/字段名漂移导致探针误识别。 | Plugin | Warm-up interception 凭据 flag：以 plugin 形态 opt-in 注入到 inbound pipeline，按用户路由策略匹配空载/空提示请求并返回 schema 合法的合成体；带 audit 行 + 合成响应抽样回放钩子。**DECISION-POINT-Q1**：Owner 可改为 reject 不做（仅留兼容性话术），由 Owner 在 Phase 5+ Personal Edition 启动前定。 | AT-COMPAT-001 | Claude | Open (L3 Phase 5+ Personal Edition; default off) — DECISION-POINT-Q1 |
| F-AUTH-006 | sub2api | E-S2A-OAUTH-BOOTSTRAP-001 | 操作员可通过登录引导流程把全新的上游订阅账号转化为可被网关复用的 OAuth 凭据，包括首次授权码兑换、刷新窗口管理（含 5 小时短窗与 7 天长窗）、客户端身份伪装策略，构成 sub2api 套利核心商业链路。 | 引导流程涉及上游 ToS 灰区 + 第一次拿 token 的法律/合规风险；客户端伪装语义需 Owner / 法务确认。 | Mandatory Roadmap | OAuth 引导子系统：登录 bootstrap 端点 + 短窗/长窗状态机 + 客户端身份伪装策略表（受 opt-in flag + audit 控制）；与已有 F-AUTH-005（已在册的 refresh / cache / mimicry 安全等价）并列，分担"首次取 token vs 已有 token 续期"。Phase 6 commercial blocker（已在 [02_HUAKAI_FUSION_ARCHITECTURE.md L0-1](02_HUAKAI_FUSION_ARCHITECTURE.md) 列出）。 | AT-AUTH-006 | Claude | Open (L0 commercial blocker, Phase 6) — Mandatory Roadmap |
| F-COMM-001 | new-api | E-NAI-INVITE-001 | 操作员可发放邀请码 / 推荐码并配置返佣或赠额；被邀请用户绑定后产生关联记录，便于运营拉新与商业化裂变。 | 邀请链可被刷量；返佣回路与计费/退款 / 反作弊耦合，错配会泄漏额度。 | Plugin | 邀请 / 推荐子系统：以 plugin 形态接入 Phase 6+ 商业化层，独立于 F-PAY-001 但共享同一审计与额度账本；邀请码/绑定关系/返佣事件以独立表存储，写入 Usage Record 关联键。**DECISION-POINT-Q2**：Owner 可改判为 Mandatory Roadmap（与 F-PAY-001 并列）若邀请被视为商业化必备。 | AT-COMM-001 | Claude | Open (L3 Phase 6+ commercial; plugin shell) — DECISION-POINT-Q2 |
| F-OBS-003 | new-api | E-NAI-FAILSTREAM-001 | 流式请求失败时，网关按四种语义状态分别记账：客户端断开、上游超时、零输出 token、上游 5xx，运营可在用量记录与对账面板看到对应原因；与 F-OBS-001 / F-BILL-001 共享 Tx2 结算路径。 | 状态分类不齐导致退款/对账漏配；四态字段污染现有 schema 需向后兼容迁移。 | Mandatory Roadmap | 4-state 失败流计费扩展：在 F-OBS-001 Tx2 结算钩子内追加 `terminal_class ∈ {client_gone, upstream_timeout, output_token_zero, upstream_5xx}` 字段 + 各自的退款 / 部分计费策略；列入 Phase 4.5 F-OBS-001 扩展，不另开新 spec。 | AT-OBS-003 | Codex | Open (L2 Phase 4.5) — Mandatory Roadmap |
| F-OBS-004 | Helicone | E-HLC-ASYNC-001 | 用量、请求体、审计、告警等异步副作用走一条 14 段处理器链；每批消费拥有独立的耗尽（drain）窗口，确保单批失败不污染同一队列其他段。 | 任意一段实现失序 / 重入会破坏 ledger 不变量；批级 drain 不当导致背压塌陷。 | Mandatory Roadmap | 14 段异步处理器链：在 Phase 4.5 axis 5 异步任务扩展中按角色（Spec 命名而非上游 identifier）落档 14 个 handler 槽位 + 每槽幂等键 + 每批 drain 边界；与 F-OBS-001 outbox / F-OBS-005 DLQ 协同，共用一个 consumer runtime。 | AT-OBS-004 | Codex | Open (L2 Phase 4.5) — Mandatory Roadmap |
| F-OBS-005 | Helicone | E-HLC-DLQ-001 | 异步消费失败进 DLQ；超时（默认 15 分钟）的消息被标记可重投或低优先级；高低优先级双通道并存；主队列 / 备份队列采用非对称双写以容灾。 | DLQ 重投乱序导致 Tx2 重放；优先级翻转造成关键账单事件被低优先级 starve；双写分歧造成账本不可对账。 | Mandatory Roadmap | DLQ + 优先级 + 双写 异步控制面：在 Phase 4.5 axis 5 中扩 F-OBS-004 chain 为 (主队列, 备份队列) 非对称双写，加 15 min 超时降级 + 显式低优先级 lane + 重投幂等闸；与 F-OBS-001 outbox 共享 idempotency key。 | AT-OBS-005 | Codex | Open (L2 Phase 4.5) — Mandatory Roadmap |
| F-CRED-001 | Envoy AI Gateway | E-EAG-CRED-001 | 企业租户可声明上游凭据来源（cloud STS / 外部 IdP）+ 预轮换窗口（在凭据到期前 N 分钟主动取新串），网关运行时无人工干预即完成滚动；客户端不可见短暂双串期。 | OIDC→cloud STS 链多一跳故障；预轮换窗口设短会与 F-AUTH-005 风暴控制器抢锁；CRD 蓝图与 HUAKAI 非 K8s 形态有摩擦。 | Mandatory Roadmap | 凭据提供者 + 预轮换 + OIDC→cloud STS 子系统：作为 SaaS Edition 企业层独立 spec；以非 K8s 表驱动声明（行=凭据来源 + 来源类型 + 预轮换窗口 + STS 信任链），与 F-AUTH-005 续期、F-AUTH-006 引导分别承担"已有 token / 首次 token / 凭据来源轮换"。Phase 9+ enterprise tier。 | AT-CRED-001 | Claude | Open (L4 Phase 9+ SaaS enterprise) — Mandatory Roadmap |
| F-PROTO-003 | OpenAI | E-OAI-COMPACT-001 | 客户端可经由 native passthrough 调用上游服务侧上下文压缩接口（OpenAI `/responses/compact` 类语义），网关不解析正文、不重写 schema、不补 14 capability 缺项。 | passthrough 默认对客户端展示"全上游风险"必须显式标注；若 Owner 后续要把压缩升为 first-class capability，需要扩 14→15 capability。 | Safe Equivalent | 服务侧压缩透传：复用既有 P-4 native passthrough 路径 `/v1/native/openai/responses/compact`，HCSF 不增加 capability slot；admin UI 标记"全上游风险"。**DECISION-POINT-Q3**：Owner 可决定升级到 first-class HCSF capability（14→15），届时再补 P-1 schema row 与 spec。 | AT-PROTO-003 | Claude | Open (L2 covered by P-4 native passthrough) — DECISION-POINT-Q3 |

## Matrix Template

| Feature ID | Reference | Evidence ID | User Outcome | Risk | Disposition | Local Capability | Test ID | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TBD | TBD | TBD | TBD | TBD | Mandatory Roadmap | TBD | TBD | TBD | Open |

## Review Rules

- Every reference feature must appear here.
- Similar features may be merged only when the merged capability fully covers every user outcome.
- A safer equivalent must document the behavior preserved and the risk reduced.
- Mandatory roadmap items block parity claims until implemented, pluginized, or feature-flagged.

## Algorithm Upgrade A-IDs (2026-05-02)

Cross-reference table: 30 algorithm upgrade IDs from synthesis ([docs/plans/2026-05-02-huakai-algo-upgrade-synthesis.md](plans/2026-05-02-huakai-algo-upgrade-synthesis.md)) mapped to F-* rows. DR-009 is the authority source. Phase order: A→B→C→D→E.

| A-ID | Topic | Priority | Phase | F-* Row(s) |
|---|---|---|---|---|
| A01 | Binding-aware scheduler | P0 | A | F-POOL-001 |
| A02 | Pareto band + Welford standardization | P0 | B | F-POOL-001 |
| A03 | Strategy enum (compat_priority_lru / round_robin / fill_first / risk_pareto) | P1 | D | F-CONFIG-001 |
| A04 | Sticky migration loss function + manifest headers | P0 | C | F-SESSION-001 |
| A05a | Sticky HRW Rendezvous Hashing | P1 | D | F-SESSION-001 |
| A05b | Sticky hotspot rebalance (Gini coefficient) | P1 | D | F-SESSION-001 |
| A06 | Credential lease state machine (VALID/LEASED/STALE/REVOKED + version CAS) | P0 | A | F-AUTH-005 |
| A07 | 3-scope refresh storm controller (account/endpoint/global token bucket + singleflight) | P0 | A | F-AUTH-005 |
| A08a | Stale-while-refresh admission guard (per request-class risk) | P1 | D | F-AUTH-005 |
| A08b | Lease orphan sweep + heartbeat watermark | P1 | D | F-AUTH-005 |
| A09 | 2-phase quota reserve+settle (Tx1 reserve + Tx2 settle + attempt attribution) | P0 | B | F-OBS-001 |
| A10 | Quantile reserve estimator (tiered P99/P95/P90 by user tier) | P1 | D | F-OBS-001 |
| A11 | Bounded multi-attempt DAG planner (edge types + min expected-time path) | P0 | B | F-GW-004 |
| A12a | Stream-safe retry boundary FSM (BEFORE_FIRST_TOKEN / CONTENT_STARTED / TOOL_SIDE_EFFECT_STARTED) | P0 | B | F-GW-004 |
| A12b | Retry decision matrix (error_class × attempt_n × budget → action) | P1 | D | F-GW-004 |
| A13 | Provider error normalization rule table (versioned + provider override) | P0 | A | F-RATE-001 |
| A14 | Retry-After harmonizer (harmonic jitter + cooldown spread) | P1 | D | F-RATE-001 |
| A14b | Misclassification learning loop | P3 | E | F-RATE-001 |
| A15 | Versioned pricing vector (dot-product over token fields × Merkle-chained snapshot) | P0 | A | F-BILL-001 |
| A16a | Pricing drift reconciliation (post-hoc authoritative usage correction) | P1 | D | F-BILL-001 |
| A16b | Pricing expression AST compile (50-200µs → 1µs) | P2 | E | F-BILL-001 |
| A17 | Capacity depletion forecast (EWMA + seasonal P95 → ETA) | P0 | C | F-GW-003 |
| A18 | Bounded knapsack restock recommendation | P1 | D | F-GW-003 |
| A19 | Cross-vendor min-residual graph (fault_domain + min-cut) | P0 | C | F-POOL-001 |
| A20 | Fault-domain spillover guard (exponential decay counter) | P1 | D | F-POOL-001 |
| A21 | Risk-weighted probe scheduler (multi-signal risk vector) | P0 | C | F-RATE-001 |
| A22 | Health hysteresis FSM (normal/degraded/cooling/needs_refresh + score decay) | P0 | A | F-RATE-001 |
| A23 | Client identity priority detector (weighted signals + spoof penalty + TTL) | P0 | B | F-AUTH-005 |
| A24 | Identity cache drift detector | P1 | D | F-AUTH-005 |
| A25 | Adaptive stream buffer (historical P99×4 + memory-pressure clipping) | P0 | C | F-GW-002 |
| A26 | Expected-value drain decision (E[value]>E[cost] default-greedy + three-budget early-stop) | P0 | C | F-GW-002 |
| A27 | Stream-time quota dynamic reserve extension | P2 | E | F-OBS-001 |
| A29 | Model substitution engine (opt-in + client header disclosure) | P1 | D | F-MODEL-SUBSTITUTION-001 |

**Count verification**: P0=14 (A01,A02,A04,A06,A07,A09,A11,A12a,A13,A15,A17,A19,A21,A22,A23,A25,A26) — **17 items** (synthesis §4 lists 17 P0 items including A23/A25/A26 which were in §1 agree list); P1=11 (A03,A05a,A05b,A08a,A08b,A10,A12b,A14,A16a,A18,A20,A24,A29) — **13 items** (synthesis §4 P1=11 + A29 newly added + A24); P2=2 (A16b,A27); P3=1 (A14b). Total unique A-IDs: 30 (A01-A27 + A29, noting A28 merged into A20).

**Note**: A28 (cross-vendor fault-domain awareness with exponential decay counter) was merged into A20 per synthesis §3. The 30-item count is A01–A27 + A29 = 28 distinct IDs + A05a/A05b split + A08a/A08b split + A12a/A12b split + A14/A14b split + A16a/A16b split = 30 line items total as listed in synthesis §4.
