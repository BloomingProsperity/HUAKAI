# 2026-05-20 renew P2 fixes

| Owner directive | "HUAKAI /renew 方案 B — 修复 codex review 第 2 轮的 2 个 P2。不执行任何 git 操作。" |
| Scope | In: renew status keyset pagination by `(updated_at, id)`, supporting index migration `0048`, stable credential id in renew status API response, frontend row key, OpenAPI/tests alignment. Out: billing, auth core, quota/ledger hot paths, audit whitelist, reference-source reading, new dependencies, git operations. |
| Success criteria | Renew status query orders by `updated_at DESC, id DESC`; cursor encodes both fields opaquely; API item exposes stable credential id; frontend table row key uses that id; migration adds and drops the approved partial index; backend build/tests and frontend type-check/build pass with 0 failures/errors. |
| Time estimate | 45-75 minutes wall clock; one Codex work unit. |
| Blast radius | Admin renew status listing, its API contract, related credential store tests, gateway handler tests, frontend renew page rendering, and one additive PostgreSQL index migration. |
| Failure modes | Cursor compatibility could regress existing opaque cursors; mitigate with focused handler tests. SQL placeholder ordering could break pagination; mitigate with store tests. API type mismatch could break frontend build; mitigate with `npm run type-check` and `npm run build`. Migration ordering/name could be wrong; mitigate by listing migrations first and using next sequential number only. |
| Decision points | Owner already approved the additive `CREATE INDEX` migration. No further Owner sign-off is expected unless the implementation requires high-risk areas explicitly marked out of scope. |
| Pre-execution checklist | Confirm migration max number; inspect existing renew store/handler/frontend/OpenAPI/tests; apply narrow patches; run requested backend and frontend checks; report real results. |
| Concrete execution order | 1. Read relevant store, handler, tests, frontend type/page, and OpenAPI sections. 2. Restore `(updated_at, id)` store params/query/cursor behavior. 3. Add response item id through backend types/handler/OpenAPI/frontend types. 4. Add `0048` up/down migration with Chinese comments. 5. Update tests. 6. Run requested verification commands without git. |
