# 2026-05-15 L2-A2 TLS Parser Negative Tests
| Owner directive | "补 L2-A2 TLS parser negative tests (per-commit review MED)" |
| Scope | In: test-only negative cases for `core_gateway/tests/common/tls_capture.rs` behavior, preferably in `core_gateway/tests/mimicry_capture_test.rs`; this plan. Out: parser implementation, production code, dependencies, schema, auth, quota, billing, deployment, secrets, `LICENSE`. |
| Success criteria | Seven malformed TLS byte streams each return the expected `CaptureError` variant through `spawn_capture_once`; each spawned capture task is awaited with timeout and `.await.unwrap()` so task panic is detected. Full `cargo test` passes from `exploratory/rust-core-gateway/merged`. |
| Time estimate | 30-45 minutes wall clock; one test implementation pass plus compile/test fixes. |
| Blast radius | Test-only Rust code and this docs plan. Failure can break local test compilation or expose parser behavior mismatch; production behavior is unaffected. |
| Failure modes | Hand-built TLS bytes may target the wrong parser branch; mitigate by reading `tls_capture.rs` variants and constructing the shortest record that reaches each branch. Async capture can hang if client write/close ordering is wrong; mitigate with per-case `tokio::time::timeout`. |
| Decision points | Stop before modifying parser logic, adding dependencies, or touching any high-risk files. No Owner sign-off is expected for test-only additions. |
| Pre-execution checklist | Read `CaptureError` variants and parser cursor paths; confirm existing async capture test structure; add shared raw-TCP negative helper; add seven independent test functions; run full cargo test; run whitespace checks. |
| Concrete execution order | 1. Build minimal ClientHello body/record helpers inside the test file. 2. Add one async assertion helper that starts `spawn_capture_once`, writes malformed bytes, shuts down the client, times out the join, and returns the parser error. 3. Add seven tests matching the requested malformed inputs to real `CaptureError` variants. 4. Run `cargo test 2>&1 | tail -25`. 5. Run whitespace checks for unstaged and staged diffs. |
