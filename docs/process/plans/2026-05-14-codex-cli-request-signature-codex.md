# 2026-05-14 Codex CLI Request Signature Source Analysis - Codex Plan

| Owner directive | "HUAKAI R-3 — 分析 openai/codex 源码: codex CLI 怎么构造 HTTP 请求 / header / 鉴权, 给 HUAKAI transport mimicry 当蓝本。" |
| --- | --- |
| Scope | In: clone or update `openai/codex`, verify commit SHA and license, read only the source regions needed for HTTP client construction, headers, authentication, endpoints, and TLS stack, then write one Chinese research doc under `docs/research/`. Out: HUAKAI implementation code, auth core, billing, quota, DB schema, deployment, secrets, real token use, packet capture. |
| Success criteria | `docs/research/2026-05-14-codex-cli-request-signature-codex.md` exists with source-cited conclusions for HTTP version/settings, header names/order/value shape, User-Agent format and version, ChatGPT auth token/refresh flow, endpoint paths, TLS stack, license, commit SHA, and HUAKAI implementation recommendations. `/tmp/codex-src-analysis-final.txt` records the requested final facts. |
| Time estimate | 60-120 minutes wall clock; one source-mining pass plus citation verification and doc write. |
| Blast radius | Documentation only. Main risk is inaccurate source interpretation or clean-room leakage into a planning artifact, not runtime breakage. |
| Failure modes | Stale clone: update before reading. Missing explicit h2 settings: state "not found in observed regions" instead of inventing. Header order hidden behind library internals: cite the construction order and mark wire order as library-controlled when source does not guarantee it. License ambiguity: stop detailed summary until license is identified. Clean-room leakage: paraphrase behavior, avoid source code blocks, and keep citations as evidence anchors. |
| Decision points | No Owner mid-flight confirmation expected because the task explicitly says not to ask. If source shows restricted/non-permissive licensing or private endpoints requiring secrets, record risk and keep to behavior-level summary. |
| Pre-execution checklist | 1. Create `/tmp/codex-src-analysis.txt`. 2. Clone or update `~/refs/openai-codex`. 3. Capture HEAD SHA and license. 4. Locate Rust workspace/package version. 5. Locate HTTP client creation. 6. Locate request construction and headers. 7. Locate ChatGPT auth storage and refresh. 8. Locate endpoint paths and TLS feature selection. 9. Write cited Chinese research doc. 10. Write `/tmp/codex-src-analysis-final.txt`. |
| Concrete execution order | License/commit first, then targeted `rg` over HTTP/auth/header symbols, then line-number reads, then synthesize doc with explicit "observed / not observed" status. |

