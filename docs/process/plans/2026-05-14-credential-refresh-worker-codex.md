# 2026-05-14 credential refresh worker
| Owner directive | "HUAKAI 反代主链路 Q3 — credential refresh 后台 worker / scheduler。" |
| Scope | In: new `internal/credentialworker` package, one refresh candidate query, sqlc-equivalent DB method, gateway startup/shutdown wiring, focused unit tests, `/tmp` progress/final markers. Out: `internal/auth` internals, provider-specific refresh adapters, DB schema, quota/billing/auth core, external dependencies. |
| Success criteria | Scheduler starts a background loop, finds expiring provider accounts, gates refresh through storm controller, retries failed refresh up to 3 attempts with backoff, writes signed audit ledger entries, stops gracefully, and `go test ./internal/credentialworker/...` plus `go vet ./internal/credentialworker/...` pass. |
| Time estimate | 60-90 minutes wall clock; one Codex implementation pass plus test/debug pass. |
| Blast radius | Medium: new worker may be wired into gateway lifecycle. Query and startup changes could break compile if signatures do not match existing DB/auth/audit APIs. |
| Failure modes | Storm controller API mismatch: adapt via public method only. Audit signer/ledger mismatch: use existing package contracts and keep a small package-local interface for tests. Main wiring config gap: default safe values and no-op placeholder refresher to avoid runtime destructive behavior. Tests timing flakes: inject ticker/backoff via options and use explicit synchronization. |
| Decision points | Provider-specific adapters are explicitly placeholder only. Any schema migration, auth core change, or new dependency remains out of scope and would require Owner confirmation, but this task will avoid them. |
| Pre-execution checklist | Read existing `internal/auth` public APIs, audit ledger writer/signing contracts, provider account generated DB methods, query style, and gateway lifecycle. Implement package with injectable clock/ticker/backoff for tests. Add query and sqlc-equivalent method. Wire scheduler using defaults. Run gofmt, targeted tests, targeted vet. Write final `/tmp` evidence file. |
| Concrete execution order | 1. Inspect existing APIs and table columns. 2. Add `refresher.go`. 3. Add `scheduler.go`. 4. Add SQL query and DB method/interface line. 5. Add tests. 6. Wire `cmd/gateway/main.go`. 7. Run formatting, tests, vet. 8. Write `/tmp/codex-q3-renew-worker-final.txt` and Chinese summary. |
