# 2026-06-07 captcha-multiprovider Codex execution plan
| Owner directive | "模块A闭环 — 多 captcha provider(AUTH-116:reCAPTCHA / hCaptcha 接入,现仅 Turnstile)。Branch fix/a-captcha. HUAKAI-internal,clean-room(禁读参考源码)。全加性,默认行为不变..." |
| Scope | In: `backend/internal/captcha` verifier/tests; provider allow-list support in `backend/internal/platformsettings` if needed for runtime provider switching. Out: reference source, `internal/gatewayhttp` wiring, schema/migrations, stored secrets, commits. |
| Success criteria | Blank secret remains noop; Turnstile path remains compatible; `captcha_provider` can select `turnstile`, `recaptcha`, or `hcaptcha`; provider tests prove provider-specific POST endpoints and fail-closed behavior; requested build/vet/test commands pass. |
| Time estimate | 45-75 minutes wall clock; one Codex implementation pass plus verification. |
| Blast radius | Auth CAPTCHA gating only. Wrong provider routing could silently hit the wrong verifier endpoint; wrong fail policy could allow bots or block real users. Platform setting validation change affects only accepted `captcha_provider` values. |
| Failure modes | Accidentally changing noop behavior; breaking Turnstile defaults; allowing unsupported providers to fail open; test transport hiding wrong URLs; widening settings too far. Mitigation: RED tests for routing/fail-closed, existing Turnstile tests, narrow allow-list. |
| Decision points | None expected. Stop for Owner only if implementation requires changing `internal/gatewayhttp`, schema, auth core, quota/billing, or secret storage. |
| Pre-execution checklist | Read `docs/RULES.md`; read `internal/captcha/verifier.go`; confirm `verifyAuthCaptcha` only depends on `CaptchaVerifier`; confirm branch; do not read reference source; write failing tests before production code; run requested Go gates. |
| Concrete execution order | 1. Add provider routing/fail-closed tests. 2. Run captcha tests to verify RED. 3. Implement generic provider verifier and default URLs. 4. Widen provider setting validation only to `recaptcha`/`hcaptcha`. 5. Run focused tests, then requested build/vet/test. |
