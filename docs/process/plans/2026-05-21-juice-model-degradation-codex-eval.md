# 2026-05-21 juice model degradation codex eval

| Owner directive | "HUAKAI Owner 要加一个 \"juice 功能\" ... 你是 codex 独立评估 lane ... 直接修订 ... 不做任何 git 操作。" |
| Scope | In: independently evaluate the existing research report, read CLIProxyAPI/helicone source evidence in specifier lane, pin local reference citations to commit SHAs, verify Anthropic `reasoning_effort` against official API docs, and patch only the research report wording/evidence needed for those issues. Out: implementation code, schema/API changes, commits/staging, broad report restructuring. |
| Success criteria | The report no longer uses `(local)` citation anchors for referenced projects where local clones exist; CLIProxyAPI and helicone conclusions include file:line evidence or are moved to open questions; the Anthropic `reasoning_effort` statement is corrected if official docs do not support it; final Owner-facing evaluation distinguishes passive metadata monitoring from active proof-of-quality. |
| Time estimate | 45-75 minutes wall clock; single Codex lane. |
| Blast radius | Low to medium: one research report plus this plan artifact. Incorrect edits could weaken clean-room evidence quality or mislead feature planning. |
| Failure modes | Missing a citation replacement: run `rg "\\(local\\)|:\\(local\\)"` on the report after edits. Clean-room leakage: paraphrase behavior only and avoid code blocks/identifier copying from reference source. Official-doc mismatch: cite public Anthropic docs separately from reference source. Overclaiming absence: use `rg` evidence plus targeted source reads and phrase absence as "not observed in searched/read regions" if full certainty is not warranted. |
| Decision points | No high-risk repo changes planned. If a required clone is unreadable, leave that project as Open Question rather than inventing evidence. If official docs contradict the current vendor mapping, patch the report rather than preserving the prior claim. |
| Pre-execution checklist | 1. Read report sections 1-6 and appendix. 2. Discover referenced local projects and current HEAD SHAs with read-only commands. 3. Search/read CLIProxyAPI and helicone source for model-quality, scoring, monitoring, usage, and probe logic. 4. Check Anthropic official Messages API docs for native request fields. 5. Patch the report. 6. Verify citation format and relevant line references. 7. Produce concise Chinese evaluation with source list and lane metadata. |
