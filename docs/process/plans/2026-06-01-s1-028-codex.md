# 2026-06-01 S1-028 Codex Plan

| Owner directive | "Fix audit finding S1-028 to high quality and push it for review." |
| Scope | In: password-reset confirm sequencing, session revocation failure handling, discriminating Go regression tests. Out: schema changes, auth policy redesign, reference-project mining. |
| Success criteria | Reset confirm cannot complete password/token mutation when existing-session revocation fails or session dependencies are unavailable; old-password login is blocked while a valid reset token waits for retry; a regression test fails if the old post-reset ignored/soft-failed revoke behavior is restored; `go test` for gateway HTTP and `go build ./...` pass. |
| Time estimate | ~45-75 minutes wall clock; one Codex worker. |
| Blast radius | Auth reset-confirm HTTP path and userauth password-reset token lookup. Existing login/register/request-reset behavior should remain unchanged. |
| Failure modes | Token preparation and consume can diverge; mitigate by sharing hash/time validation semantics and still letting consume enforce final validity. Revocation-before-reset can allow a concurrent old-password login; mitigate by placing the token subject behind `reset_required` before revocation. Frozen package violation; mitigate by editing existing `gatewayhttp` files only. |
| Decision points | No Owner question because the dispatch says work fully autonomously; auth-path risk will be flagged in final output. Schema/database changes remain out of scope. |
| Pre-execution checklist | 1. Read `CLAUDE.md`/`AGENTS.md`. 2. Load S1-028 audit row and JSON reason. 3. Verify current code still permits reset completion after revocation failure. 4. Claim coordination lock. 5. Write failing regression test. 6. Implement minimal fix. 7. Run targeted test, build, self-review, commit, push. |

Concrete execution order:

1. Change the gateway HTTP regression so a user-scoped session revoke error must return non-2xx and leave the old password valid.
2. Add a HUAKAI-internal auth service/store preparation step for the reset-token subject before consuming the token.
3. In reset confirm, prepare the token subject with a login barrier, revoke sessions, then consume the token to change the password.
4. Keep existing session-dependency preflight and successful reset response shape.
5. Verify with mutation reasoning: restoring `ResetPassword` before `Revoke` makes the regression red because the password changes despite revoke failure.
