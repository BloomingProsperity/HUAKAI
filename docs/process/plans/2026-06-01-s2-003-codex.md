# 2026-06-01 S2-003 Codex Plan

| Field | Plan |
| --- | --- |
| Owner directive | "Fix audit finding S2-003 to PRODUCTION QUALITY." |
| Scope | In: remove the false-green AT-GW-002 skipped placeholders, add or extend executable Go tests that cover the same stream failover/replay/settlement/tenant risks at their owning layer, and update the dev-test skip note. Out: no new gateway/gatewayhttp/proto files, no schema migration, no auth/billing/quota production logic rewrite, no reference-project source reading. |
| Success criteria | `rg "t\\.Skip\\(" backend/internal/gateway/forwarder_test.go` no longer finds the eight S2-003 placeholders; targeted gateway/gatewayhttp tests fail under a deliberate skip-regression mutation and pass after restore; `go test ./internal/gateway/...`, relevant `go test ./internal/gatewayhttp/...`, and `go build ./...` pass from `backend/`. |
| Time estimate | 1 focused Codex session, roughly 1-2 wall-clock hours including mutation and build verification. |
| Blast radius | Test files in frozen packages are edited in place; docs skip inventory changes. Production runtime code should remain unchanged unless the executable tests expose a real behavior bug that cannot be covered honestly without a small safe fix. |
| Failure modes | Weak tests that only prove names exist; mitigated by assertions on retry counts, settlement/abort counts, tenant IDs, replay store isolation, and draft fields. Reintroducing cross-module coupling into `internal/gateway`; mitigated by keeping executor/handler tests in `gatewayhttp`. Long-running full test/build failures from unrelated repo state; record exact command output if observed. |
| Decision points | None needing Owner sign-off under the explicit autonomous directive unless implementation requires high-risk production changes to auth, billing ledger, quota enforcement, schema, secrets, deployment scripts, or new runtime dependencies. |
| Pre-execution checklist | 1. Read CLAUDE.md, AGENTS.md, S2-003 audit row, F-GW-002 spec, CMB invariants, and current gateway/gatewayhttp flow. 2. Confirm skips still exist at HEAD. 3. Claim files via `.coordination`. 4. Edit only existing files in frozen packages. 5. Run targeted red/green and mutation checks. 6. Run package tests and `go build ./...`. 7. Stage, run Codex review best-effort, commit, push `HEAD:work/s2-003`. |

Concrete execution order:

1. Replace the eight skipped AT-GW-002 placeholder bodies in `backend/internal/gateway/forwarder_test.go` with executable forwarder-owned boundary tests where possible, plus a source-level guard that fails if those AT IDs regain `t.Skip`.
2. Extend existing `gatewayhttp` tests for executor-owned cases using the current handler harness: pre-stream retry/sanitized error, post-delivery no-retry, replay storage/isolation, settlement recovery, and tenant propagation.
3. Update `docs/dev-tests.md` so it no longer claims these eight AT-GW-002 tests are intentionally skipped.
4. Run the targeted test set, perform the skip-regression mutation check, restore, then run required build/tests.
