# 2026-05-17 R-3-A-fix-2-deeper Codex Executor Plan
| Owner directive | "修 R-3-A-fix-2-deeper 的 4 个 codex review 阻塞 + F-AUDIT-1-A 的 receipt JOIN + UUID schema + cost 字段单位." |
| Scope | In: `boring-pq.patch`, backend audit receipt formatter/storage/tests, billing settler/query support if needed, migrations 0028/0029, spec field-name correction. Out: frontend, control plane, billing core redesign, quota/auth core, reference project source. |
| Success criteria | Patch no longer depends on HUAKAI strict-mode context; receipt derivation joins audit and billing through a real shared key; receipt request IDs align with text audit IDs while keeping external receipt UUIDs; canonical cost field uses micro-USD naming; requested Go and Rust checks are run or honestly reported. |
| Time estimate | 2-3 hours wall clock; one Codex executor pass plus verification. |
| Blast radius | Migration and sqlc/query changes can affect billing/audit package compile; BoringSSL patch context affects `mimicry-boring` builds. |
| Failure modes | Wrong join key leaves receipts undiscoverable; schema/query drift breaks sqlc-generated code; cargo check can be slow or blocked by local toolchain; existing dirty worktree may include intentional user changes. Mitigation: inspect current schema and generated query patterns first, keep edits additive, and report any pre-existing or environment failures. |
| Decision points | Adding a new billing event audit request column is allowed only if no existing durable join key exists; no new dependencies; no reference-source reads. |
| Pre-execution checklist | Read patch hunks; read billing settler/storage/query definitions; read audit ledger schema/storage; read receipt formatter/storage/tests; inspect migration 0028 and existing migration numbering; check docs field names. |
| Concrete execution order | 1. Repair patch context. 2. Determine and implement receipt join key. 3. Align receipt request ID storage with text. 4. Rename canonical micro-USD field and docs. 5. Add focused acceptance-style tests. 6. Run Go build/tests and Rust cargo checks where feasible. |
