# 2026-07-21 账号导入、账号创建与用户创建单目标闭环计划

| 项目 | 内容 |
| --- | --- |
| Owner 指令 | “单个目标来干！”“一定要挖透彻，所有相关的代码链路都要查看！” |
| 唯一目标 | 在当前分支闭环账号导入、账号创建与用户创建三条链路；不切换其他目标，不新建分支、工作树、计划或 PR。 |
| 范围 | OAuth 授权后直接创建或更新账号、通用与专用导入、直接建号、三身份授权、用户四类创建入口、事务一致性、健康初始化、订阅标签、日志、失败恢复、OpenAPI、迁移和测试。 |
| 不包含 | 前端页面、生产部署、真实凭据内容输出、与本目标无关的 relay/计费/模型协议改造。 |
| 成熟行为输入 | 成熟中转项目只提供“导入即建号、来源证明、冲突显式化、管理员可运维”的行为合同；HUAKAI 依照自身多租户、事务、日志和恢复能力独立实现，不复制代码、结构或标识符。 |
| 成功标准 | OAuth 无需预建空账号；账号、凭据、健康和日志同事务或具备明确恢复；三身份绝不越级；用户创建无半成品；必要失败可重试；OpenAPI 与路由一致；普通全量测试和真实 PostgreSQL 集成测试通过；代码预算不抬基线。 |
| 预计时间 | 实施、复验和评审合计约 2-4 小时；真实厂商 OAuth 活体验证另取决于可安全使用的测试账号和是否允许旋转 refresh token。 |
| 爆炸半径 | 错误会造成跨租户管理、来源伪造、孤儿账号/用户、凭据已生效但接口报错、账号缺健康状态、奖励漏发、OAuth 重放或真实账号绑定错误。 |
| 失败模式 | 只修 HTTP 表面而遗漏事务和恢复；把 OAuth 临时会话普遍放宽为空账号；把错误 state 消耗为终态；把代码预算基线调大；用 mock 代替所有数据库验证。 |
| 缓解方式 | 只允许显式 `account_intake` 目的使用空账号会话；回调先校验操作者与 state；授权结果短期加密暂存；执行后绑定真实账号；真 PostgreSQL 检查表状态；保留单一计划和报告。 |
| 决策点 | 当前实现无待 Owner 的代码决策。真实 Claude OAuth 刷新可能旋转测试账号 refresh token，执行活体验证前需确认该测试账号允许被消耗或重新登录。活体清单还必须保留：用 Claude 订阅 OAuth 反转号验证 `opus-4-8`、`fable-5` 与 Sonnet 的 429 差异；现有官方 `sk-ant-api03` key 不能替代该场景。 |

## 执行顺序

1. 核实账号导入、直接建号和用户创建的全部生产入口、权限、写库、日志与恢复链。
2. 收紧来源证明和三身份边界，把直接建号与正式导入统一到可靠事务合同。
3. 增加 OAuth 创建型会话、回调、加密暂存、预检、执行、终态绑定和重放拒绝。
4. 原子化社交建号与管理端建号，统一字段校验，补首装令牌、验证邮件重发和奖励恢复。
5. 更新迁移、OpenAPI、路由与判别式测试；按职责拆分超预算文件，删除本目标内的僵尸平行入口。
6. 跑专项测试、`go test ./...` 和带 `integration_pg` 的真 PostgreSQL 测试。
7. 更新唯一报告，执行未提交改动强制评审；只保留一个可审查 PR，合并仍由 Owner 决定。
8. 完成度复核发现 OAuth 执行终态原先落在账号事务之外：把账号/凭据/健康/业务日志、OAuth 会话终态和导入完成日志收敛进同一事务，并用完成日志写失败的真 PostgreSQL 变异场景证明不会留下孤儿账号或假成功。
9. 原子终态测试继续暴露 Grok OAuth 身份链缺口：生产换码器保存了 `id_token`，却没有验证并接入稳定主体，刷新材料变化时可能重复建号。先按当时授权码合同补齐 nonce、ES256/JWKS、issuer、audience、时效校验与受信 `sub` 投影；第 15 步又依据当前设备码合同替换该入口，最终实现以第 15 步为准。
10. 横向检查复合身份发现 OpenAI 工作区与 Grok 团队账号都可能被单独 `sub` 错误合并：共享账号域统一改为“账号范围 + 个人主体”复合消歧，同一人处于不同工作区/团队时分别建号，同一复合身份才允许轮换。
11. 核实套餐标签链发现统一 quota probe 已有 Grok/Codex/Antigravity 等适配器，但导入后只能等周期扫描。把账号导入事务提交后的 created/updated 事件接到单账号异步探测队列；触发失败由原周期扫描兜底，绝不把上游只读探测并入建号事务。
12. 横向复核设备码导入发现响应归一化会丢弃明确的通用套餐字段；保留该发行方证据并复用统一系统标签投影。没有上游证据的 Kimi 套餐仍保持 unknown，不伪造等级。
13. 全量门发现 `credentialacq` 主包越过 6000 行软预算；把设备授权共用 HTTP、大小限制和响应归一化下沉到 `oauthwire` 子包，不修改预算基线。
14. 未提交改动首轮复审发现 xAI 在缺少团队范围时会把个人主体退化为账号范围，同一用户跨团队可能误轮换凭据。按 xAI 官方 team 归属合同收紧为：共享账号 OAuth 必须同时得到已验签的个人主体与团队范围，缺任一项直接拒绝导入，不允许按个人主体猜测合并。
15. PR 通过后继续做完成度审计，发现 xAI 公共客户端被错误套入可变回调地址的授权码流程：现有 GET 回调要求 `flow_id`，通用 xAI 授权地址却无法可靠携带该值；固定公共客户端也不能冒充部署者自配客户端。依据 xAI 当前官方设备码合同和成熟实现的固定客户端行为，复用现有设备码加密暂存、轮询租约、失败恢复与账号导入预检，把 xAI 改为服务端设备码导入；成功换码后仍必须通过 OIDC 验签和团队范围校验。
16. 强制评审发现 access token 虽已验签并与 ID Token 主体绑定，却未校验目标 audience。官方发现文档只声明 `aud` claim、未公开取值；用 Owner 提供的既有测试令牌仅解码非身份合同字段后确认，真实 `aud` 等于固定公共客户端 ID。访问令牌验证因此再绑定该 audience，并补签名正确但 audience 错误时拒绝导入的判别测试。

## 当前状态

- 第 1-16 步均已实施；账号/凭据/健康/账号操作日志、OAuth 会话终态、暂存完成态和完成日志现为同一 PostgreSQL 事务。
- Grok OAuth 使用固定公共客户端的设备码合同；ID Token 验证 ES256 签名、issuer、client audience 与时效并确定个人主体，access token 独立验证签名、issuer、client audience、时效及同一主体后确定团队范围。OpenAI 工作区与 Grok 团队账号均按“账号范围 + 个人主体”复合身份消歧。
- 账号创建或轮换提交后会异步触发单账号额度/套餐采集；Kimi 明确套餐字段可进入系统标签，缺证据仍诚实显示 unknown。
- 第 16 步专项通过；强制评审第一轮发现并闭环 access token audience 的 `S1`，第二轮无 S0/S1 或其他可归因缺陷。最终普通全量、代码预算和质量门全部通过；完整 PostgreSQL 集成门在迁移 `1-211` 后逐包克隆纯净库并带 `-race` 跑完 80/80。第一次全跑曾因同机瞬时数据库连接竞争导致一个包在克隆前清库失败，单包复现无连接泄漏，第二次完整全跑越过同点并 80/80 全绿；未修改脚本掩盖失败。合并由 Owner 决定。
- 只读取既有 xAI 测试令牌的非身份合同字段用于确认 audience，未打印、复制或提交令牌及主体/团队/邮箱；真实厂商 OAuth 完整活体验证仍单列为最后的外部系统验证，不冒充已完成。
