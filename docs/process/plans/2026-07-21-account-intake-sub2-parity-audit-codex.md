# 2026-07-21 账号导入与用户创建全链路审计

| 项目 | 内容 |
| --- | --- |
| Owner 指令 | “不要修，只说明问题就行。报告输出问题。账号导入模块 用户创建模块都要横纵向扫一遍” |
| 范围 | 只读核实 Sub2 当前源码与 HUAKAI 真码；横向覆盖上游账号导入/创建、公开密码注册、社交注册、管理端建用户和首装管理员，纵向追踪入口、身份与租户边界、来源证明、重复冲突、事务一致性、凭据刷新、奖励入账、日志、失败恢复和运维面。 |
| 不包含 | 不修改业务代码，不读取或输出真实凭据，不触碰前端，不创建新 PR。 |
| 成功标准 | 每项结论都有当前源码位置；明确真实缺陷、合理差异和开放问题；逐项说明爆炸半径、受影响与不受影响边界，并结合成熟项目行为和 HUAKAI 现有架构给出修复建议。 |
| 预计时间 | 约 30-60 分钟，只读审计。 |
| 爆炸半径 | 若 OAuth 来源、三身份权限或创建事务判断错误，可能导致手工凭据绕过、跨租户越级、首装被抢占、账号或用户半创建、奖励丢失、凭据不可刷新及日志与真实状态不一致。 |
| 失败模式 | 把 README、搜索命中或 AI 表格当事实；把不同产品 OAuth 混成一种；只核对字符串不追运行入口。缓解方式是逐文件读生产调用链，并把推断与观察分开。 |
| 决策点 | 本轮无实施决策；涉及扩大 OAuth scope、改变部署者/租户权限或替换回调方式时，报告中单列供 Owner 后续拍板。 |
| 执行顺序 | 更新 Sub2 镜像并锁定 SHA；读其账号导入和用户创建生产链；读 HUAKAI 对应入口与完整写链；按厂商、角色和创建方式横向扫描；跑现有聚焦测试；形成一个集中报告，不修改业务代码。 |
