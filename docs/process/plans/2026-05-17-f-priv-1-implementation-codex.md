# 2026-05-17 F-PRIV-1 privacy implementation codex
| Owner directive | "你是 HUAKAI codex executor lane, 任务 = F-PRIV-1 隐私 / 无用户数据日志 实施 (Wave 3 of plan velvety-exploring-lake)." |
| Scope | In: HUAKAI-only Go backend privacy package, audit/ledger/outbox writer hooks, HTTP privacy middleware, credential zeroize helper call sites, schema review script, acceptance tests, and AT matrix status. Out: frontend, Rust, LICENSE, billing core, spec wave, F-PAY, F-COMM, anti-ban implementation, database migrations/schema changes, new dependencies, and all reference-project source. |
| Success criteria | `internal/privacy` defines labels, redactor, 3-channel logger, middleware, zeroize helper, and tests for AT-PRIV-001-001..014; audit ledger / F-OBS DLQ / channel health / voucher/email-adjacent audit payloads pass through the redactor where this patch touches write paths; schema review script rejects raw prompt/completion/body/content columns; requested Go checks pass or blockers are recorded honestly. |
| Time estimate | 1 work session; Owner estimate 5-7 engineer days compressed into a scoped executor patch focused on policy plumbing, middleware, tests, and cross-spec guard coverage rather than broad product feature expansion. |
| Blast radius | Cross-cutting logging/redaction policy, audit ledger append path, F-OBS DLQ payload sanitizer, gateway middleware chain, credential decrypted-byte cleanup, and test/matrix evidence. |
| Failure modes | Overblocking safe metadata: mitigate with explicit allowlist and focused package tests. Underblocking raw content: mitigate with sentinel injection tests across sink fixtures and JSON pre-write guard. Logger channel bleed: mitigate with distinct event types and compile-time interface assertions. Middleware behavior regression: keep wrappers forward-only and preserve request bodies for downstream transit while discarding retained raw copies. Credential lifecycle regression: zeroize only local decrypted buffers after caller-owned conversion/copy. Schema script false positives: limit to migration/query DDL field patterns named by spec. |
| Decision points | Stop before any new dependency, schema migration, auth core, billing ledger/quota enforcement, frontend, Rust, LICENSE, real secret, or destructive operation. If full production external sink integration requires new infrastructure, leave a tested interface/no-op sink and record residual work. |
| Pre-execution checklist | Read `docs/RULES.md`; read `docs/specs/privacy-no-user-data-logs.md` §2/§4/§5 and AT outline; read `backend/cmd/gateway/main.go`; read `backend/internal/credentialstore/crypto.go`; read `backend/internal/auditledger/postgres.go`; read `backend/internal/obs/dlq/redaction.go`; inventory current audit/log write paths with `rg`; confirm clean worktree and no reference source reads. |
| Concrete execution order | Add `internal/privacy` package and package tests; replace F-OBS DLQ sanitizer with shared redactor wrapper; hook audit ledger serialization through redactor guard; add HTTP privacy middleware and wire it before existing request classification; add zeroize helper and credential decrypt key/plaintext cleanup; add schema review shell script; add integration tests for sentinel scan and AT-PRIV rows; update acceptance matrix evidence; run requested tests/build/FAIL scan. |

## Implementation Notes

- This is an implementer-lane HUAKAI-only plan. It intentionally does not read reference projects.
- The previous Wave 2 Codex plan for `velvety-exploring-lake` is present at `docs/process/plans/2026-05-17-f-trust-1-b-append-only-ed25519-codex.md`; no separate Claude implementation plan for this exact F-PRIV-1 patch was found during initial `rg`, so this plan records the Codex execution slice under the Owner's direct Wave 3 instruction.
- External sink integration will be interface-first unless an existing sink is already present; no new table or dependency is allowed in this wave.
