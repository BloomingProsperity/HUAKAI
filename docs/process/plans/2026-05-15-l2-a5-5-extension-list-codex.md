# 2026-05-15 L2-A5.5 OpenSSL Extension List Preflight

| Owner directive | "R-C Lane 2 L2-A5.5 — OpenSSL ClientHello extension list ordering capture diff (最后一个 field family)" |
| Scope | In: HUAKAI internal OpenSSL mimicry adapter, test-only capture diff normalizer, targeted OpenSSL mimicry tests, Codex CLI fingerprint template read-only verification, and `tools/recapture/RUNBOOK.md` §10. Out: dispatch changes, database/auth/billing/quota/deployment files, and all forbidden reference projects (`sub2api`, `new-api`, `portkey`, `helicone`, `litellm`, `all-api-hub`, `envoy-ai-gateway`). |
| Success criteria | `OpenSslMimicryAdapter::new_with_profile` validates that profile `extensions` are an ordered subset of runtime wire extensions; missing or wrong-order profile extensions fail with `PreflightFailed { field: "extensions", ... }`; preflight records native wire extras; `diff_capture_against_profile` reports extension subset/missing/wrong-order status; requested feature combo and targeted tests pass; `git diff --cached --check` is clean; no commit is made. |
| Time estimate | 45-75 minutes wall clock, mostly Rust compile/test time. |
| Blast radius | Medium: only `mimicry-openssl` code and test helpers should change, but profile construction fail-closed behavior can block OpenSSL dispatch if runtime ClientHello drift is detected. |
| Failure modes | OpenSSL runtime emits unsupported or environment-specific extensions: mitigate with ordered-subset policy and recorded extras. Test fixture accidentally copies template list verbatim: avoid by constructing synthetic subset/missing/wrong-order profiles from captured runtime data or minimal independent variants. Existing exact extension diff assumptions fail: migrate only extension diff to explicit subset status while preserving other list comparisons. |
| Decision points | Owner confirmation needed only if implementing exact-match policy, adding a runtime dependency, touching high-risk files, or reading a forbidden reference project becomes necessary. Current plan requires none of those. |
| Pre-execution checklist | 1. Read `docs/RULES.md` and relevant skill instructions. 2. Read current OpenSSL adapter, capture parser, capture diff helper, tests, RUNBOOK §10, and Codex template `extensions` field. 3. Keep all edits inside allowed HUAKAI files. 4. Add implementation and tests. 5. Run targeted cargo tests and four feature-combo checks. 6. Stage only this task's files and run `git diff --cached --check`. |
| Concrete execution order | 1. Extend `OpenSslAdapterError::PreflightFailed` with `missing` and `unexpected` vectors. 2. Add `PreflightExtras` to `OpenSslMimicryAdapter`, store wire extension extras, and expose a getter for tests/ops plumbing. 3. Replace extension-22-only preflight with full ordered-subset extension preflight while keeping extension 22 apply-time guard. 4. Extend test capture diff with `ExtensionsListStatus { Subset, Missing, WrongOrder }`. 5. Add requested tests using independently constructed runtime-based extension vectors. 6. Update RUNBOOK §10 with subset + extras behavior. 7. Run acceptance commands, stage task files, run cached diff check, and report verdict. |

Clean-room note: this task is specifier lane but reads only HUAKAI-internal files and the allowed OpenSSL/http2 references if needed. No forbidden reference project source is in scope, and no reference implementation details will be copied.
