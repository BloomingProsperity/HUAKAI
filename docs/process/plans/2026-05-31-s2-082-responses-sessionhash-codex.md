# 2026-05-31 S2-082 Responses SessionHash

| Owner directive | "Do ONE task now... OpenAI Responses previous_response_id 会话亲和被 prompt-hash 覆盖" |
| Scope | Fix `S2-082` only: preserve OpenAI Responses `previous_response_id` as `RequestMeta.SessionHash` through the live dispatch path when no stronger prompt hash is available. In scope: existing gatewayhttp/proto tests and implementation files already covered by task scope. Out of scope: schema, auth, billing ledger, quota, provider routing redesign, new runtime dependencies, and new files in frozen packages. |
| Success criteria | A discriminating live dispatch test sends `{"previous_response_id":"resp_abc","input":"hi"}` through `/v1/responses` and observes final `RequestMeta.SessionHash == "resp_abc"`. Reverting to unconditional prompt-hash assignment makes the test fail. Targeted tests pass, Codex pre-commit review has no unresolved S0/S1, commit is pushed to `origin work/s2-082`, and the task is moved to review with branch + SHA notes. |
| Time estimate | 45-90 minutes wall clock; one local Codex work session. |
| Blast radius | Request metadata enrichment in the OpenAI Responses dispatch path. Incorrect change could weaken PASR session affinity, alter chat-completions session hashing, or break existing handler tests. |
| Failure modes | Test only exercises proto adapter and misses live handler overwrite: use existing gatewayhttp handler test helpers. Prompt hash remains preferred when present and accidentally hides previous_response_id: use a body with no system/tools per DoD. Frozen-package violation: edit existing files only in `backend/internal/gatewayhttp` and `backend/internal/proto`; no new files there. |
| Decision points | High-risk changes are not expected. If the fix requires auth, billing, quota, schema, deployment, secrets, or a new dependency, park the task for Owner decision. |
| Pre-execution checklist | 1. Confirm task is claimed. 2. Inspect current Responses request parsing and live dispatch metadata enrichment. 3. Add failing gatewayhttp regression test before production edits. 4. Run the targeted test and capture RED. 5. Apply the smallest implementation patch. 6. Run targeted package tests. 7. Stage intended diff and run `codex exec review --uncommitted -m gpt-5.5 -c model_reasoning_effort=xhigh`. 8. Commit, push `HEAD:work/s2-082`, then mark task review with branch + SHA. |
| Concrete execution order | Read `chat_completions_dispatch.go`, `chat_completions_handler.go`, `chat_completions_handler_headers.go`, `openai_responses_request.go`, and existing `chat_completions_dispatch_test.go`; add the live dispatch regression test; verify RED; patch metadata enrichment to avoid overwriting non-empty adapter-derived SessionHash with an empty prompt hash; verify GREEN; run review/push/review-note sequence. |
