# 2026-05-31 S2-006 Codex Plan

| Owner directive | "Do ONE task now... S2-006 Bedrock 流扫描器静默丢弃未知帧" |
| Scope | In: edit existing `backend/internal/gateway/bedrock_stream_scanner.go` and `backend/internal/gateway/bedrock_stream_scanner_test.go`; add this plan artifact. Out: new files under frozen gateway package, provider adapter changes, schema/auth/billing/quota/deploy changes. |
| Success criteria | A Bedrock event-stream exception delivered as a non-chunk event frame yields/returns an error satisfying `errors.Is(err, ErrBedrockException)`, emits an error event or stops with error, and does not deliver a trailing clean `message_stop`; unknown message types fail loud instead of being skipped. |
| Time estimate | 45-75 minutes wall clock; one local Codex implementation pass. |
| Blast radius | Bedrock streaming scan termination behavior only; forwarder already maps `ErrBedrockException` to `UpstreamError5xx`. |
| Failure modes | Over-broad event rejection could break benign control frames; mitigate by preserving explicit benign `initial-response` skip. Weak tests could pass on old code; mitigate with RED run before implementation and marker payload/trailing stop assertions. |
| Decision points | None requiring Owner sign-off: no high-risk files, no new runtime dependency, no schema/auth/billing/quota/payment/deploy changes. |
| Pre-execution checklist | Read `.coordination/DISPATCH.md`; read CLAUDE.md #8/#13/#14; check AWS Bedrock `InvokeModelWithResponseStream`/`ResponseStream` docs; check Smithy Amazon Event Stream semantics; claim target files; write RED tests first; run focused test to see failure; implement minimal scanner change; run focused tests, package tests, codex review, commit, push `work/s2-006`, mark review. |

Concrete execution order:
1. Replace the blanket unknown-event test with discriminating tests: one for event-frame `modelStreamErrorException` and one for unknown message type.
2. Verify the new tests fail on current scanner behavior.
3. Add helper logic that treats Bedrock response exception event-types as protocol errors and unknown message types as `ErrBedrockException`.
4. Preserve skip behavior only for explicitly benign control event(s), not error/exception frames.
5. Run `go test` for the focused package, then Codex review, then commit and push.
