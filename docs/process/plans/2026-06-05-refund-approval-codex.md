# 2026-06-05 Refund Approval Closed Loop - Codex Plan

| Owner directive | "退款审批闭环(用户申请→admin 审批→真退款)MONEY 高危" |
| Scope | In: HUAKAI-only clean-room implementation for persisted refund requests, admin list/approve/reject endpoints, approve-to-`payment.Service.RefundOrder`, migration 0096, OpenAPI sync, cmd/gateway wiring, tests, local commit. Out: `/home/ubuntu/refs`, external reference-source citations, frontend UI, pushing, `LICENSE`, auth core, billing ledger redesign, quota enforcement changes. |
| Success criteria | `payment_refund_requests` exists with tenant/order uniqueness; user-created pending requests persist in Postgres; admin can list pending, approve exactly once, reject without moving money; duplicate approve is idempotent; cross-tenant access is isolated; OpenAPI and gateway route consistency pass; required Go gates pass; commit created with required co-author. |
| Time estimate | 2-4 hours wall clock in one Codex session, dominated by repository exploration, money-path tests, full Go build/test gates, and mutation verification. |
| Blast radius | MONEY path: erroneous approve can double-refund; erroneous reject can move money; bad tenant filter can expose or mutate another tenant's refund request; bad wiring can silently keep in-memory pending requests in production. |
| Failure modes | Duplicate refund if status check or stable idempotency key is missing; partial state if request is marked approved before refund succeeds; data loss if down migration drops live requests; route/spec drift; weak tests that pass without exercising `RefundOrder`; clean-room breach if reference source is read. Mitigation: TDD red/green tests, Postgres row locks, status machine, stable `refund-req:<requestID>` key, guarded down migration, OpenAPI consistency test, no `/home/ubuntu/refs` reads. |
| Decision points | High-risk changes requiring Owner confirmation: `LICENSE`, auth core, billing ledger schema beyond adding refund-request table, quota enforcement, destructive migration rollback policy, real secrets, production deployment. This plan does not require those changes. |
| Pre-execution checklist | 1. Read `docs/RULES.md` and task prompt. 2. Confirm current highest migration is 0095 and allocate 0096. 3. Confirm existing `payment.Service.RefundOrder` and balance/refund tests exist. 4. Avoid new files in frozen packages. 5. Write failing tests before production code. 6. Implement minimal code. 7. Run `gofmt`, package tests, build, vet, cmd/gateway consistency test. 8. Stage intended diff, run Codex review if available, commit. 9. After commit, mutate idempotency/status guard, verify target test fails, restore with git checkout. |
| Concrete execution order | 1. Add tests for memory recorder state machine and admin HTTP approve/reject/list behavior. 2. Add Postgres recorder integration tests for one pending request, duplicate approve, reject no-money, list pending only, cross-tenant isolation. 3. Implement recorder interface expansion and memory methods. 4. Add `0096_payment_refund_requests` up/down migration. 5. Add Postgres recorder implementation in `internal/paymenthttp`. 6. Add admin handlers and mount routes. 7. Wire `cmd/gateway` to `NewPostgresRefundRequestRecorder(pgPool, paymentService)`. 8. Sync OpenAPI. 9. Run required gates, stage, review, commit, mutation check, restore. |

## Independent-Plan Note

This is the Codex independent implementation plan. The task was directly dispatched to Codex as a clean-room implementer in a prebuilt worktree. I will not read any Claude plan or reference source for this work. If a separate Claude plan is later produced, this artifact can be reconciled against it; for this execution, the Owner's direct task text is treated as the start signal.
