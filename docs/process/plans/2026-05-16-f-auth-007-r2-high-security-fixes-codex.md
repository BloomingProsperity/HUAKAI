# 2026-05-16 F-AUTH-007 R2 High Security Fixes

| Owner directive | "F-AUTH-007 round 2 review REJECT 中 2 HIGH security gap fix." |
| Scope | In: PKCE verifier encryption-at-rest via additive migration and application read/write changes; refresh-token cross-user enforcement; targeted tests for both HIGH fixes. Out: reference project source, LICENSE, billing/quota, Rust core_gateway, F-CH-002, F-BILL-002, social identity policy, log redaction broadening, new dependencies. |
| Success criteria | PKCE verifier is written encrypted and read decrypted with raw verifier only in temporary service variables; existing short-lived plaintext OAuth flow rows are invalidated and cleared by migration; refresh requires bearer-authenticated caller and rejects same-tenant cross-user token use with ErrSessionUserMismatch; targeted tests are run or failures are reported. |
| Time estimate | 60-90 minutes wall clock; one Codex executor pass. |
| Blast radius | Authentication callback state validation, session refresh endpoint, session family revocation semantics, and SQL migration ordering. |
| Failure modes | Existing plaintext rows may still need transitional read support; key-provider wiring may be missing in userauth constructors; refresh handler tests may use stubs that do not expose family ownership; audit implementation may not support the requested fields. Mitigation: preserve backward compatibility, reuse existing crypto/key abstractions, keep migration additive, and test the explicit cross-user path. |
| Decision points | Any database destructive migration, auth-core redesign, runtime dependency addition, or change to billing/quota/LICENSE requires Owner confirmation. |
| Pre-execution checklist | Read migrations 0020/0021; inspect userauth PKCE read/write; inspect session refresh handler and bearer middleware; inspect existing AES-GCM PKCE pattern; inspect usersession store/rotation; implement additive migration 0024; patch scoped Go code; add AT-AUTH-007-011; run requested tests and build. |
| Concrete execution order | 1. Read only HUAKAI internal files named by Owner. 2. Add encrypted PKCE column migration. 3. Wire PKCE encrypt/decrypt through existing key provider pattern. 4. Enforce refresh family ownership against bearer user and tenant. 5. Add targeted cross-user refresh test. 6. Run requested Go checks. 7. Report changed files, test result, diff stat, and clean-room self-check. |
