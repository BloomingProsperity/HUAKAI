# 2026-06-06 Admin Dispute List Codex Plan

| Owner directive | "Add ADMIN dispute LIST endpoint to HUAKAI (branch fix/dispute-admin-list). Verified partial: admin can resolve-by-id but cannot LIST/filter disputes. Reach CLOSURE: admin list route + SQL + store + tests + gate-ready. Read-only. No shortcuts." |
| Scope | Add a read-only admin dispute list endpoint over the existing `cost_disputes` table. In scope: SQL query, generated db access updates, audit store method, control HTTP handler, route registration, OpenAPI path, unit/integration-style tests that PM can run. Out of scope: migrations, billing ledger mutation, auth core changes, quota changes, commits, reading `/home/ubuntu/refs`. |
| Success criteria | `GET /v1/admin/disputes/` requires admin auth, requires a tenant scope authorized by `CanIssueForTenant`, lists all disputes for that tenant across users, supports optional `status`, `limit`, and `offset`, caps limit at existing dispute max, and is documented in OpenAPI. Tests discriminate tenant leakage, ignored status filter, pagination/cap handling, missing admin auth, and accidental user-scoped reuse. |
| Time estimate | 45-75 minutes wall-clock in this session; PM-only integration runs remain external because the sandbox cannot run `integration_pg`/sockets. |
| Blast radius | Admin operations API, generated audit SQL accessors, dispute store interface, control HTTP dispute tests, OpenAPI route consistency. Frozen packages are only modified in existing files; no new files are added under `gatewayhttp`, `gateway`, or `proto`. |
| Failure modes | SQL optional status filter mishandled: add query-string and SQL shape tests. Tenant filter omitted: add tenant-scope test and query text check. Handler accidentally user-scopes: add multi-user admin contrast test. Route omitted from OpenAPI: add OpenAPI path. Generated code mismatch: update `querier.go` and generated query file consistently and run build/tests. |
| Decision points | None expected. High-risk work such as migration, auth core change, billing/quota/ledger change, new runtime dependency, or destructive command remains out of scope and would need Owner confirmation. |
| Pre-execution checklist | Read required HUAKAI files; confirm status enum from migration; write failing tests before production code; modify only existing frozen-package files; add OpenAPI path; run available Go build/vet/unit checks; report commands that cannot run in sandbox. |
| Concrete execution order | 1. Add RED tests for control HTTP admin list and audit store/query behavior. 2. Add SQL query to `backend/sql/queries/cost_disputes.sql` and update generated db files. 3. Add `ListForAdmin` store method and validation/capping. 4. Add admin list handler and route. 5. Add OpenAPI path. 6. Run targeted tests, build, vet, and unit package checks. |

Note: this is the Codex-side independent plan. No `/home/ubuntu/refs` reference source is in scope or read for this HUAKAI-native task.
