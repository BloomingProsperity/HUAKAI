# 2026-05-14 R-3 template backfill codex plan

| Owner directive | "把抓到的 codex/kiro 真实指纹回填进 mimicry template (TLS + HTTP + 鉴权 三层), 替换 stub。" |
| Scope | In: codex/kiro template real TLS+HTTP+auth backfill, template schema doc, mimicry loader struct compatibility, focused tests, kiro request-signature research doc. Out: gemini-advanced.json changes, registry/factory/utls_dialer behavior changes, database/auth/billing/quota/deploy changes. |
| Success criteria | codex-cli.json and kiro-cli.json have real captured TLS fields plus HTTP/auth metadata; gemini remains stub; mimicry loader accepts old and new templates; focused tests assert real templates are non-stub; `go test ./internal/transport/...`, `go build ./internal/transport/...`, and `go vet ./internal/transport/...` pass or any blocker is recorded. |
| Time estimate | 60-90 minutes wall clock; one Codex work unit. |
| Blast radius | Low/medium: data templates and mimicry metadata parsing. Runtime TLS dialing should remain unchanged unless callers consume new fields later. |
| Failure modes | Mis-copied fingerprint values could cause mimicry mismatch; mitigate by reading `/tmp/fingerprint-data/*` directly and recording key values. JSON field drift could break loader; mitigate with tests for real and stub templates. Clean-room risk from source-analysis prose; mitigate by recording behavior and endpoints only, no copied source implementation. |
| Decision points | No Owner sign-off needed for low/medium scope requested explicitly. Stop only for high-risk files (`LICENSE`, secrets, auth core, billing, quota, schema, deploy), which are out of scope. |
| Pre-execution checklist | 1. Preserve existing dirty worktree changes. 2. Read captured TLS/HTTP files and current templates/tests. 3. Patch templates and docs. 4. Patch mimicry metadata structs with backward-compatible optional fields. 5. Run focused and package checks. 6. Write `/tmp/codex-template-backfill-final.txt` with file list, LoC, key fields, test evidence, and IsStub result. |
| Concrete execution order | Plan -> inspect capture/template shapes -> update JSON templates -> add schema/research docs -> update Go structs/tests -> run formatting/checks -> final evidence file -> Chinese Owner summary. |
