# 2026-06-03 admin usage overview codex

| Owner directive | "管理员运营仪表盘汇总总览,只读"; "严禁读任何外部参考源码"; "实现+验证"; "不要 git commit" |
| Scope | In: add read-only admin usage overview aggregation queries, HTTP handler, route, OpenAPI path, and discriminating tests. Out: external reference source reads, schema migrations, runtime dependencies, caching unless already trivial, auth/billing/quota core changes, commits. |
| Success criteria | `/v1/admin/usage/overview` returns `window`, aggregate `totals`, and daily `trend`; totals use `actual_cost` as decimal text; success rate is successful end classes divided by total requests; active users/keys are distinct counts; existing admin gate wraps the route; requested gate command passes or blockers are recorded. |
| Time estimate | 45-75 minutes wall clock; one Codex implementation pass plus verification. |
| Blast radius | Medium: generated sqlc billing query types and one admin route. Risk is limited to read-only SELECTs and admin-only HTTP surface. |
| Failure modes | Wrong aggregation window admits historical rows; `COUNT` instead of `COUNT(DISTINCT)` inflates active counts; success rate ignores end-class filter; decimal money accidentally converted through float; OpenAPI route drift breaks consistency tests; sqlc generation may reformat adjacent generated code. Mitigation: TDD stub tests plus SQL string guard tests, decimal string formatting only, run sqlc and gate commands. |
| Decision points | None expected. High-risk paths (`LICENSE`, secrets, schema migrations, auth core, billing ledger writes, quota enforcement, runtime dependencies, deployment scripts) are out of scope and would require Owner confirmation. |
| Pre-execution checklist | 1. Read local `CLAUDE.md` and `AGENTS.md`. 2. Confirm coordination lock for target files. 3. Read existing HUAKAI leaderboard/performance SQL, handlers, tests, route, and OpenAPI pattern. 4. Write failing tests before production code. 5. Run sqlc generation after query edits. 6. Run focused package tests and requested gate. |

## File Scope

- `backend/sql/queries/usage_analytics.sql`: add `AggregateUsageOverviewTotals` and `AggregateUsageOverviewTrendByDay` SELECT-only queries.
- `backend/internal/db/billing/usage_analytics.sql.go`: generated by `sqlc generate`.
- `backend/internal/db/billing/querier.go`: generated by `sqlc generate`.
- `backend/internal/db/billing/models.go`: generated only if sqlc changes shared generated models.
- `backend/internal/db/billing/sql_filters_test.go`: add SQL mutation guards for window, distinct active counts, success filter, day bucketing.
- `backend/internal/usageanalyticshttp/overview_handler.go`: new non-frozen package file serving the admin overview response.
- `backend/internal/usageanalyticshttp/overview_handler_test.go`: discriminating stub tests for totals, trend, window cap, and invalid params.
- `backend/cmd/gateway/routes_usageadmin.go`: add the admin-gated overview route.
- `docs/openapi/openapi.yaml`: add `/v1/admin/usage/overview` contract.

## Clean-Room Boundary

This work intentionally uses only the PM-provided behavior summary and HUAKAI-local leaderboard/performance/time-series patterns. No external reference repositories or `/home/ubuntu/refs` paths are in scope. The usual reference-project prestudy rule is superseded here by the Owner/PM explicit clean-room instruction for this task.

## Concrete Execution Order

1. Add failing handler tests with an in-memory stub implementing the expanded `usageanalyticshttp.Querier`.
2. Add failing SQL string guard tests for the new generated constants.
3. Add the two SQL queries and run `cd backend && sqlc generate`.
4. Implement `NewOverviewHandler`, response structs, window parsing reuse, decimal normalization, success-rate formatting, and trend formatting.
5. Add `/v1/admin/usage/overview` to `mountUsageAdminRoutes`.
6. Add the OpenAPI path.
7. Run focused tests, then the requested gate command.
