# 2026-06-07 A key expiry sweep worker

| Field | Plan |
| --- | --- |
| Owner directive | "TASK: 模块A闭环 — 入站 API key 过期清扫 worker(AUTH-150:expires_at 已有,但无 worker 把过期 key 状态翻成 expired,列表显示陈旧 active)。Branch fix/a-keyexpiry. HUAKAI-internal,clean-room(禁读参考源码)。全加性、安全中性(认证层已就地拒绝过期 key;本任务只修正展示状态,不授予任何访问)。强而不破。" |
| Scope | In: new `backend/sql/queries/api_key_expiry.sql`, sqlc config/generated auth DB code, new `backend/internal/apikeyexpiry` package, additive `backend/internal/config` env fields/tests, and additive `backend/cmd/gateway` wiring/lifecycle/tests. Out: reference-project source reads, schema/table/enum/index migrations, frozen package new files, auth resolver behavior changes, billing/quota/auth core rewrites, new runtime dependencies, commits. |
| Success criteria | `SweepExpiredKeys` flips only `api_keys` rows with `status='active'`, non-null `expires_at <= now()`, and `deleted_at IS NULL` to `status='expired'`; future active keys and revoked/disabled/deleted keys are untouched; the service is idempotent and loops through bounded batches; gateway starts a default-on bounded worker with env override/disable; required build/vet/test commands pass or blockers are reported honestly; integration test name is reported for PM fresh-DB run. |
| Time estimate | 60-100 minutes wall clock; one Codex implementation pass plus local red/green verification. |
| Blast radius | Medium-low: changes add one data UPDATE query and a background worker. Runtime access safety is neutral because the existing resolver already refuses expired keys before bcrypt success. Config/wiring touches gateway boot path, so misconfiguration parsing must fail loudly and Stop must be graceful. |
| Failure modes | Query could update future keys if `expires_at <= now()` is wrong; mitigate with integration_pg test covering future active key. Query could touch revoked/disabled keys; mitigate with revoked fixture assertion. Query could scan/update too many rows; use bounded CTE limit and loop in service. Worker could block shutdown; use context cancellation plus Stop wait following existing worker patterns. Config could silently accept invalid intervals/batches; add config tests. |
| Decision points | Stop for Owner confirmation only if implementation requires schema migration, new dependency, frozen package new file, auth/billing/quota enforcement change, production secret/deployment edit, destructive command, or feature shrinkage. None expected. |
| Pre-execution checklist | Confirm worktree is `fix/a-keyexpiry`; read local AGENTS rules; read `0007_l0_inbound_auth` schema/index; read auth resolver expiry check; read sqlc config/query style; read gateway worker lifecycle pattern; confirm no reference source reads; write failing tests before production code; run sqlc generation and required Go checks. |
| Concrete execution order | 1. Add red integration_pg test in new `internal/apikeyexpiry` package for past-active/future-active/past-revoked keys. 2. Add red unit tests for nil-safe sweeper/service loop and config defaults/env parsing. 3. Add sqlc query with bounded CTE update returning affected count and generate auth DB code. 4. Implement `internal/apikeyexpiry` service and worker. 5. Add config fields/env parsing for default-on interval and batch limit. 6. Wire worker into `cmd/gateway` runtime and lifecycle. 7. Run targeted red/green checks, sqlc, required build/vet/test, and report PM integration_pg test name. |

Clean-room note: this is HUAKAI-internal only. No reference-project source should be read or cited for this task.
