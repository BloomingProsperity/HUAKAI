# 2026-06-02 payment-loop-slice-a-codex
| Owner directive | "HUAKAI Phase1 钱闭环 Slice A — 钱包充值闭环(像 sub2api 可插拔)。IMPLEMENTER lane。worktree branch work/payment-loop-slice-a...自主:实现→判别测试→build/test→self-review(≤2 轮)→push。" |
| Scope | In: add non-frozen `backend/internal/paymenthttp` package, mount user recharge and public webhook routes, add production HMAC provider verification, add release-mode guard for mock provider, update OpenAPI route contract, add discriminating unit/integration tests. Out: schema changes, payment store rewrite, billing ledger changes, auth core changes, new runtime dependencies, Stripe/易支付/微信/支付宝 SDK implementations, merging to `landing`. |
| Success criteria | `POST /v1/users/me/recharges` takes tenant/user only from session context and calls existing `payment.Service.OpenRecharge`; `POST /v1/payment/webhooks/{provider}` is public but verifies provider HMAC before calling existing `FulfillCallback`; forged signatures do not credit; replayed completed callbacks are idempotent; mismatched provider/amount/currency return 200 with audit/no credit; cross-tenant trade numbers do not credit another tenant; mock provider is rejected in production; requested build/tests and self-review run before push. |
| Time estimate | 2-4 wall-clock hours; one Codex implementation session plus review iteration. |
| Blast radius | Public HTTP route surface, payment webhook security boundary, OpenAPI contract, and gateway boot/wiring. Existing money persistence remains in `backend/internal/payment` and is reused unchanged except tests may exercise it. |
| Failure modes | Bad HMAC canonicalization could reject real callbacks or admit forged callbacks; mitigate with raw-body tests and timestamp-window checks. Tenant/user body trust could allow unauthorized recharge creation; mitigate by not accepting tenant/user in request DTO and testing session-only identity. Webhook tenant lookup could credit another tenant; mitigate by embedding server-generated tenant prefix in external trade numbers and testing cross-tenant collisions. Mock provider exposure in production could create money without real provider proof; mitigate with release-mode guard and test. OpenAPI drift could break generated clients; mitigate by updating `docs/openapi/openapi.yaml` and running consistency/build checks. |
| Decision points | Owner must later confirm first real payment platform, whether Stripe SDK is approved as a new runtime dependency, and whether endpoint paths remain `/v1/users/me/recharges` + `/v1/payment/webhooks/{provider}`. This slice proceeds with HMAC-only provider and no dependency addition per directive. |
| Pre-execution checklist | 1. Read project rules, payment service/store, session middleware, gateway routing/wiring, and OpenAPI consistency test. 2. Claim target files in `.coordination`. 3. Write failing discriminating tests before production implementation. 4. Implement minimal `paymenthttp` package and gateway wiring. 5. Update OpenAPI. 6. Run gofmt. 7. Run targeted tests, requested build/tests, mutation self-check, Codex review, commit, push. |
| Target packages | New files only in `backend/internal/paymenthttp` (not frozen). Existing file edits in `backend/cmd/gateway/{routes.go,wiring.go,config.go}` and `docs/openapi/openapi.yaml`. No new files in frozen `backend/internal/gatewayhttp`, `backend/internal/gateway`, or `backend/internal/proto`. |
| Clean-room posture | IMPLEMENTER lane. No non-MIT reference source will be read in this session; implementation is based on Owner-provided behavior contract and HUAKAI internal code. |

## Concrete execution order

1. Add `paymenthttp` tests for HMAC provider verification, bad signature rejection, replay idempotency, mismatch no-credit behavior, cross-tenant no-credit behavior, mock-in-production rejection, and session-only recharge identity.
2. Run `go test ./internal/paymenthttp/...` and confirm the new package fails because implementation is absent.
3. Implement `PaymentProvider`, HMAC provider, mock dev-only provider, route handlers, raw body limit, JSON parsing, error mapping, and server-generated external trade numbers carrying tenant lookup prefix.
4. Wire `paymenthttp.MountRoutes` into existing `backend/cmd/gateway/routes.go` using existing `paymentService`, session middleware for recharge, and public no-user-auth webhook route.
5. Add gateway release-mode provider registry helper in existing `wiring.go/config.go` only if needed for production mock rejection.
6. Update OpenAPI with both paths and compact request/response schemas.
7. Run `gofmt` on touched Go files.
8. Run `go test ./internal/paymenthttp/...`, `go test ./internal/payment/...`, `go build ./...`, and integration tests when `HUAKAI_DATABASE_URL` is available.
9. Perform mutation self-check by temporarily disabling signature verification or replay status handling and confirming the discriminating tests fail, then restore.
10. Stage intended diff, run `timeout 600 codex exec review --uncommitted -m gpt-5.5 -c xhigh < /dev/null`, normalize findings, fix S0/S1 within max two rounds.
11. Commit with review verdict in body, push `origin HEAD:work/payment-loop-slice-a`, and do not merge to `landing`.
