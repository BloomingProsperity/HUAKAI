# 2026-06-06 USER Self-Service Usage CSV Export

| Owner directive | "Add USER self-service usage CSV export to HUAKAI (branch fix/me-data-export). Verified partial: admin export.csv exists; no user self-export (/v1/me/usage is JSON-list only). Reach CLOSURE. Self-scoped, no cross-user leak. No shortcuts." |
| Scope | In: add `GET /v1/me/usage/export.csv` handler in new package `backend/internal/meexporthttp`, mount it under existing `/v1/me` `SessionMiddleware`, add discriminating unit tests, update `docs/openapi/openapi.yaml`, run requested build/vet/unit checks. Out: no migrations, no commits, no `/home/ubuntu/refs` reads, no changes to frozen package file counts. |
| Success criteria | Authenticated session user can export only their own usage rows as CSV with required headers; query tenant/user/key scope is ignored; CSV formula cells are guarded; `from`/`to` are required RFC3339 and capped at 366 days; unauthenticated requests return 401 through session middleware; OpenAPI includes the route; requested local checks pass or failures are reported truthfully. |
| Time estimate | 60-90 minutes wall clock; one Codex implementation pass with TDD red/green verification. |
| Blast radius | Read-only user export path, route registration, OpenAPI docs, and tests. No database schema, auth core, billing ledger, quota enforcement, deployment, or secrets changes. |
| Failure modes | Cross-user leak if scope is accepted from query string or user-only scoping is too broad; mitigated by asserting store params and A/B fixtures. CSV injection if helper is bypassed; mitigated by formula-cell test. OpenAPI drift; mitigated by updating route docs and running targeted gateway/openapi tests. Handler invoked without session; mitigated by a direct handler test expecting 401 before any store call. |
| Decision points | None requiring new Owner sign-off: requested route, columns, auth boundary, package boundary, no migration, and PM-owned integration_pg are explicit. |
| Pre-execution checklist | 1. Read admin export helper behavior. 2. Read self-usage JSON scoping behavior. 3. Read session middleware identity shape. 4. Write tests in new package before production handler. 5. Verify tests fail for missing package/route behavior. 6. Implement minimal handler and route mount. 7. Update OpenAPI. 8. Run requested checks. |
| Concrete execution order | Create `internal/meexporthttp` tests first, including mutation comments. Implement handler using session identity and `dbbilling.ListUsageRecordsParams{TenantID}` plus an in-handler `TenantID + UserID` row guard because the existing store params do not expose `UserID`; support CSV by default and JSON when `format=json`. Mount route inside `/v1/me` session group in `cmd/gateway/routes.go`. Update OpenAPI path. Run gofmt, targeted tests, build, vet, and stage `backend/ docs/` without committing. |

Assumptions:

- Because `auth.SessionIdentity` contains `TenantID` and `UserID` but not `APIKeyID`, the self-service session export scope must use `TenantID + UserID`. It must never use `tenant_id`, `user_id`, or `api_key_id` query parameters.
- The existing `/v1/me/usage` JSON endpoint remains API-key scoped and unchanged.
- The Owner-requested `?format=json` is accepted as a non-widening alternate representation for the same export rows. Unsupported format values should fail closed with 400 rather than silently changing scope.
