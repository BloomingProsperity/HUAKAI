# 2026-06-06 Moderation Bulk Import Codex Plan

| Owner directive | "TASK: Add BULK import for moderation keywords/hashes (branch fix/moderation-bulk-import). Verified real_missing: only single-item create exists. Reach CLOSURE. Admin-gated, tenant-scoped, partial-success. No shortcuts." |
| Scope | Add HUAKAI-native JSON bulk import for moderation keywords and hashes. In scope: `backend/internal/moderationhttp`, `backend/internal/moderation`, `backend/sql/queries/moderation.sql`, generated sqlc moderation files, integration/unit tests, `docs/openapi/openapi.yaml`. Out of scope: migrations, `/home/ubuntu/refs`, frozen package new files, CSV import, commits, PM-owned `integration_pg` run. |
| Success criteria | `POST /admin/v1/moderation/keywords/bulk` and `/hashes/bulk` are admin-gated, tenant-checked, cap request size, validate each row independently, skip duplicates, insert valid rows, and return `{accepted, skipped_duplicate, errors}`. Tests cover all-inserted, partial success, bad SHA-256 rejection, tenant scope, and admin auth. OpenAPI includes both new public routes. |
| Time estimate | 2-3 wall-clock hours; one Codex implementation session. |
| Blast radius | Moderation admin API, moderation sqlc package, generated query code, OpenAPI docs. No auth core, billing, quota, schema, deployment, or frozen package new files. |
| Failure modes | Weak tests that pass with single-row insert; mitigate with discriminating integration tests and explicit mutation comments. Bad batch SQL can miscount duplicates; mitigate with returned row keys and counts from attempted valid rows. Tenant leakage; mitigate with HTTP auth test and integration assertions on stored tenant IDs. SQLC drift; mitigate by regenerating only moderation sqlc output and running build/vet/tests. |
| Decision points | Owner already authorized direct execution in the task brief. No additional Owner sign-off needed unless implementation requires schema migration, new runtime dependency, frozen-package new files, auth core change, billing/quota change, or deleting files. |
| Pre-execution checklist | 1. Read named moderation HTTP handlers and store. 2. Confirm table columns and unique indexes from local migration. 3. Write failing tests first. 4. Add only new sqlc queries to `sql/queries/moderation.sql`. 5. Regenerate sqlc moderation output. 6. Implement bulk store and HTTP handlers in existing allowed packages. 7. Update OpenAPI. 8. Run requested build, vet, targeted tests; stage `backend/ docs/`; do not commit. |
| Concrete execution order | Add bulk request/result domain types, store tests, query SQL, generated sqlc, store implementation, HTTP tests, HTTP handlers/routes, OpenAPI docs, then verification. |

Parallel-plan note: AGENTS.md prefers independent Claude and Codex plans plus synthesis for non-trivial work. This task was directly assigned to Codex with "Reach CLOSURE"; no Claude draft is present in this worktree, so this plan records the Codex lane and proceeds without cross-agent blocking.
