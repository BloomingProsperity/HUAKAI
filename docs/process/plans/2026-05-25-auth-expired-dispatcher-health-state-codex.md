# 2026-05-25 auth_expired dispatcher health_state S3
| Owner directive | "auth_expired synthesis §D S3 切片: dispatcher 接 health_state filter" |
| Scope | In: dispatcher pool-account eligibility, credentialworker audit-outcome to provider_accounts health_state transition, focused mutation-discriminating tests, real PostgreSQL round-trip tests. Out: frozen packages `backend/internal/gatewayhttp`, `backend/internal/gateway`, `backend/internal/proto`; auth core redesign; billing ledger/quota enforcement; reference-project source reading; git add/commit/push. |
| Success criteria | Dispatcher only selects `health_state='healthy'`; `throttled`/`revoked`/`cooldown` rows are skipped until `health_state_until <= now()` and then reset to healthy; credentialworker maps `auth_expired`, `rate_limit_exceeded`, `risk_control_triggered`, `account_disabled`, and `refresh_succeeded` to the requested health states; throttled/revoked cooldown defaults are 3m/30m and configurable through a scheduler option; tests fail under the named mutations; requested Go test/build commands either pass or blockers are recorded truthfully. |
| Time estimate | 2-3 hours wall clock; agent time mostly in test/code/verification loops. |
| Blast radius | Provider account selection hot path, credential refresh audit side effects, generated billing SQL constants, one provider_accounts CHECK/default migration. A bug could over-exclude all accounts, route to revoked credentials, or leave health state stale after audit outcomes. |
| Failure modes | Legacy schema still permits only `operational/degraded/failed/cooling_down/error`; mitigation: add a narrow migration that changes the existing CHECK/default but adds no column. Non-discriminating tests; mitigation: fixtures use a throttled winner versus a healthy fallback and an expired revoked high-priority row versus healthy fallback. Audit-only success without health update; mitigation: unit fake health store and PG readback assert exact state/until. Permanent revoked accidentally auto-recovers; mitigation: `account_disabled` stores NULL `health_state_until` and expired-reset CTE requires a non-NULL deadline. |
| Decision points | Schema CHECK/default change is high-risk by project rule, but this Owner task states AE-D2 is locked and explicitly requires the new state literals; implement as the smallest additive migration and record the risk. If Owner later rejects schema mutation, code can be adapted to legacy synonyms, but that would not satisfy the requested `revoked/throttled/healthy` assertions. |
| Pre-execution checklist | 1. Confirm dispatcher files: `backend/internal/pool/dispatcher/account_source.go`, `backend/sql/queries/pool_accounts.sql`, generated `backend/internal/db/billing/pool_accounts.sql.go`. 2. Confirm credentialworker audit path: `backend/internal/credentialworker/scheduler.go`, `audit.go`, `options.go`, existing unit and PG tests. 3. Confirm target packages are not frozen. 4. Write failing tests first. 5. Implement minimal health policy and DB update paths. 6. Run focused tests, requested race test, build, and review command if CLI supports it without staging. |
| Concrete execution order | Add failing dispatcher PG tests for throttled skip and expired revoked reset. Add failing credentialworker unit/PG tests for auth_expired and round-trip transitions. Implement SQL eligibility normalization/filter. Add health-state transition helper and scheduler option. Wire `recordAudit` to update health state in the same transaction when tx mode is active and through an optional store in legacy tests. Add migration 0056 for health_state literals/default. Run gofmt and verification. |

## File Scope

- Modify `backend/sql/queries/pool_accounts.sql` and generated `backend/internal/db/billing/pool_accounts.sql.go`; package `internal/db/billing` is not frozen.
- Add `backend/sql/migrations/0056_provider_account_health_state_check.up.sql` and `.down.sql`; migration is the smallest schema change needed for the Owner-requested state literals.
- Modify `backend/internal/pool/dispatcher/account_source.go` only if generated row shape requires mapping new fields; add focused tests under `backend/internal/pool/dispatcher/`; package is not frozen and below budget.
- Add `backend/internal/credentialworker/health_state.go` plus tests; modify `scheduler.go`, `audit.go`, and `options.go` only for wiring.
- Do not modify `LICENSE`, secrets, payment logic, auth core, billing ledger, quota enforcement, or frozen packages.

## Clean-Room Note

This is HUAKAI-internal implementation work. No non-MIT reference project source is in scope and no reference source will be read.
