# 2026-05-31 S1-005 Codex Plan

| Owner directive | "HUAKAI worker local-codex... S1-005... anthropic_claude_session either fully servable or fail-closed unregistered" |
| Scope | In scope: `backend/internal/provider/registrydefault/default.go` and its package tests. Out of scope: protocol-layer wiring, auth/OAuth refresh flow, database schema, production deploy. |
| Success criteria | `anthropic_claude_session` is not default-registered unless a complete serving path exists; the package has a discriminating test that fails if the half-wired provider adapter is restored to the default registry; focused tests and build pass; pre-commit Codex review has no unresolved S0/S1. |
| Time estimate | 30-60 minutes wall clock. |
| Blast radius | Default provider registry construction. Risk is rejecting a session protocol family that may have been accidentally available, but that is the requested fail-closed behavior unless the full serving path is wired. |
| Failure modes | Weak test still passes if registration is restored; mitigate with an explicit `ErrAdapterNotRegistered` assertion for `ProtocolAnthropicClaudeSession`. Accidental removal of stable API-key Anthropic messages path; mitigate by keeping `ProtocolAnthropicMessages` in the default registered list. |
| Decision points | If Owner prefers making `anthropic_claude_session` fully servable instead of fail-closed, that requires broader protocol/wiring scope and a separate task; current path chooses fail-closed within S1-005 file scope. |
| Pre-execution checklist | Task `S1-005` started and file locked; read `.coordination/DISPATCH.md`; read `CLAUDE.md#8/#11/#12/#14`; inspect current default registry and tests; follow TDD red-green; run focused tests; stage intended diff; run `codex exec review --uncommitted -m gpt-5.5 -c model_reasoning_effort=xhigh`; commit only if no unresolved S0/S1. |

## Execution Order

1. Add a focused failing test in `backend/internal/provider/registrydefault/default_test.go` that requires `ProtocolAnthropicClaudeSession` to be absent from the default registry and return `provider.ErrAdapterNotRegistered`.
2. Run `go test ./internal/provider/registrydefault` from `backend` and confirm the new test fails because the current default registry still registers the half-wired adapter.
3. Remove default registration of `ProtocolAnthropicClaudeSession` from `backend/internal/provider/registrydefault/default.go`; keep the constant for routing/config compatibility and keep `ProtocolAnthropicMessages` registered.
4. Update existing registry tests so the default registered protocol list and adapter reachability checks reflect fail-closed behavior.
5. Re-run focused tests, then broader relevant Go tests if focused tests pass.
6. Stage only S1-005 files, run Codex review with xhigh reasoning and no fast mode, fix any S0/S1, then commit.
7. Submit `task.sh review S1-005 "<commit> + self-review summary"`; do not mark done.
