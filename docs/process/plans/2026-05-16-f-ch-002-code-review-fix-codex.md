# 2026-05-16 F-CH-002 channel health code review fix

| Owner directive | "F-CH-002 channel health code review fix (3 HIGH + 2 MED)" |
| Scope | In: wire channel health signals into gateway request attempts, lazy cooldown ramp recovery, atomic health state/audit/trust-ledger writes, PASR degraded/ramp behavior, focused unit tests. Out: F-AUTH-007, F-CRED-001, LICENSE, billing, quota, Rust core_gateway, database schema changes, reference-project source. |
| Success criteria | Gateway attempts emit normalized channel health signals; expired cooldown can ramp without manual operator action; state, channel audit, and trust ledger writes commit or roll back together; PASR returns all-degraded errors and samples ramping channels by configured percentage; requested Go test subset runs or blockers are recorded. |
| Time estimate | 2-4 wall-clock hours depending on existing uncommitted code shape; one Codex executor lane. |
| Blast radius | Backend gateway request handling, channel health persistence, failover eligibility, pool selection priority. |
| Failure modes | Signal hook can misclassify upstream failures, mitigated by using bounded classes only; transaction wiring can bypass rollback, mitigated by tests with injected failures; lazy ramp can create stale reads, mitigated by re-reading state after transition; PASR sampling can make tests flaky, mitigated with deterministic RNG injection if available or bounded test seams already present. |
| Decision points | No Owner sign-off needed for listed medium-risk implementation/test changes. Stop if a fix requires schema migration, new runtime dependency, auth/billing/quota core edits, real secrets, or destructive commands. |
| Pre-execution checklist | Read project rules and relevant skills; inspect existing dirty worktree; inspect target files before editing; preserve unrelated changes; avoid reference-project source; implement smallest safe patches; run focused tests; report diff stat and residual risks. |
| Concrete execution order | 1. Inspect channelhealth service/store/failover/audit and gateway/pool current code. 2. Add gateway health signal interface and per-attempt signal application. 3. Add lazy cooldown expiry ramp transition in eligibility path. 4. Add transaction wrapper and atomic state/audit/trust ledger writes. 5. Adjust PASR all-degraded and ramp/degraded priority behavior. 6. Add focused tests for production path signal, lazy ramp expiry, audit rollback, and PASR behavior. 7. Run requested Go tests and summarize results. |
