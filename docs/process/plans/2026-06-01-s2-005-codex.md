# 2026-06-01 S2-005 Codex Plan

| Owner directive | "Fix audit finding S2-005 to PRODUCTION QUALITY." |
| Scope | In: billing receipt hook failure recovery, settlement recovery outbox intent preservation, request construction wiring, focused tests, migration for one DLQ event kind. Out: full scheduler outbox consumer, precise cross-threshold quota algorithm, new billing/quota schema beyond the DLQ CHECK extension. |
| Success criteria | Receipt hook failures enqueue a durable replay intent without making successful settlement look fully proven; receipt replay can append the missing receipt; scheduler outbox intent is serializable and survives post-delivery settlement recovery; targeted tests fail under mutation and pass with the fix; `go test` relevant packages and `go build ./...` pass from `backend/`. |
| Time estimate | 2-3 focused agent hours including mutation check, build, self-review, commit, and push. |
| Blast radius | Money-path settlement, audit receipt generation, DLQ event-kind validation, gateway request builders. No frozen package new files; only existing frozen `gatewayhttp` files may be edited. |
| Failure modes | Duplicate receipt replay rows: mitigate with tenant+claim+request idempotency key. Receipt replay loops: replay handler does not enqueue another recovery event. Outbox duplicate on recovery: replace non-serializable callback with an explicit bool so original and replay intent are deterministic. Schema risk: add a narrow CHECK value and down migration that refuses rollback while rows exist. |
| Decision points | No Owner decision needed inside this delegated worker task. Risk flagged as schema/billing in final output because the change touches billing proof-chain and DLQ CHECK migration. |
| Pre-execution checklist | Read `CLAUDE.md`, `AGENTS.md`, `docs/RULES.md`, S2-005 audit row, `docs/specs/_invariants/cross-module-boundaries.md`, `docs/specs/observability-billing.md`, `docs/specs/trust-chain-user-verifiable-ledger.md`; verify premise at HEAD; claim coordination locks; write failing tests before production edits. |

## Execution Order

1. Add failing tests for receipt-hook durable recovery, receipt replay handler, DLQ kind semantics, gateway outbox intent wiring, and settlement recovery payload preservation.
2. Implement `cost_receipt_append` DLQ kind plus migration.
3. Add receipt recovery payload/enqueue/replay support inside `internal/audit` without creating a new frozen package file.
4. Replace `SettleRequest.OutboxEmitter func() bool` with a serializable outbox intent and wire it from provider-backed gateway settlement requests.
5. Update settlement recovery payload round-trip to preserve the outbox intent.
6. Run red/green and mutation checks, then build, self-review, commit, and push.
