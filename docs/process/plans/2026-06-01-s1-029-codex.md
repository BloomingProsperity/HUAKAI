# 2026-06-01 S1-029 Codex Plan
| Owner directive | "Fix audit finding S1-029 to high quality and push it for review." |
| Scope | In: HUAKAI-only S1-029, delivered stream rows with no provider usage, zero actual cost, `pending_reconciliation=true`, and no worker. Out: positive estimated billing, provider usage retrieval, schema migrations, unrelated billing/audit fixes. |
| Success criteria | Current premise is confirmed; a worker can append a zero-delta reconciliation marker for eligible no-usage pending rows; pending-only admin usage queries exclude rows finalized by that marker; tests fail if the worker path is removed or targets the wrong row shape; `go build ./...` and relevant tests pass from `backend/`. |
| Time estimate | 1-2 hours wall clock, one Codex implementation pass plus verification/review. |
| Blast radius | Money/billing reconciliation path, admin usage pending filter, gateway background worker lifecycle. No auth, quota enforcement, payment ledger, secrets, or schema changes. |
| Failure modes | Worker finalizes real reported usage: guard on zero token/cache buckets, zero cost, inferred source, partial stream, delivered frames. Worker creates duplicate markers: lock candidate rows and skip existing marker source. Pending view hides still-open rows: use the same marker source exclusion only when pending-only is requested. Runtime leak on shutdown: wire Stop into existing lifecycle. |
| Decision points | No extra Owner question because the direct task says work autonomously and still push high-risk findings; risk is flagged as money in final output. |
| Pre-execution checklist | Read CLAUDE.md/AGENTS.md; extract S1-029 row; verify current code premise; claim coordination lock; write failing test first; implement minimal billing worker and wiring; regenerate sqlc for changed query; run targeted tests, build, self-review, commit, push. |

Concrete execution order:
1. Add a failing unit test in `backend/internal/billing` for a pending no-usage worker path, plus an `integration_pg` predicate/result test for real eligible and ineligible `usage_records` row shapes.
2. Implement a small billing worker/store in a new non-frozen billing file.
3. Wire the worker into `cmd/gateway` startup/shutdown.
4. Update `observability.sql` pending-only filters to exclude rows with the worker finalization marker and regenerate sqlc output.
5. Run focused package tests, `go build ./...`, self-review, then commit and push.
