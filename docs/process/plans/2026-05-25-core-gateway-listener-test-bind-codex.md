# 2026-05-25 core_gateway listener test bind remediation - Codex

| Owner directive | "core_gateway listener_test 12 用例 sandbox bind permission denied ... 区分必须 TCP bind 验证的真实集成测试与 core listener logic 可以 mock 的测试" |
| Scope | In: analyze `core_gateway/tests/listener_test.rs`, convert listener tests that can exercise router/proxy behavior with an in-memory upstream seam, mark true TCP listener/socket behavior tests as ignored for bind-allowed environments, preserve discriminating assertions, run requested cargo command and at most two Codex review rounds. Out: git add/commit/push, production deployment, dependency/schema/auth/billing/quota changes, non-HUAKAI reference source. |
| Success criteria | Sandbox run of `cd exploratory/rust-core-gateway/merged && cargo test -p core_gateway -- listener 2>&1 | tail -30` no longer fails on bind denial; full listener test binary has deterministic pass/ignored split; every converted in-memory test still asserts concrete status/body/header/counter/timing behavior that would fail under a plausible mutation. |
| Time estimate | 30-60 minutes wall clock; one Codex implementation session. |
| Blast radius | Existing `core_gateway` listener/proxy test harness and any small test-only seam needed in existing core files; no new runtime dependency and no frozen-package new files. |
| Failure modes | In-memory seam becomes too broad and hides proxy behavior; mitigation: keep it trait-shaped and assert requests/counters/body/header semantics. Over-ignoring shrinks coverage; mitigation: only ignore tests whose purpose depends on live TCP listener, client socket behavior, or hyper server timeout behavior. Requested filtered command may not cover all 12 tests; mitigation: also run `--test listener_test` for full pass/ignored count. |
| Decision points | Stop for Owner confirmation if a high-risk file is required, if a new dependency is needed, or if fixing requires schema/auth/billing/quota changes. |
| Pre-execution checklist | 1. Reproduce current bind failure. 2. Read all 12 listener tests and prior attempt_reporter in-memory/ignore pattern. 3. Classify each test. 4. Patch smallest existing files only. 5. Run targeted/full listener tests. 6. Run mutation-oriented self-checks for converted tests. 7. Run up to two Codex review rounds without staging. |
| Concrete execution order | Classify tests, add TCP bind preflight + ignore attributes for live-socket cases, add or reuse an in-memory upstream seam for router-level cases, update assertions to remain discriminating, verify, review, report Chinese Owner summary plus clean-room provenance. |
