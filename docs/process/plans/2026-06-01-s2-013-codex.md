# 2026-06-01 S2-013 Codex Plan

| Field | Value |
| --- | --- |
| Owner directive | "Fix audit finding S2-013 to PRODUCTION QUALITY." |
| Scope | Add a small authenticated OpenAI-compatible `GET /v1/models` backend surface backed by HUAKAI's existing model registry. Update OpenAPI and tests. No schema migration, no billing/quota/auth-core changes, no frozen-package new files. |
| Out of scope | Admin model writer UI/API, model-cache TTL, upstream model sync, broad provider catalog defaults. Custom display-list configuration is not dropped; disposition is Mandatory Roadmap for the future model-admin/catalog slice. |
| Success criteria | `/v1/models` is registered, bearer-authenticated with the existing inbound API-key resolver, returns `{"object":"list","data":[...]}` with OpenAI model objects for models the tenant can actually route, and fails closed on auth/registry errors. Tests fail if the route or response contract is removed. |
| Time estimate | 1-2 wall-clock hours; one Codex implementation lane. |
| Blast radius | Client-facing gateway route table, registry read path, OpenAPI path set. No writes except existing auth last-used telemetry. |
| Failure modes | Over-listing unroutable models; exposing another tenant's/global model without explicit inheritance; breaking OpenAPI route consistency; copying reference implementation shape too closely; weak tests that pass with the route removed. Mitigation: query only active visible aliases with enabled bindings, keep implementation local and paraphrased, add route and handler tests, run mutation. |
| Decision points | None requiring Owner confirmation: this is a medium-risk additive compatibility endpoint, no high-risk files, no new dependency, no schema change. |
| Pre-execution checklist | Read CLAUDE.md / AGENTS.md / docs/RULES.md; read cross-module invariants; read `routes.go`, registry package, model-registry migration, OpenAPI consistency test; under the direct Owner source-must-read dispatch, read reference source only for behavior evidence; claim files in `.coordination`; write failing tests before production code. |
| Deferred parity disposition | Sub2API-style operator-curated display-list filtering is Mandatory Roadmap, not `Out of Scope`. This patch implements the base authenticated discovery surface; the future model-admin/catalog slice should decide the admin contract, audit trail, and whether display filtering is tenant, key, group, or route scoped in HUAKAI terms. |
| Concrete execution order | 1. Add RED route/handler tests. 2. Add registry list query and model HTTP handler. 3. Mount `GET /v1/models`. 4. Update OpenAPI. 5. Run focused tests, mutation, build, Codex review. 6. Commit and push. |

Reference note: Sub2API's current default branch exposes a gateway `/v1/models` list response and treats group-level model-list customization as display-only, not dispatch policy. Evidence read: `Wei-Shaw/sub2api@aa69e3947dac0282c5973bc3a51fadf058bbc9ca:backend/internal/handler/gateway_handler.go:971` and `Wei-Shaw/sub2api@aa69e3947dac0282c5973bc3a51fadf058bbc9ca:backend/migrations/143_group_models_list_config.sql:1`.

Clean-room note: reference source was read only for behavior evidence. This implementation will use HUAKAI's existing registry schema and module boundaries, not upstream names, structure, comments, schemas, or code.

Authority note: this worker dispatch explicitly required source reading for reference-equivalent concerns and stated that reading is required while copying is the violation. No Option-C carve-out area is touched: the patch is a client discovery endpoint, not account-pool routing, auth core, billing ledger, quota enforcement, or schema migration.

Source files read: backend/internal/handler/gateway_handler.go; backend/migrations/143_group_models_list_config.sql; backend/internal/domain/models_list_config.go; backend/internal/handler/gateway_models_test.go
Lane: specifier
Agent: GPT-5 Codex small-safe-patch S2-013
UTC timestamp: 2026-06-01T15:32:33Z
