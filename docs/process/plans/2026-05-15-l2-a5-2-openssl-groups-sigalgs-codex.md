# 2026-05-15 L2-A5.2 OpenSSL supported_groups and signature_algorithms

| Owner directive | "L2-A5.2: OpenSSL profile-driven supported_groups + signature_algorithms injection" |
| Scope | In: HUAKAI internal Rust OpenSSL adapter, existing mimicry OpenSSL tests, codex-cli template read, local OpenSSL CLI probes. Out: non-HUAKAI reference projects, auth/billing/quota/database/deployment, custom extension injection for 4588 beyond public OpenSSL APIs. |
| Success criteria | `new_with_profile` applies cipher suites, ALPN, supported groups, and signature algorithms in order; unsupported groups/sigalgs fail explicitly; tests prove either 4588 is expressible or that the adapter reports `UnsupportedGroup(4588)` before using a 4588-stripped variant; signature algorithms reach `OrderedMatch` or fail explicitly with `UnsupportedSigalg`. |
| Time estimate | 45-75 minutes wall clock, mostly implementation plus feature/default test runs. |
| Blast radius | Limited to the feature-gated OpenSSL mimicry adapter and its feature-gated tests. Default builds should remain unaffected. |
| Failure modes | OpenSSL/rust-openssl names differ from IANA labels; mitigate by probing local group list commands (`-groups` if available, otherwise `-tls-groups`/`-all-tls-groups`), using public rust-openssl APIs, and letting unsupported IDs surface as typed errors. Captured ordering may include OpenSSL defaults; mitigate by asserting actual diff and recording output. Local OpenSSL may not support PQ group 4588; mitigate with explicit `UnsupportedGroup(4588)` path and a 4588-stripped test variant. |
| Decision points | No Owner sign-off expected unless implementation requires custom extension injection, a new runtime dependency, schema/auth/billing/quota changes, or reading prohibited reference projects. |
| Pre-execution checklist | 1. Read `docs/RULES.md`, current adapter, profile loader, capture/diff helpers, and codex-cli template. 2. Probe local OpenSSL group support. 3. Confirm rust-openssl exposes `set_groups_list` and `set_sigalgs_list` in the installed crate/docs. 4. Implement typed error variants and mapping helpers. 5. Add capture/diff test. 6. Run requested build/tests and `git diff --check`. |
| Concrete execution order | Probe local OpenSSL and rust-openssl API surface; patch adapter mappings and call order; patch feature-gated test including 4588 fallback profile; run focused feature test with `-- --nocapture` to capture `eprintln`; run requested build/test/default-test/diff-check; summarize counts and risks in Chinese. |
