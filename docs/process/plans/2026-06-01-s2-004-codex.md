# 2026-06-01 S2-004 Codex Plan

| Owner directive | "Fix audit finding S2-004 to PRODUCTION QUALITY." |
| Scope | Fix the production trust-ledger escape flag risk for money-path request completion events. In scope: existing `eventbus` validator, eventbus config loading, focused Go tests, and release-gate wording. Out of scope: billing schema, ledger schema, durable reconciliation table, new runtime dependencies, and broad gateway refactors. |
| Success criteria | Production missing-ref events are rejected even if `AllowMissingMoneyRef` is true; production config fails fast when `HUAKAI_TRUST_LEDGER_ALLOW_MISSING_MONEY_REF=true`; valid persisted refs and DLQ refs still pass; non-production behavior remains unchanged; mutation check proves the new test turns red if the production guard is reverted. |
| Time estimate | 45-75 minutes wall clock; one focused Codex work unit. |
| Blast radius | Eventbus request-completion validation and gateway direct-settle fallback tests. If wrong, money-path settlement could be over-blocked or the production escape could remain open. |
| Failure modes | A production flag remains effective through a manually built policy; mitigation: validator-level production rejection. Config accepts unsafe production env; mitigation: config-level fail-fast test. Existing dev/test fixtures break; mitigation: retain non-production exemption and test it. |
| Decision points | No Owner sign-off needed unless the fix requires schema changes, billing ledger changes, new dependencies, deleting files, or changing `LICENSE`; this plan avoids all high-risk actions. |
| Pre-execution checklist | 1. Read `CLAUDE.md` and `AGENTS.md`. 2. Read audit row and cited files. 3. Read cross-module boundary invariants because the finding touches gateway/billing/eventbus. 4. Verify premise at current HEAD from `backend/` module root. 5. Claim edit paths via `.coordination/`. 6. Write failing tests before production-code edits. |

## Concrete Execution Order

1. Add a failing config test proving production mode plus `HUAKAI_TRUST_LEDGER_ALLOW_MISSING_MONEY_REF=true` returns a typed error and does not produce a usable `EventBusConfig`.
2. Add or invert an eventbus validator test proving a production `AuditRefPolicy{AllowMissingMoneyRef:true}` still rejects a request-completion event with no audit ledger ID/fingerprint and no DLQ ref.
3. Update the gateway direct-settle test that currently expects bypass so it expects rejection/no settlement under the same production escape-policy fixture.
4. Run the focused tests and confirm they fail for the intended reason.
5. Implement the minimal production guard in `eventbus.ValidateMoneyPathAuditRef`.
6. Implement the minimal config fail-fast check in `runtimeconfig.LoadEventBus`.
7. Update release-gate wording to say this is now enforced by startup validation and the release gate remains a deployment check.
8. Run focused tests, perform the required mutation check by temporarily reverting the production guard, restore the fix, then run full relevant package tests and `go build ./...` from `backend/`.
