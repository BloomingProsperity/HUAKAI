# 2026-05-15 R-3 R-D 端到端验真 Claude 平行计划

| Lane | Claude（与 codex α 平行；非交流前独立产出） |
| Owner directive | R-3 R-D = 端到端验真,把 R-C 拿到的 ClientHello/extension subset 真接到 transport pipeline,在与上游 mock + 真上游 (4 vendor 内) 之间验真。 |
| Scope | In: end-to-end test harness 设计;wire-level capture@adapter-exit vs template diff;real-upstream gate (Owner 本机授权才跑);R-C closure 后的回归套件;feature flag scaffold (production dispatch off)。Out: production dispatch wiring (R-E);R-SEC-002 mTLS (R-E 前置);新增 vendor template;新引入 transport dep。 |
| Success criteria | mock 上游路径 4 cargo combo PASS;真上游 (Owner 本机, anthropic/openai/gemini/codex 之一) ClientHello 抓出来与 builtin profile 精确逐字段 match;feature flag default OFF;dispatch 仍走 hyper-rustls fallback;新 atom 文件改动量 < 600 LoC。 |
| Time estimate | 第一 atom (harness scaffold + mock 上游 e2e) ≤ 2-3 小时;真上游回放需 Owner 本机才能跑,Claude 无 sandbox 凭据。 |
| Blast radius | Medium. transport pipeline 单点 (mimicry::dispatch),feature flag 默认 off。但 harness 改 ProxyEngine 接线时若 fallback 路径回归,会影响 Go 控制面 control-plane probe — 必加 hyper-rustls fallback 回归。 |
| Failure modes | 1. mock 上游无法复现真实 SETTINGS / pseudo header 顺序;2. cargo target 26G 与并行 lane 抢锁 (用 CARGO_TARGET_DIR=/tmp/cargo-target-r3rd 隔离);3. Owner 本机 e2e 一时无法跑,真上游 leg 阻塞。 |
| Decision points | (D1) 第一 atom 是 scaffold + mock 路径,还是直接做 wire diff harness?  (D2) feature flag 名字 = `mimicry-prod-dispatch` 还是 `r3-rd-shadow`?  (D3) hyper-rustls fallback 触发条件:adapter PreflightFailed,还是任何 adapter init err?  (D4) 真上游 leg 是否同 atom 同 PR,还是单独 follow-up? |
| Concrete first atom (Claude 推荐) | 1. 加 `mimicry::dispatch::DispatchFlavor` 枚举:`Native`(走 hyper-rustls 现状) / `Mimicry`(走 L2-A5 系列 adapter)。2. ProxyEngine 配置加 `dispatch_mode: DispatchMode`,默认 `Native`。3. e2e test harness `tests/r3_rd_dispatch_shadow_test.rs`:启 mock 上游,创建 ProxyEngine `dispatch_mode=Mimicry`,assert ClientHello capture vs 4 builtin profile (codex/kiro/gemini-advanced/anthropic-claude-code-pending) 各跑一遍 `diff_capture_against_profile`,Subset/Match/ordered 三类 status 全覆盖。4. 仍保留 `dispatch_mode=Native` 的 baseline 测试不变,保证 fallback 完整。 |
| 备选 atom | 如果 codex α 选择更深的 atom (例如直接对接 R-E shadow),Claude 立刻让步,做更窄的 review 平行(查 fallback 路径未回归)。 |
| Claude 与 codex α 可能分歧点 | (a) 选哪个 atom 作"第一可执行";(b) feature flag 暴露给运维的 surface (env / config / metric);(c) 是否在本 atom 加 `wire_extension_extras` 的 ops surface (Lane F follow-up MED1 提到的);(d) 测试是 sync 还是 tokio runtime。 |
| Synthesis 期望 | codex α 实施后,Claude 看 staged diff + cargo result,拒绝任何把 fallback 路径默认关闭、或把真上游 leg 强行塞进同 PR 的实施;接受任何把 wire diff harness 写干净、feature flag 严格 default-off 的实施。 |

## Cross-discuss 备忘

- 与 codex α: 看 codex 选的 atom 与 Claude 推荐对齐度 → 若 codex 选 D1 (scaffold + mock),直接合并;若选 wire diff 直接 against 真上游,要 Owner 本机授权才能跑,需先 surface。
- 与 codex β: R-E plan 应引用本文件做 "R-D 已闭" 的前置 (D3 fallback 触发条件 + D4 真上游 leg 时机)。
- 与 Lane γ: R-SEC-002 不在 R-D scope,R-D 期间 transport 仍是本地 mock,无凭据传输。明确 R-SEC-002 是 R-E 前置而非 R-D 阻塞。

## 严禁

- Production dispatch wiring (R-E)。
- 新 vendor template (Lane 外 work)。
- 新 dep。
- 改 backend/。

## Source

Read: `docs/process/plans/2026-05-14-r3-on-merged-closure-codex.md` (§3 Phase R-D),`docs/10_RISK_REGISTER.md` (R-SEC-002 / R-TRANSPORT-001 / R-LIC-003),`exploratory/rust-core-gateway/merged/crates/core_gateway/src/mimicry/{dispatch,openssl_adapter,http2_adapter}.rs` 已 commit 状态,`docs/process/reviews/2026-05-15-l2-a5-5-codex-review.md` (extras getter 现状)。

Lane: Claude parallel  
Agent: Claude Opus 4.7 (1M context)  
UTC timestamp: 2026-05-15T12:06:00Z
