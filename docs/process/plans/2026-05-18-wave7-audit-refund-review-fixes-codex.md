# 2026-05-18 wave7 audit refund review fixes
| Owner directive | "修 wave 7 codex review: 1 P1 (receipt_sequence verify) + 2 P2 (refund worker idempotent)." |
| Scope | In: audit receipt DTO/canonical mapping, refund worker duplicate retry handling, receipt storage sequence lookup, gateway receipt tests, audit worker tests, OpenAPI receipt schemas. Out: frontend, Rust, vendor/boring, reference project source, new runtime dependencies, schema changes. |
| Success criteria | Refunded receipt GET response includes receipt_sequence; detached verify recomputes the same canonical hash for sequence 1 receipts; refund worker treats duplicate refund ledger and duplicate refund receipt retries as idempotent success after reading existing facts; requested build and tests pass. |
| Time estimate | 60-90 minutes wall clock; one Codex implementer lane work unit. |
| Blast radius | User-facing receipt JSON schema, detached receipt verification, refund DLQ retry behavior, receipt storage read helpers. |
| Failure modes | Missing sequence in one canonical path could keep refunded verify broken; over-broad duplicate handling could mask unrelated storage errors; test stubs could pass while production wrappers cannot read existing receipts. Mitigation: add explicit AT tests for sequence verify and both duplicate retry paths, and wire production refundReceiptSink to sequence lookup. |
| Decision points | No high-risk Owner decision expected. Stop only if changes require schema migration, auth/billing ledger logic, frontend/Rust/vendor edits, or new dependencies. |
| Pre-execution checklist | Read project rules; read receipt formatter/user DTO/canonical code; read refund worker duplicate paths; read ledger duplicate behavior; read storage receipt sequence support; read OpenAPI receipt schemas; inspect existing tests and dirty worktree. |
| Concrete execution order | Add receipt_sequence to UserCostReceipt, canonical payload, audit receipt reconstruction, verify response, and OpenAPI; add receipt storage sequence lookup; make refund worker recognize pg unique/request duplicate and fetch existing ledger row; make refund receipt duplicate fetch existing sequence 1 receipt and continue; add AT_AUDIT_001_032/033/034 tests; run gofmt, build, and requested tests. |
