# 2026-06-03 models capabilities codex plan

| Field | Plan |
| --- | --- |
| Owner directive | "目标(Task① 回扫升级 FU):富化 GET /v1/models —— 现在只有 pricing + context_length;加模型能力矩阵 + 输出上限 + 模式...实现+验证。先读 CLAUDE.md + AGENTS.md。" |
| Scope | Add additive `models` columns for public capability descriptors, project them through registry list output, expose optional `/v1/models` fields, add focused platform-admin write endpoint, update OpenAPI, and add discriminating tests. |
| Out of scope | No external reference source reads, no `/home/ubuntu/refs`, no pricing/billing/quota/auth-core logic changes, no real secrets, no feature comparison claims about non-HUAKAI projects, no new files in frozen `internal/{gatewayhttp,gateway,proto}`. |
| Clean-room / reference scope | Codex implementer scope is HUAKAI-local code only. The Owner supplied the PM scheme and explicitly forbade external source reads for this task; this overrides the default FU source-reading rule for Codex execution. The fields are standard OpenAI-style public descriptors, not copied code/data. |
| Files / packages | Create migration files under `backend/sql/migrations/` (not a Go package). Modify `backend/sql/queries/registry.sql` and generated `backend/internal/db/registry/*` if sqlc is available. Modify existing `backend/internal/registry/models_list.go` and tests; add `backend/internal/registry/model_capabilities.go` and tests (package has 11 Go files, not frozen). Modify existing `backend/internal/modelhttp/list_handler.go` and tests; add `backend/internal/modelhttp/admin_capabilities_handler.go` and tests (package has 2 Go files, not frozen). Modify existing `backend/cmd/gateway/routes.go` and `openapi_consistency_test.go`; update `docs/openapi/openapi.yaml`. |
| Success criteria | A model with `capabilities`, `max_output_tokens`, and `model_mode` returns `capabilities`, `max_output_tokens`, and `mode` from `GET /v1/models`; a model without them omits all three fields; `PUT /v1/admin/models/{id}/capabilities` persists the public descriptors; OpenAPI and runtime routes stay in sync; requested gate commands pass or blockers are reported truthfully. |
| Time estimate | 2-3 hours wall clock for TDD, implementation, sqlc generation or manual generated updates, and verification. |
| Blast radius | Additive nullable/default schema change and model discovery/admin model metadata surfaces. Runtime read path remains metadata-only; admin write is platform-admin-gated and only updates public descriptor columns. |
| Failure modes | Empty maps leaking as `{}` and breaking compatibility; forgetting one UNION arm so global models lose fields; dropping canonical/context projection; accepting invalid admin JSON; route/OpenAPI drift; sqlc unavailable; concurrent edit overwrite. Mitigation: discriminating tests, explicit UNION string tests, admin handler tests, OpenAPI sync test, coordination lock, and final gate. |
| Decision points | Owner has already confirmed the high-risk schema migration shape in this directive. Stop only if adding a runtime dependency, touching auth/billing/quota core, changing non-additive schema semantics, or reading external source becomes necessary. |
| Pre-execution checklist | Read `CLAUDE.md`, `AGENTS.md`, `docs/RULES.md`, `.coordination/README.md`; check and claim edit lock; inspect current model list, registry, admin route, sqlc, migration, OpenAPI, and test patterns; write failing tests before production code; run targeted red/green tests and final gate. |
| Concrete execution order | 1. Add list-handler and registry projection tests that are red before implementation. 2. Add admin handler/store tests, including persistence-shape and route/OpenAPI sync assertions. 3. Run targeted tests to capture expected RED. 4. Add migration 0086, registry projection fields, admin persistence helper, list response omitempty logic, admin route wiring, and OpenAPI schema/path updates. 5. Run sqlc generation when available, targeted tests, then the requested build/vet/test gate. 6. Release coordination lock and report file:line, compatibility proof, mutation evidence, gate, risks, blockers in Chinese. |
