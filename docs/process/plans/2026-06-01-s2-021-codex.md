# 2026-06-01 S2-021 Codex Security Log Redaction Plan

| Owner directive | "TASK S2-021 (audit: confirmed; risk: SECURITY). Owner has chosen the fix approach." |
| Scope | In: redact raw upstream stream error payloads before internal logging, update existing discriminating tests, add a focused helper in non-frozen `backend/internal/redact`, verify build/test/review, commit and push `work/s2-021`. Out: schema, auth, billing, quota, `LICENSE`, new runtime dependencies, new files in frozen packages. |
| Success criteria | Internal logs include payload byte length, a short non-reversible SHA-256 prefix over the redacted summary, and a capped snippet with obvious secret markers redacted; raw sensitive markers and raw-payload hashes do not appear in logs; client stream error remains canonical and unchanged; restoring raw logging makes the updated tests fail. |
| Time estimate | 30-60 minutes wall clock; one Codex worker session. |
| Blast radius | `backend/internal/gateway` streaming error handling and tests. Package is frozen, so only existing gateway files may be edited. |
| Failure modes | Raw payload or credential marker still reaches `slog`; test fixture remains non-discriminating; Bedrock scanner error string still embeds raw payload; helper over-redacts client-visible canonical error path; package structure rule violated by adding a gateway file. Mitigation: test first, mutate-check by restoring raw logging, run targeted gateway tests and full backend build. |
| Decision points | None expected: Owner already chose truncate + redact. Stop only if implementation requires high-risk files, new dependency, schema/auth/billing/quota changes, or a frozen-package new file. |
| Pre-execution checklist | 1. Confirm branch/worktree. 2. Check and claim coordination locks. 3. Read `forwarder.go`, `bedrock_stream_scanner.go`, and existing tests. 4. Write failing test inversion before production edits. 5. Implement minimal redaction in existing files. 6. Run red/green and mutation checks. 7. Run `go build ./...`, `go test ./internal/gateway/...`, and best-effort Codex review. 8. Stage, commit, push `HEAD:work/s2-021`. |

## Concrete Execution Order

- [x] Update existing tests in `backend/internal/gateway/bedrock_e2e_test.go` and `backend/internal/gateway/forwarder_clientadapter_test.go` so sensitive markers must be absent from logs while summary fields remain present.
- [x] Run targeted tests and confirm they fail against current raw logging.
- [x] Add a minimal safe payload summary helper in `backend/internal/redact/payload_summary.go` (non-frozen): byte length, SHA-256 prefix over the redacted summary, and a bounded capped redacted snippet.
- [x] Add direct helper tests in `backend/internal/redact/payload_summary_test.go` proving ordinary `message`/`content` values, arbitrary JSON keys, and non-JSON bodies are redacted.
- [x] Update `handleEventWithAdapter` to log the summary instead of raw `evt.Data`.
- [x] Update existing `backend/internal/gateway/bedrock_stream_scanner.go` error construction so it no longer formats raw payload bytes into the scanner error.
- [x] Run targeted tests and confirm green.
- [x] Perform mutation check by temporarily restoring raw logging and confirming the test goes red, then restore the fix.
- [x] Run required verification and self-review, commit, and push.

## File Scope

- Modify: `backend/internal/gateway/forwarder.go` (existing frozen-package file; no new gateway file).
- Modify: `backend/internal/gateway/bedrock_stream_scanner.go` (existing frozen-package file).
- Modify: `backend/internal/gateway/bedrock_e2e_test.go` (existing frozen-package test).
- Modify: `backend/internal/gateway/forwarder_clientadapter_test.go` (existing frozen-package test, stale sibling assertion).
- Create: `backend/internal/redact/payload_summary.go` (non-frozen package; focused helper only).
- Create: `backend/internal/redact/payload_summary_test.go` (non-frozen package test).

## Self-Review Notes

- Round 1/2 review finding normalized S1: snippet/log summary still exposed payload values or key fingerprints. Resolution: redact string/number/bool JSON values, replace unsafe JSON keys with ordinal placeholders, and do not hash raw JSON keys.
- Round 3 review finding normalized S1: raw-payload SHA prefix would become a content fingerprint/search key. Resolution: keep Owner-required hash field, but compute it over the already-redacted summary instead of raw upstream bytes.
- Round 3 review finding normalized S1: JSON redaction did full parse/marshal before capping. Resolution: bound inspection to 4096 bytes and emit a redacted oversized-payload placeholder beyond that limit.
