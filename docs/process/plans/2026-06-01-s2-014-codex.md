# 2026-06-01 S2-014 Codex Plan

| Owner directive | "Fix audit finding S2-014 to PRODUCTION QUALITY." |
| Scope | In: HUAKAI runtime wiring for provider-account per-model cooldown state: record upstream 404 as model-scoped cooldown, persist audit evidence, and exclude active model cooldown entries during pool selection. Out: full F-RATE-001 multi-platform 429/403/OAuth decision tree, schema migrations, non-MIT source reads, UI changes. |
| Success criteria | Current code premise is verified; reference approach is lane-correctly cited; a discriminating Go test fails before implementation and passes after; mutation run proves the test turns red when the gate is reverted; focused package tests and `go build ./...` pass; Codex review has no unresolved S0/S1; commit is pushed to `work/s2-014`. |
| Time estimate | 1-2 wall-clock hours; one Codex implementation session. |
| Blast radius | Gateway chat failure handling, pool account eligibility, generated billing SQL accessors, and provider account cooldown audit writes. No schema, auth, quota, billing ledger, payment, secrets, or deployment changes. |
| Failure modes | Over-broad 404 handling could suppress healthy accounts; mitigate by scoping to the selected provider account and current upstream model key with bounded default cooldown. Corrupt `model_rate_limits` JSON could break selection; mitigate by tolerantly parsing only valid timestamp entries. Generated sqlc drift could break build; mitigate by regenerating and running build/tests. |
| Decision points | None for Owner during this autonomous S2 patch. If a full multi-platform `rate.Service.HandleUpstreamError` implementation becomes necessary, stop as a large feature gap; this plan implements the narrow S2-014 production path only. |
| Pre-execution checklist | 1. Read `CLAUDE.md`, `AGENTS.md`, `docs/RULES.md`, and CMB invariants. 2. Load audit row. 3. Trace HUAKAI runtime: classifier, handler, selector, DB account source, schema, admin clear. 4. Use only MIT-anchor source or source-derived decompositions for references. 5. Claim edit files via `.coordination`. 6. Add failing tests first. 7. Implement minimal runtime wiring. 8. Run mutation and verification. 9. Stage, review, commit, push. |
| Target packages | New files only in non-frozen `backend/internal/rate`; existing-file edits in frozen `backend/internal/gatewayhttp`; no new files in `backend/internal/{gatewayhttp,gateway,proto}`. |

## Concrete Execution Order

1. Add RED tests for model cooldown selection and model cooldown persistence parameterization.
2. Run focused tests and confirm RED.
3. Implement `rate.ModelCooldownService` and sqlc query for `model_rate_limits` update + `rate_limit_audit_events` append.
4. Add selector model-cooldown key and gate; wire DB JSON into `AccountSnapshot`.
5. Wire gateway handlers to record model cooldown on upstream 404 using the current upstream model key.
6. Run sqlc generation, gofmt, focused tests, mutation check, `go build ./...`, and relevant package tests.
7. Run required uncommitted Codex review, fix S0/S1 only, then commit and push.
