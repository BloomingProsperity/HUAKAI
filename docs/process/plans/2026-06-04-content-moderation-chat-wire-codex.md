# 2026-06-04 Content Moderation Chat Wire Codex Plan

| Field | Plan |
| --- | --- |
| Owner directive | "把 screener 接入 chat 请求路径,拦截违规输入 + auto-ban...默认关闭(opt-in)...本切片不做违规费...严禁读 /home/ubuntu/refs 等外部参考源码。只读 HUAKAI 自有码。" |
| Scope | In: wire `internal/moderation.Screener` into chat before billing reserve, add auto-ban call using authenticated identity, harden keyword matching, add discriminating tests, wire `cmd/gateway` dependencies behind a default-off runtime env gate. Out: billing/violation fee, schema changes, new runtime dependencies, external reference source reads, git commit. |
| Clean-room lane | Codex implementer lane. Reference source is explicitly out of scope by Owner dispatch and `docs/RULES.md` CR-R-001. No `/home/ubuntu/refs` reads; no reference-project behavior claims. |
| Files | New: this plan only, under `docs/process/plans` (not a frozen package). Modify existing only: `backend/internal/gatewayhttp/chat_completions_handler.go`, `backend/internal/gatewayhttp/chat_completions_handler_test.go`, `backend/internal/moderation/screener.go`, `backend/internal/moderation/screener_test.go`, `backend/internal/moderation/doc.go`, `backend/internal/clienterr/catalog.go`, `backend/internal/clienterr/catalog_test.go`, `backend/cmd/gateway/routes.go`, `backend/cmd/gateway/routes_moderation.go`. No new files in frozen `gatewayhttp`. |
| Success criteria | Runtime moderation gate off or no screener means no screen call and existing behavior path remains unchanged; runtime gate on + enabled tenant + blocked input returns 403 `content_policy_violation` before `ClaimGate.Reserve` and before dispatch; clean input proceeds; screener backend error blocks by default and fail-open config passes; keyword test catches `ass` vs `passage` and zero-width/fullwidth normalization; auto-ban uses `auth.Identity.APIKeyID`, not body fields. |
| Time estimate | 2-3 hours wall clock in one Codex session, mostly tests + gate. |
| Blast radius | Chat hot path for `/v1/chat/completions`, `/v1/messages`, `/v1/responses`; moderation keyword matching; gateway runtime dependency wiring. |
| Failure modes | Hook after reserve would bill/block late: guard with reserve/dispatch call-count test. Default-on regression would call screener or block without config: guard with nil-screener/default test. Raw-content leak in logs/storage: only pass body in memory and rely on moderation audit hash metadata; no logging of body. API-key spoofing: guard by forging body `api_key_id` while asserting ban event APIKeyID equals auth identity. Over-broad keyword matching: guard `ass` not matching `passage`. Unicode bypass remains possible beyond NFKC/zero-width/token boundaries: document residual limitation. |
| Decision points | None requiring new Owner confirmation unless implementation would need schema/auth/billing/quota/dependency changes. Current approach avoids those high-risk areas. |
| Pre-execution checklist | 1. Read `CLAUDE.md`, `AGENTS.md`, `docs/RULES.md`, coordination rules, and HUAKAI moderation/chat code. 2. Claim files in `.coordination`. 3. Write failing moderation keyword tests. 4. Write failing chat-path tests for block/pass/fail-closed/default-off/APIKeyID. 5. Implement minimal code. 6. Run focused tests after each red/green step. 7. Run required gate command. 8. Release coordination lock. |
| Concrete execution order | TDD first: add failing `moderation` keyword tests; run them and record expected red. Implement normalization/token matching. Run `go test ./internal/moderation`. Add failing `gatewayhttp` moderation tests; run target tests and record expected red. Add `ChatHandlerDeps.ModerationScreener`, `ScreenerDeps.Ban`, pre-reserve hook, safe 403 error, and cmd wiring. Run focused packages, then full gate. |
