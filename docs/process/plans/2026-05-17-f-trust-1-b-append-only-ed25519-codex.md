# 2026-05-17 F-TRUST-1-B append-only ed25519
| Owner directive | "你是 HUAKAI codex executor lane, 任务 = F-TRUST-1-B append-only trigger + ed25519 KMS integration (Wave 2 of plan velvety-exploring-lake)." |
| Scope | In: HUAKAI-only Go backend `internal/auditledger`, migration `0027`, acceptance matrix rows for AT-TRUST-001-001..006/008, and local tests. Out: frontend, Rust, LICENSE, billing core, spec wave, anti-ban implementation, F-PAY, F-COMM, reference project source. |
| Success criteria | Append-only trigger migration exists; local ed25519 signer supports env/KeyProvider material and pubkey rotation verify; canonical payload/hash is deterministic and cross-language reproducible; Postgres writer signs canonical entry hash in transaction and uses per-tenant advisory locking; AT rows 001..006 and 008 have passing evidence where implemented; requested Go checks are run or blockers recorded. |
| Time estimate | 1 work session; owner estimate 4-5 engineer days compressed into scoped executor patch, with implementation focused on existing skeleton and tests. |
| Blast radius | `auditledger` signing/hash semantics and PG ledger insert path; migration only adds trigger function/triggers to `audit_ledger_entries`. |
| Failure modes | Signing the wrong bytes: mitigate by replacing legacy hash input with canonical payload hash and adding verify/tamper tests. JSON instability: mitigate by manual canonical JSON sorting and Go/Python comparison test. Writer race: mitigate by tenant-scoped advisory lock and 100-entry continuity test. Trigger blocking tests/truncation: integration tests should use INSERT plus rollback/cleanup patterns and avoid UPDATE/DELETE outside expected assertions. |
| Decision points | None expected. High-risk areas from Owner directive remain untouched. If a new dependency, schema rewrite, billing/auth/quota core change, or frontend change becomes necessary, stop for Owner confirmation instead of proceeding. |
| Pre-execution checklist | Read `docs/RULES.md`; read F-TRUST spec §2/§6 and AT outline; read existing `auditledger` writer/tests/types; read F-AUTH-005 `credentialstore.KeyProvider` pattern; confirm migration next number is 0027; confirm git worktree state before editing. |
| Concrete execution order | Add migration trigger; add signer and canonical payload/hash helpers; update memory/Postgres writer to use canonical hash and tenant advisory lock; add/adjust unit and integration tests; update AT matrix rows; run requested package test, build, and FAIL scan. |
