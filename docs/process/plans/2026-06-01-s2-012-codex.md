# 2026-06-01 S2-012 Registration Policy And Invitation Binding - Codex Plan

| Field | Value |
| --- | --- |
| Owner directive | "Fix audit finding S2-012 to PRODUCTION QUALITY." |
| Scope | In: read HUAKAI auth/registration/invitation source; read source-verified reference behavior; add explicit registration policy defaults; connect community invitation redemption into the existing registration transaction; add discriminating tests and mutation check. Out: new schema migrations, reward issuance, billing ledger mutation, quota enforcement, frontend UI, production deployment. |
| Success criteria | Production no longer defaults to open public registration; explicit open/invite/disabled policy is validated; `/v1/invitations` codes can be redeemed by registration without losing invite/referral state; tests fail when the new gate or binding call is removed; targeted tests and `go build ./...` are run and reported truthfully. |
| Time estimate | 1-2 hours wall clock; one Codex implementation/review loop. |
| Blast radius | Auth registration, social signup, userauth Postgres redemption, community invitation usage/referral rows, and gateway startup configuration. No gateway routing, billing, quota, credential acquisition, or schema files are intended. |
| Failure modes | Policy parser could silently allow production open registration; mitigate with env tests. Invitation redemption could consume a code before user creation fails; mitigate by using the existing userauth transaction and rollback test. Community invitation usage could diverge from auth invite usage; mitigate by updating both in the same DB transaction. Referral insert could break registration; mitigate with scoped pending insert and duplicate-safe conflict handling. |
| Decision points | No Owner question is possible in this autonomous worker lane. If schema, billing, quota, or reward issuance becomes required, stop as blocked. |
| Pre-execution checklist | 1. Read audit row. 2. Trace routes, auth handler, userauth service/store, community invitation service/store, migrations, specs, and CMB invariant. 3. Read reference source at current fetched HEAD with clean-room paraphrase only. 4. Claim files through `.coordination`. 5. Write tests before production edits. 6. Run mutation check, targeted tests, build, Codex review, commit, and push. |
| Target packages | `backend/internal/userauth` existing files; `backend/internal/gatewayhttp` existing file only because frozen; `backend/cmd/gateway` existing files; no new files in frozen packages. |
| Risk | Auth-core risk: public signup behavior changes in production. No money/security schema mutation is planned, but the final result should be flagged as `auth` risk. |

## Execution Order

1. Add red tests for registration policy defaults, disabled mode, and community invitation referral binding.
2. Implement `RegistrationMode` with dev-open / production-disabled env default.
3. Extend userauth redemption so community invitation codes redeem inside the existing registration transaction and create pending referral linkage after user creation.
4. Map disabled-registration errors through the public auth handler.
5. Run RED/GREEN tests, mutation check by reverting the relevant guard, targeted package tests, `go build ./...`, and pre-commit review.
