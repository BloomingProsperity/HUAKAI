# 2026-06-06 Referral Reward Issuance Slice 1

| Owner directive | "Implement REFERRAL REWARD ISSUANCE — slice-1 (branch fix/referral-reward)." |
| Scope | In: migration 0100, payment-owned referral reward transaction, receipt-hook feature-flag wiring, platformsettings keys, invitation summary read surface, focused unit/integration tests. Out: withdrawals, voucher rewards, reading `/home/ubuntu/refs`, git commits, integration_pg execution in sandbox. |
| Success criteria | Enabled reward path credits the referrer at most once per referral, writes one `referral_rewards` fact, links the reward billing event, moves status to `rewarded`, and rolls back all reward state on credit failure. Disabled path keeps qualify-only behavior with no money mutation. Build, vet, and touched-package unit tests run locally; integration_pg tests are written but not run here. |
| Time estimate | 2-3 hours wall clock in this session; integration_pg and mutation verification deferred to PM because sandbox cannot run sockets/integration_pg. |
| Blast radius | Money tables (`payment_orders`, `payment_credits`, `billing_events`, `user_balances`), referral tables, receipt-settlement hook, platform settings, invitation API response surface. |
| Failure modes | Double credit under replay/concurrency: mitigate with `referral_rewards` unique constraint plus insert-before-credit gate. Partial reward row or status change after credit failure: mitigate with one serializable transaction and rollback tests. Disabled feature accidentally pays: add receipt-hook unit test and settings default false. Invalid settings paying wrong amount: validate non-negative cents and test exact configured amount. |
| Decision points | No Owner sign-off needed for requested low/medium-risk files. No `LICENSE`, real secrets, auth core, billing ledger schema beyond requested additive migration, quota enforcement, or destructive migration rollback changes. |
| Pre-execution checklist | 1. Confirm branch/worktree and no `/home/ubuntu/refs` reads. 2. Trace checkin money-credit transaction, referral qualification, receipt hook, settings, migrations, and invitation handler. 3. Write failing tests first for platformsettings, receipt-hook routing, payment reward transaction, migration 0100, and invitation summary. 4. Implement payment tx and config/wiring. 5. Run `gofmt`, build, vet, and touched-package unit tests; report that integration_pg was not executed. |
| Concrete execution order | 1. Replace invitation reward qualification with qualify-only and add summary store/service methods. 2. Add payment `ReferralRewardInput` / `ApplyReferralReward` with serializable retry transaction and injectable topup helper for rollback test. 3. Add 0100 migration. 4. Add platformsettings keys and validation. 5. Extend receipt hook with reward issuer and settings reader; wire payment service/settings in gateway middleware. 6. Extend invitation handler and route for user summary. 7. Verify. |

Notes:
- Existing `backend/internal/community/invitation/referral_reward_store.go` appears to be an older implementation that duplicates payment SQL in the invitation package. This slice will stop using it for receipt qualification; payment issuance moves to `backend/internal/payment/referral_reward.go`.
- Frozen packages: edit `backend/internal/gatewayhttp/invitation_handler.go` only; add no files under `backend/internal/gatewayhttp`, `backend/internal/gateway`, or `backend/internal/proto`.
