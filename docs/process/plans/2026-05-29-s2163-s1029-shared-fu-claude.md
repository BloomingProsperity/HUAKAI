# S2-163/S1-029 shared follow-up — 计划（Claude）

**作者**: Claude PM  **日期**: 2026-05-29  **基线**: fix/hermes e35b9ea
**来源**: docs/process/reviews/DEFERRED-S2-163-tokencheck-wire.md（reasoning-token 误报 + 流式交叉校验）、
docs/process/reviews/DEFERRED-S1-029-provisional-reconcile.md（accurate positive provisional）

## Context
S2-163（ce31bdf）把 tokencheck.CrossCheck 接进**非流**结算 draft（审计-only：confidence_score /
pending_reconciliation，成本与 usage_source 不变）。两项延后：①隐藏 reasoning token（o1/o3 计入
reported OutputTokens 但 CanonicalUsage 未单列）→ 估算器低估可见内容 → cross-check 假阳性；②cross-check
未覆盖流式路径。S1-029（15d4eb3）让缺 usage 流以零成本暂估 + pending；延后项③「partial usage 时算准确
正向 provisional」。

## 范围决策（本切片 = 审计完整性，sub-part 3 延后）
- **commit 1 = sub-part 1**（reasoning_tokens 去非流假阳性）：审计-only、无金额、无迁移。
- **commit 2 = sub-part 2**（流式交叉校验）：审计-only、无金额、无迁移。
- **sub-part 3（accurate positive provisional）不并入**：它是 **money-path**（改计费额），且 understand
  阶段揭示 4 个未决点 + 可能需迁移（新 usage_source 值），与 **Owner 门控的 settlementreconcile worker
  切片**共享 settler.go `outputTokensForAttempt` frame/token 消歧 + 迁移。→ 折入门控 worker 切片统一处理，
  不在本审计切片做半截 money-path 改动。（已在报告中向 Owner point out，可否决。）

## Sub-part 1 设计（commit 1）
- `backend/internal/proto/hcsf.go`（**frozen proto**，加既有 struct 字段，允许）：CanonicalUsage 加
  `ReasoningTokens int \`json:"reasoning_tokens,omitempty"\``，紧邻 OutputTokens，注释说明=隐藏推理 token，
  计入 OutputTokens、cross-check 需扣除。
- `backend/internal/proto/openai/sse.go`（非 frozen 子包）：openAIUsage 加
  `CompletionTokensDetails *openAICompletionTokensDetails`（新类型，字段 ReasoningTokens int
  `json:"reasoning_tokens"`）；`canonical()` 内 `out.ReasoningTokens = details.ReasoningTokens`。
- `backend/internal/gatewayhttp/chat_completions_billing.go`（**frozen**，改既有）：nonStreamingUsageDraft
  内 `visibleOutputTokens := usage.OutputTokens - usage.ReasoningTokens`（clamp ≥0）→ CrossCheck +
  outputTokenDelta 用 visibleOutputTokens。**TokensOutput 仍写 usage.OutputTokens（计费/落账完全不变）**。
- 与 `Accounting.ReasoningTokens`（accounting.go:16，env 级、openai_responses 输出投影用）分层不冲突：
  CanonicalUsage.ReasoningTokens = 解析的上游 usage 明细；Accounting 字段 = 输出渲染。互不替代。
- **判别测试**（chat_completions_pricing_test.go / billing_test）：o1 响应 reported OutputTokens=300
  （100 可见+200 reasoning）、估算可见≈100。无 ReasoningTokens 解析 → delta=200 → Fail20 →
  confidence 0.5 + pending=true（误报）；有解析 → visible=100 vs 估算 100 → OK → confidence 1.0 +
  pending=false。self-proving 双路相异 + mutation（去 ReasoningTokens 扣减）→ RED。

## 执行结果
- **commit 1（sub-part 1）已落** fix/hermes 2676a63 + 推送（#8 R1 clean）。
- **commit 2（sub-part 2）实现完成**（本 worktree，待 #8 review + 落地）：增量估算最终落地；
  新增 tokencheck.EstimateStreamDelta（导出，排除 reasoning）+ acc.EstimatedOutputTokens 逐事件累加
  + acc.Update 累积 ReasoningTokens + draft 携带 Reasoning/EstimatedOutputTokens + 共享 crossCheckAudit
  helper（非流/流式共用）+ streamingCompletionEvent stamp。
  - #8 R1 出 2×S2（均为估算准确性、产生假 pending）当轮修复：① drain 循环也累加 canonicalVisibleEstimate
    （断连后 drain 完成的响应不再因估算偏低误判）；② canonicalVisibleEstimate 计入 ContentBlock.Input
    （Gemini 式一次性 tool call 不再估算为 0/Unknown）。
  - 5 变异 RED→GREEN（M1 reasoning 排除 / M2 stamp 缺失 / M3 流式不扣 reasoning / R2-① drain 估算 /
    R2-② cb.Input 计入）。全后端 build+test 绿。
  - **#8 R2 出 1×[P2]，归类 S1（主路径误报，必修不延后）**：reviewer 指出估算无条件排除 `Delta.ReasoningText`，
    但 reported 侧只在 `ReasoningTokens>0` 时扣减；若 provider 把 thinking 以 ReasoningText 流出、计入
    OutputTokens、却不单列 ReasoningTokens → 估算偏低、reported 不扣 → 误报 pending。**3 reader 取证确认**
    （anthropic/sse.go:488-489 thinking_delta→ReasoningText 且 mergeUsage 从不写 ReasoningTokens；
    gemini/sse.go:252-258 thought→ReasoningText 且 usageMetadata 连 thoughtsTokenCount 都不解析；
    openai/sse.go:466 仅 OpenAI 单列 reasoning_tokens、不流 ReasoningText）。HUAKAI 是 Anthropic-兼容
    网关，Claude extended-thinking 是主流量 → 当前代码会误报整类 thinking 流 → **S1**。
  - **更深结论**：修法不能简单「把 reasoning 估算加回」。reported 是否含 thinking 因 provider 而异
    （Anthropic 计入 output_tokens / Gemini 不计入 candidatesTokenCount），两者都 `ReasoningTokens==0`，
    canonical 层无「folding」信号，靠 `RT==0` 无法区分；任一加/减规则必修好一个弄坏另一个。
  - **R2 修复（Option A —— folding 不可知即跳过）**：单独累加 `EstimatedReasoningTokens`（新
    canonicalReasoningEstimate，只数 Delta.ReasoningText/ContentBlock.Thinking）；crossCheckAudit 增
    `estimatedReasoning` 参,当 `reasoningTokens==0 && estimatedReasoning>0`（reasoning 文本流出但无对应
    单列 count）→ **跳过**交叉校验（保持满置信、不 pending）。宁可在 thinking 流上少覆盖,不误报主路径
    Claude 流量。**非流不动**（HeuristicEstimator 已计 buffered thinking 块、与 reported 自洽，传 0）。
    顺手修正 canonicalVisibleEstimate 过期注释（R1 已计 cb.Input，注释仍写"不计"）。
  - R2 再 2 变异 RED→GREEN（R2-fix-① 去跳过 → thinking 流 Fail20/pending → RED；R2-fix-②
    canonicalReasoningEstimate 误计可见 Text → visible-only reasoning 估算 >0 → RED）。build+相关包 test 绿。
  - **延后**：完整 reasoning-aware 逐 provider 交叉校验（按 folding 语义建模/对 Gemini 解析 thoughtsTokenCount
    并 wire ReasoningTokens）→ `docs/process/reviews/DEFERRED-S2-163-reasoning-folding-crosscheck.md`。
    本切片按「review 发现 spec drip → 停止扩张 + 写下一切片 spec」纪律,只消除误报、不在审计切片里做半截
    provider 建模。

## Sub-part 2 设计（commit 2，实际落地版）
- **不保留 ContentBlocks**（hot-path 内存隐患）。改**逐事件增量估算**（O(1) int，零内容滞留）：
  - `backend/internal/gateway/forwarder_types.go`（**frozen**，加既有字段）：UsageAccumulator 加
    `EstimatedOutputTokens int`。
  - `backend/internal/gateway/forwarder.go`（**frozen**，改既有）：handleEventWithAdapter 对每个 canonical
    event 的可见内容跑 tokencheck.HeuristicEstimator、累加到 acc.EstimatedOutputTokens（O(1) 内存）。
  - finishDraft：`visible := acc.Usage.OutputTokens - acc.Usage.ReasoningTokens`（若流式 usage 带
    reasoning），CrossCheck(visible, acc.EstimatedOutputTokens)；gate 同非流（delta≥50 + actualCost>0）；
    stamp confidence_score / pending_reconciliation 到 draft，**不改 usage_source / 成本**。
  - 流式 usage 是否带 reasoning_tokens：实现时核实流式 usage 事件解析；若 acc.Usage 未带 reasoning 则
    visible=OutputTokens（退化为不扣，安全：最多保留旧假阳性，不引入新错误）。
- **判别测试**：注入流式响应 reported=1000、累计估算≈500（稀疏内容）→ 断言 draft.ConfidenceScore=0.5 +
  pending=true（Fail20）；mutation（去 gate 或 estimate 累加）→ stamp 不发生 → RED；空内容→Unknown→不 stamp。

## 成功标准 / 纪律
- 每 commit：go build ./... + 相关包 test 全绿；判别测试 mutation RED→GREEN；#8 review 无未结 S0/S1。
- frozen 包仅改既有文件 / 加既有 struct 字段；**无金额改动、无 usage_source 改动、无迁移**（纯审计信号）。
- commit-naming-v2；落 fix/hermes（ff）+ push（非 force、非 main）。

## 风险
- sub-part 1/2 均审计-only：confidence_score/pending_reconciliation 仅观测，cost 与 usage_source 不变
  （cross-check 仅在 actualCost>0 时降级、且只动这两个既有审计列）。无 schema 迁移。
- sub-part 2 hot-path：增量估算每事件一次轻量字符计数，无内容滞留；估算失败/空→Unknown→不 stamp。
