# 2026-05-31 S2-002 Codex Plan

| Owner directive | "S2-002 one placeholder session flag enables six unverified upstreams" |
| Scope | In scope: `backend/internal/provider/registrydefault/default.go` and package tests. Out of scope: implementing the six session adapters, credential refreshers, protocol translators, schema, deployment. |
| Success criteria | No single placeholder env flag can silently register all six unverified session adapters; each placeholder family is independently gated; a discriminating test fails if the old aggregate flag re-enables many adapters; focused tests/build pass; Codex review has no unresolved S0/S1. |
| Time estimate | 30-60 minutes wall clock. |
| Blast radius | Startup-time default provider registry only. Operators who used the aggregate experimental flag must now opt into each unverified family explicitly, which is safer and matches the task acceptance. |
| Failure modes | A weak test might only count adapters and miss which family registered; mitigate by asserting every legacy-enabled family still returns `provider.ErrAdapterNotRegistered`, and by testing each new per-family flag registers exactly one expected adapter. |
| Decision points | None for the current fail-closed safety path. A future product decision can add a documented multi-flag profile, but not as a silent single env toggle. |
| Pre-execution checklist | Task `S2-002` started; files locked; `CLAUDE.md#8/#11/#12/#14` already read for this worker cycle; follow TDD red-green; run focused tests; stage intended diff; run `codex exec review --uncommitted -m gpt-5.5 -c model_reasoning_effort=xhigh`; commit only if no unresolved S0/S1. |

## Execution Order

1. Add a failing test proving `HUAKAI_ENABLE_PLACEHOLDER_SESSION_ADAPTERS=true` alone does not register the six placeholder session families.
2. Run `go test ./internal/provider/registrydefault` from `backend` and confirm the test fails against current aggregate-gate behavior.
3. Replace aggregate registration with explicit per-family env gates, one per placeholder protocol family.
4. Update opt-in tests so each per-family flag registers exactly its adapter and does not register siblings.
5. Re-run focused and provider tests, then `go build ./...`.
6. Stage only S2-002 files, run xhigh Codex review, commit, then submit `task.sh review S2-002 "<commit> + self-review summary"`.
