# 2026-06-01 S1-006 Codex Plan

| Owner directive | "Fix audit finding S1-006 to high quality and push it for review." |
| Scope | In: verify and fix the HUAKAI-internal model-route allowlist bypass in `backend/internal/pool/router/default_selector.go`; extend `backend/internal/pool/pool_test.go` with a discriminating regression test. Out: reference-project source reads, schema changes, auth/billing/quota changes, new runtime dependencies, and new files in frozen packages. |
| Success criteria | Current-code premise is confirmed; a test fails on the current bypass and passes after the fix; `go build ./...` passes from `backend/`; relevant `go test ./internal/pool/...` passes; staged diff receives best-effort Codex self-review; commit is pushed to `origin work/s1-006`. |
| Time estimate | 30-60 minutes wall clock; one Codex work session. |
| Blast radius | Account selection for requests with `RoutingPolicy.ModelAccountIDs` configured for a requested model. The policy source is dormant in production per audit, so production blast radius is latent feature behavior. |
| Failure modes | Test fixture may be non-discriminating: use an allowed account with no available slot and a better non-allowed account with an available slot, so broken code returns the non-allowed account while fixed code does not. Selector fix may accidentally disable standalone fresh selection when no model route exists: preserve current nil/no-route branch. Build or package tests may expose unrelated failures: record accurately and avoid unrelated edits. |
| Decision points | None for Owner during execution. No high-risk files, schema, auth core, billing ledger, quota enforcement, secrets, deployment scripts, or runtime dependencies are touched. Reference-project comparison is not required because this plan makes no reference-project behavior claim and follows the HUAKAI audit row only. |
| Pre-execution checklist | 1. Read `CLAUDE.md`, `AGENTS.md`, and `docs/RULES.md`. 2. Load `S1-006` audit row from `/home/ubuntu/audit`. 3. Inspect cited selector lines on current HEAD. 4. Claim edit files through `.coordination`. 5. Add failing test before production code. 6. Apply minimal selector change. 7. Run focused test, package tests, and build. 8. Stage and run best-effort self-review. 9. Commit and push. |

## Execution Order

1. Add a regression test to `backend/internal/pool/pool_test.go` that configures a model allowlist for account `101`, makes account `101` unable to acquire a slot, and includes non-allowed account `999` as an otherwise better fresh candidate. Expected behavior: the selector must not return `999`; it should return a wait plan for `101` or no eligible result, depending on candidate wait settings.
2. Run the focused test and confirm it fails on current HEAD because fresh fallback sees all eligible accounts.
3. Change `DefaultSelector.Select` so the fresh/fallback candidate list is the model-routed subset whenever the requested model has a configured allowlist. Preserve the current full eligible fresh path when no model allowlist is configured.
4. Re-run the focused test and `go test ./internal/pool/...`.
5. Run `go build ./...` from `backend/`.
6. Stage intended files and run the requested Codex review command with a 600-second timeout.
7. Fix any S0/S1 findings, then commit and push `HEAD:work/s1-006`.
