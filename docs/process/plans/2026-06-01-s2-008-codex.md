# 2026-06-01 S2-008 Codex Plan

| Owner directive | "Fix audit finding S2-008 to PRODUCTION QUALITY." |
| Scope | In: provider upstream-passthrough `base_url` endpoint safety, dispatcher target validation, dial-time binding, and discriminating tests. Out: schema changes, allowlist/profile subsystem, auth core, billing, quota, deployment. |
| Success criteria | Unsafe passthrough destinations fail before request construction, before secret-bearing dispatch, or at guarded dial time; existing safe passthrough path joining still works; mutation check observes red when the guard is removed; `go test` for touched packages and `go build ./...` pass. |
| Time estimate | 1 focused worker session; about 60-120 minutes including reference check, TDD, mutation, build, review, commit, push. |
| Blast radius | Provider request construction and gateway dispatch for credentials of type `upstream_passthrough`; no frozen package new files; no DB schema or runtime dependency change. New provider helper file is in non-frozen `backend/internal/provider` to keep file size under the 500-line split signal. |
| Failure modes | Over-blocking legitimate public HTTPS proxy hosts; under-blocking private/metadata/obfuscated hosts; DNS rebinding between validation and dial; leaking blocked raw URL in errors; forgetting a caller after helper signature changes. Mitigation: central helper tests, dispatcher alias/rebind tests, adapter propagation test, `rg EndpointForCredential`, and package-wide tests. |
| Decision points | If a future product requires custom upstream passthrough through account proxies or custom RoundTrippers, require an explicit allowlist/design slice; this burn patch fails closed where HUAKAI cannot bind DNS validation to the actual direct dial. |
| Reference approach | Portkey validates custom upstream hosts before use and blocks unsafe schemes, credentialed URLs, metadata/internal hosts, private/reserved IPs, alternate IP forms, IPv6 private/reserved forms, and invalid ports: `Portkey-AI/gateway@669825cbe89e:src/middlewares/requestValidator/index.ts:260`. |
| Pre-execution checklist | Read `CLAUDE.md`, `AGENTS.md`, `docs/RULES.md`; load S2-008 audit row; trace credentialstore/provider/vault/adapter flow; consult safe decompositions and MIT Portkey source; claim files in `.coordination`; write failing test first. |

## Concrete Execution Order

- [x] Add a failing provider helper test for unsafe passthrough `base_url` values: HTTP scheme, loopback, private IPv4, link-local metadata, IPv6 loopback, userinfo, trailing-dot host, special-use TLD, and numeric-obfuscated loopback.
- [x] Add a failing Anthropic adapter test proving unsafe `base_url` is rejected before a request with `Authorization` or `X-API-Key` can be built.
- [x] Run the targeted tests and observe RED on current code.
- [x] Implement a centralized fail-closed `EndpointForCredential` guard in `backend/internal/provider/adapter.go`, returning a typed error and never including the raw blocked URL in error text.
- [x] Split passthrough endpoint safety helpers into `backend/internal/provider/passthrough_endpoint_guard.go` so `adapter.go` stays below the file-size split signal.
- [x] Update provider callers to propagate the helper error instead of building requests.
- [x] Add dispatcher validation and guarded direct-dial transport wrapping for custom passthrough requests, including a DNS-rebind regression test.
- [x] Keep existing safe path-joining behavior for public HTTPS custom hosts and query preservation.
- [x] Run targeted package tests, mutation revert checks, `go test -race ./internal/provider/...`, `go test -race ./internal/gateway`, `go test ./...`, and `go build ./...`.
- [ ] Stage intended diff, run Codex self-review, fix any S0/S1, commit with root cause plus Portkey citation, and push `HEAD:work/s2-008`.
