# 2026-05-20 case-c-billing-settings-admin-api-codex
| Owner directive | "HUAKAI Go backend 实施任务 — case C 可配置计费设置 Phase 1C(管理 API)。HUAKAI 内部代码,不读参考项目源码,不执行任何 git 操作。" |
| Scope | In: `backend/internal/gatewayhttp` admin billing settings handler + focused tests, `backend/cmd/gateway` route wiring/deps, `docs/openapi/openapi.yaml` contract. Out: billing hot path, `billing/state.go`, resolver cache logic, billing_settings migration, reference project source, git operations. |
| Success criteria | `GET /admin/v1/billing/settings?tenant_id=N` returns persistent policy with `source`, allowed/roadmap values, metadata. `PUT /admin/v1/billing/settings` validates tenant scope, reason, allowed values, roadmap `bill_input`, persists via billing PolicyStore entry, and writes admin audit. Tests cover platform_admin, tenant_operator own/cross tenant, invalid value, roadmap rejection, missing reason, GET source tenant/default. Requested build and tests run with the provided cache env and report 0 FAIL if successful. |
| Time estimate | 60-90 minutes wall clock; one Codex implementation/review loop. |
| Blast radius | Admin API route surface and OpenAPI only. Runtime deps gain a billing PolicyStore field for this endpoint. No DB schema, auth core, billing ledger, quota, or streaming settlement changes. |
| Failure modes | Missing audit wiring could silently lose operator evidence; mitigate by using existing `admin_audit_events` sqlc interface and tests. Cross-tenant scope bug could violate DR-001; mitigate by reusing existing admin identity roles and `adminCanAccessTenant`/query parsing style. Resolver cache drift could happen if writing store directly; accepted for Phase 1C because task forbids cache logic changes and GET must read persistent store. OpenAPI drift; mitigate by adding path/schemas near existing admin billing section. |
| Decision points | Existing admin audit infrastructure exists, so use it. If audit insertion fails after policy write, handler returns service unavailable and logs structured error; full atomic cross-package transaction is a future hardening decision because current PolicyStore entry owns its own sqlc write path. Parallel Claude/Codex independent plan reconciliation is not available in this single Codex execution; Owner/Claude may reconcile this artifact later if strict process gating is required. |
| Pre-execution checklist | 1. Read existing admin email settings handler and admin identity scope rules. 2. Read billing PolicyStore/PolicyResolver interfaces and settings validation. 3. Confirm existing admin audit writer. 4. Implement handler with no new runtime dependencies. 5. Wire route and dependency. 6. Add focused tests. 7. Update OpenAPI. 8. Run gofmt. 9. Run requested build and test commands from `backend/`. |
| Concrete execution order | Handler first, tests second, route wiring third, OpenAPI fourth, gofmt/checks last. |
