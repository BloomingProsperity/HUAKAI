# 2026-05-18 P2#2 Refund Receipt Sink Gateway Wire

| Owner directive | "修 P2#2: cmd/gateway/main.go wire refund worker WithRefundReceiptSink" |
| Scope | In: inspect gateway refund-worker wiring, audit refund worker API, receipt storage API; wire the existing receipt storage as refund receipt sink; add focused Go coverage proving refunded receipt is readable through the receipt path. Out: reference-project source, frontend, Rust, billing settler, mismatch detector, cost receipt handler, refund worker business logic, schema changes, secrets, deployment. |
| Success criteria | `cmd/gateway/main.go` passes `audit.WithRefundReceiptSink(receiptStorage)` or a minimal adapter if required; refund-worker execution persists a `mismatch_refunded` receipt observable through receipt retrieval; requested build and test commands pass or any blocker is reported honestly. |
| Time estimate | 45-90 minutes wall clock, mostly test discovery and race-test runtime. |
| Blast radius | Gateway startup wiring and audit/gateway HTTP tests. Failure could leave refund receipts signed but not persisted, or introduce compile-time interface mismatch. |
| Failure modes | Receipt storage may not match `RefundReceiptSink`; mitigate with a narrow adapter near gateway wire. Existing dirty worktree may include overlapping edits; read current files before patching and avoid reverting unrelated changes. Integration test may require existing helpers; reuse local test fixtures instead of broad new infrastructure. |
| Decision points | Stop for Owner only if the fix requires touching high-risk files, adding runtime dependencies, changing database schema, changing refund worker core logic, or editing forbidden files. |
| Pre-execution checklist | 1. Confirm current dirty worktree and avoid unrelated changes. 2. Read specified gateway wire region. 3. Read audit refund worker API and receipt storage signature. 4. Find existing receipt retrieval tests/helpers. 5. Patch gateway wire and focused test only. 6. Run requested build and race tests. |
| Concrete execution order | Read target files, inspect test helpers, patch gateway wire, add or update the smallest integration-style test, run `gofmt`, run `cd backend && GOCACHE=/tmp/go-cache go build ./...`, run `cd backend && GOCACHE=/tmp/go-cache go test ./internal/audit/... ./internal/gatewayhttp/... ./cmd/gateway -race -count=1 -timeout 180s`, summarize in Chinese with sources and lane metadata. |
