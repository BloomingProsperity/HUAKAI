# 2026-06-01 S2-016 Realtime Runtime Guard

| Owner directive | "Fix audit finding S2-016 to PRODUCTION QUALITY." |
| Scope | In: verify HUAKAI route/proto/docs logic; add a narrow runtime guard for `/v1/realtime` if the premise still holds; add a discriminating Go test; update OpenAPI/parity docs to make the deferred runtime contract explicit. Out: full Realtime/WebSocket proxy, stream resume, partial usage settlement, schema migration, auth core, billing ledger, quota enforcement, provider adapter work, non-MIT reference source reads. |
| Success criteria | `/v1/realtime` no longer falls through to an implicit 404; it returns a typed JSON error making the Phase 9+ roadmap status explicit; OpenAPI route consistency remains green; the regression test fails if the route guard is removed; `go build ./...` and targeted Go tests pass. |
| Time estimate | 45-75 minutes wall clock; one Codex small-safe-patch work unit. |
| Blast radius | `backend/cmd/gateway` route table and test coverage; `docs/openapi/openapi.yaml`; `docs/03_FEATURE_PARITY_MATRIX.md`. No frozen-package new files. No DB/auth/billing/quota path changes. |
| Failure modes | Accidentally shipping a fake Realtime implementation instead of an explicit unavailable response; adding an implementation-only route without OpenAPI; weak test that accepts 404; clean-room breach by reading non-MIT raw source; stale reference citation. Mitigation: route only returns typed 501, OpenAPI documents 501, mutation run removes route and must turn test red, reference citation limited to refreshed MIT Portkey source. |
| Decision points | Full WebSocket runtime remains Phase 9+ mandatory roadmap and is not implemented in this patch. If tests reveal the fix requires auth/billing/quota/schema changes, stop and mark blocked. |
| Pre-execution checklist | 1. Read audit row and HUAKAI route/proto/parity docs. 2. Verify current `/v1/realtime` premise with a failing test before production code. 3. Read only MIT Portkey source for reference handling and cite refreshed default-branch lines. 4. Edit existing non-frozen files only. 5. Run red/green/mutation/restore verification. 6. Stage, run Codex review, fix S0/S1 only, commit, push. |
| Target files / package check | `backend/cmd/gateway/routes.go` and `backend/cmd/gateway/openapi_consistency_test.go` are in `cmd/gateway`, not frozen. `docs/openapi/openapi.yaml` and `docs/03_FEATURE_PARITY_MATRIX.md` are docs. No files will be added under frozen `backend/internal/{gatewayhttp,gateway,proto}`. |
