# DEFERRED — Gemini credentialacq mode/credentialstore handler 缺失

- **Severity**: S2 (operator-side workflow,未接通生产即 no-impact)
- **来源 codex review**: 2026-05-24 gemini commit 前 Round 1 P2 finding #1
- **Affected files**:
  - `backend/internal/credentialacq/vendor_exchangers.go:43` Gemini exchanger 注册行
  - `backend/internal/credentialacq/DefaultModePlans` 缺 gemini/oauth case
  - `backend/internal/credentialstore/DefaultHandlerRegistry()` 缺 gemini+oauth case
  - acquisition/session + account credential schema CHECK 约束 也不含 gemini
- **问题描述**:
  - Gemini commit 注册 `gemini/oauth` exchanger,默认 callback registry 通告 flow
  - 但 `CreateFromStart` 因 `DefaultModePlans` 无 gemini/oauth → 拒绝创建 session
  - 任何手动 seed 的 callback 在 `Finalizer.ValidateCandidate` fail(credentialstore 也无 gemini+oauth handler)
  - 新 unit test 只覆盖 `registry.Exchange` lookup,advertised callback/persist path 实际不可用
- **不 block 当前 commit 的原因**:
  - Gemini 实际生产路径**未接通**(scheduler 没注 Gemini refresher)
  - 仅注册 exchanger 不会触发 fail,只在真有 Gemini OAuth flow 才命中
  - Owner 拿 Gemini 真 OAuth endpoint 抓包前不会启用此 flow
- **应在哪个切片修**:**Gemini 真接通切片**(等 Owner 提供 Gemini OAuth endpoint 抓包后):
  - 同期补 `DefaultModePlans` gemini/oauth plan
  - 同期补 `credentialstore.DefaultHandlerRegistry()` gemini+oauth handler
  - migration 加 gemini 入 vendor CHECK
  - scheduler 注入 Gemini refresher
- **Tracker**: 与 [[DEFERRED-windsurf-storage-handler]] 同模式 — Owner 真抓包切片合并修
