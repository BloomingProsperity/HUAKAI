# 2026-05-15 L2-A5.1 OpenSSL profile cipher suites and ALPN injection

| Owner directive | "L2-A5.1: OpenSSL profile-driven cipher_suites + ALPN injection (feature-gated)" |
| Scope | In: HUAKAI internal OpenSSL mimicry adapter, Codex CLI fingerprint template values, existing mimicry OpenSSL adapter tests, and local `~/refs/rust-openssl` official API reference if needed. Out: ProxyEngine dispatch, supported_groups, signature_algorithms, ec_point_formats, extension 22, database schema, auth, billing, quota, deployment, and all non-approved reference projects. |
| Success criteria | `OpenSslMimicryAdapter::new_with_profile(&FingerprintProfile)` applies profile cipher suites and ALPN protocols to `SslContextBuilder`; existing `new()` remains compatible; capture diff reports ordered matches for cipher suites and ALPN; requested build/test commands pass; no new dependency is added. |
| Time estimate | 45-75 minutes wall clock / one Codex work unit. |
| Blast radius | Limited to feature-gated OpenSSL mimicry code and its feature-gated tests. If wrong, TLS handshakes under `mimicry-openssl` may fail or fail to mimic selected ClientHello fields. |
| Failure modes | OpenSSL cipher names may not match the installed OpenSSL build; mitigate by using OpenSSL-accepted names and verifying via `set_cipher_list`/`set_ciphersuites`. ALPN wire bytes may be malformed; mitigate with explicit length-prefix construction and capture diff assertion. Test helper behavior may time out; mitigate by bounding connect with `tokio::time::timeout` and accepting the expected handshake failure after capture. |
| Decision points | Owner confirmation is needed only if implementation would require adding runtime dependencies, touching high-risk files, expanding into proxy dispatch, or reading forbidden reference projects. None is expected for this atom. |
| Pre-execution checklist | Confirm worktree state; read current adapter/profile/test shapes; read `tools/fingerprint-collector/templates/codex-cli.json` cipher suites; consult local `~/refs/rust-openssl` only for API signatures if needed; implement minimal helpers; run requested verification. |
| Concrete execution order | 1. Inspect current `src/mimicry/openssl_adapter.rs`, profile definitions, capture/diff helpers, and OpenSSL tests. 2. Extract Codex CLI cipher IDs from the template. 3. Add error variants, `new_with_profile`, cipher mapping, cipher suite application, and ALPN wire construction. 4. Add one feature-gated capture diff test with debug `eprintln!`. 5. Run build/tests and `git diff --check`. 6. Report changed files, mapping count, ALPN byte length, test counts, warnings, and risk summary. |
