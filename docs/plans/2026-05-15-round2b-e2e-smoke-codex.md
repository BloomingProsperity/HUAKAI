# 2026-05-15 Round 2-B E2E Smoke

| Field | Plan |
|---|---|
| Owner directive | "Build + run HUAKAI Round 2-B 5 features E2E module smoke test (NOT vendor key validity test)." |
| Scope | In: local PostgreSQL test DB, backend migrations 0001..0018, gateway build, one HUAKAI user request plus one identical cache-hit request, verification SQL/log checks, clean shutdown. Out: direct OpenAI key validity testing, reference-project source reads, production DB/secrets mutation, retries beyond one transient failure. |
| Success criteria | Gateway boots against isolated test DB; upstream credential is loaded through HUAKAI credential storage path; request 1 completes through HUAKAI with `gpt-4o-mini`, `max_tokens=10`, body `Hi`; request 2 returns L2 cache hit with no additional upstream charge; billing/audit/stream/event evidence is recorded or any gap is reported with source-backed root cause. |
| Time estimate | 45-90 minutes wall clock, mostly environment discovery, migration/application, boot diagnostics, one paid smoke call, and verification queries. |
| Blast radius | Local files under `docs/plans/`; local test database `huakai_smoke_2026_05_15`; `/tmp/huakai-gateway`; local gateway process on `127.0.0.1:18080`; OpenAI spend under 1 cent. |
| Failure modes | Migration failure: stop before paid call and report failing migration. Boot failure: stop before paid call and report missing env/config/root cause. Admin API unavailable: use direct SQL only for isolated test DB and record why. Cache/event/billing evidence missing: do not rerun paid request repeatedly; inspect local DB/logs and report pass/fail honestly. |
| Decision points | No high-risk production changes planned. Stop if any step would touch production DB, real secrets beyond reading the provided OpenAI upstream key, billing ledger code, auth core code, or quota enforcement code. |
| Pre-execution checklist | Read `docs/RULES.md` and backend startup/config code; verify PostgreSQL availability; create/detect isolated DB; apply migrations; build gateway with local Go cache; start gateway with required env; seed tenant/account/credential/user API key through admin API or direct SQL; make exactly one paid HUAKAI request; make one cache verification request; query verification tables; shut down process. |
| Concrete execution order | 1. Inspect rules, backend entrypoint, config/env, migrations, credential/account/key schemas, and route wiring. 2. Prepare DB and migrations. 3. Build gateway. 4. Start gateway with env and log capture. 5. Seed test data through the safest available path. 6. Execute request 1 through HUAKAI only. 7. Verify Round 2-B evidence. 8. Execute request 2 for cache hit. 9. Capture logs, kill gateway, summarize. |

Owner authorization already included in the task prompt for this session. Claude parallel-draft reconciliation is not available inside this single Codex execution context, so this plan records Codex's independent plan before execution and proceeds under the explicit Owner authorization.
