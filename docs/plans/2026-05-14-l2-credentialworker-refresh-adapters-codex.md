# 2026-05-14 L2 credentialworker refresh adapters

| Owner directive | "HUAKAI L2 — credentialworker 真刷新 adapter 注册 (消除\"占位壳\"问题, Owner 2026-05-14 issue #5)。" |
| --- | --- |
| Scope | In: credentialworker refresh registry, four real OAuth refresh adapters, mock-only stub handling, scheduler wiring in gateway main, focused credentialworker tests, `/tmp` completion report. Out: `backend/internal/provider/`, L1/L3 work, DB schema, auth core, billing/quota logic, new runtime dependencies, `LICENSE`. |
| Success criteria | `ProviderDispatchRefresher` placeholder is gone; `RegistryRefresher` dispatches real vendors through registered adapters; mock-only vendors return explicit `ErrMockOnly`; missing/unknown behavior is tested; gateway builds registry with 4 real + 7 stub vendors; `go test ./internal/credentialworker/...` and `go vet ./internal/credentialworker/...` pass from `backend/`; final `/tmp/codex-l2-refresher-adapters-final.txt` contains requested evidence. |
| Time estimate | 60-90 minutes wall clock; one Codex implementation pass plus verification. |
| Blast radius | Credentialworker refresh behavior and gateway startup wiring only. The main runtime risk is malformed credential JSON or OAuth endpoint assumptions causing refresh failure; mitigated by local HTTP server tests and conservative error returns. |
| Failure modes | Wrong credential payload shape: adapters validate required fields and return explicit errors. HTTP transient failure: adapters use 5s timeout plus one retry. Reference-source contamination: read only for behavior evidence, cite `repo@sha:file:line`, and write independent HUAKAI code. File-count mismatch: task text says <=7 files but enumerates 8 implementation files; follow explicit file list and keep changes tightly scoped. |
| Decision points | No high-risk owner sign-off expected unless implementation requires DB/auth/billing/quota/deployment changes, which are out of scope. Unknown endpoint specifics are implemented via overridable endpoint fields so tests can prove behavior without hardcoding secrets. |
| Pre-execution checklist | 1. Read current credentialworker scheduler/refresher/options/types and gateway main. 2. Read reference OAuth refresh regions under clean-room specifier lane only as needed for citations. 3. Add registry and adapters with Chinese comments. 4. Replace refresher dispatch and gateway wiring. 5. Add focused tests. 6. Run `gofmt`, credentialworker tests, credentialworker vet. 7. Write `/tmp` final report. |
| Concrete execution order | Plan, inspect code, collect clean-room citations, implement adapter registry and adapters, update refresher, update gateway main, add tests, format, test, vet, final report. |
