# 2026-05-17 F-COMM-001 Invitation Referral Spec - Codex Plan

| Owner directive | "你是 HUAKAI codex specifier lane, 任务 = F-COMM-001 邀请/推荐系统 spec 起草 (Phase 6 商业必备, 估时 4-6 hr)." |
| Scope | In scope: source-verified, clean-room reference behavior notes for `~/refs/sub2api`, `~/refs/all-api-hub`, and `~/refs/new-api`; new HUAKAI spec `docs/specs/community-invitation-referral.md`; F-COMM-001 matrix sync in `docs/03_FEATURE_PARITY_MATRIX.md`; Chinese final report with source-read tail. Out of scope: frontend, Rust, control plane, existing backend Go code, runtime implementation, schema migrations, license edits, secrets, billing-ledger code changes. |
| Success criteria | Spec contains required metadata, problem statement, principles, HUAKAI-owned data model, five endpoint contracts, cross-spec dependencies, 15-20 acceptance tests, differentiation table, assumptions, risks, and clean-room source citations. Matrix row reflects Phase 6 commercial status and dependencies. No copied reference identifiers, schemas, comments, UI source, or line-by-line algorithm translation. |
| Time estimate | 4-6 hours wall clock: 45-75 minutes repo/spec anchors, 90-120 minutes reference source reading and notes, 90-150 minutes spec drafting, 20-40 minutes matrix sync and self-review. |
| Blast radius | Documentation-only changes should not affect build/runtime. Main risks are stale or unsupported reference claims, clean-room contamination, broken internal cross-references, or accidentally overstating implementation status. |
| Failure modes | Missing source evidence: limit claims to observed files and mark open questions. Clean-room risk: paraphrase behavior only, avoid upstream identifiers and schema details, cite source lines separately. Reference repos missing or stale: record unavailable/stale state and do not invent behavior. Matrix drift: keep status as roadmap/spec unless implementation evidence exists. |
| Decision points | Owner confirmation is needed before execution under the parallel-plan rule unless this directive is treated as the synthesized approved plan. High-risk changes remain out of scope: auth core, billing ledger, quota enforcement, database schema, runtime dependencies, deployment, secrets, and `LICENSE`. If reference behavior is absent or contradictory, Owner may need to choose whether to preserve the requested HUAKAI design as product intent despite upstream gaps. |
| Pre-execution checklist | 1. Confirm this Codex plan is accepted or reconciled with Claude's independent plan. 2. Read `CLAUDE.md` sections #11/#12 or the equivalent project rules in `AGENTS.md`. 3. Read `docs/specs/_TEMPLATE.md`. 4. Read 1-2 adjacent specs such as `docs/specs/voucher-system.md` and `docs/specs/user-consumption-transparency.md`. 5. Verify reference repo availability and current HEAD SHAs under `~/refs/`. 6. Search references narrowly for invitation/referral/tier/credit/anti-abuse behavior. 7. Draft source-coverage notes before writing behavior claims. 8. Update only the new spec and parity matrix. 9. Run lightweight markdown/link/grep self-checks where available. |
| Concrete execution order | 1. Read local project rules and spec anchors. 2. Inspect relevant reference files under a clean-room specifier lane, collecting only behavior observations with `<repo>@<sha>:file:line` evidence. 3. Write internal notes in working memory only, not as copied upstream structure. 4. Add `docs/specs/community-invitation-referral.md` with HUAKAI-native design and acceptance tests. 5. Add the required differentiation table in spec section 10. 6. Update `docs/03_FEATURE_PARITY_MATRIX.md` F-COMM-001 row to point at the new spec and dependencies without claiming implementation. 7. Self-review for unsupported claims, copied identifiers, missing metadata, and acceptance-test coverage. 8. Report files changed, subtask completion, approximate word count, citations, residual risks, and required Owner confirmations in Chinese. |

## Clean-Room Lane Notes

- Lane: specifier.
- Reference projects in scope: `sub2api`, `all-api-hub`, `new-api`.
- The work must not copy source code, source schemas, comments, frontend source, distinctive identifiers, or algorithm order from non-MIT references.
- Every reference-project behavior claim must be backed by source regions actually read during execution.
- If the available source does not support a requested claim, the spec must preserve the HUAKAI product requirement as HUAKAI design intent and mark the reference gap explicitly.
