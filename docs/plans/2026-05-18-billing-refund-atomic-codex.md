# 2026-05-18 billing refund atomic transaction codex
| Owner directive | "修 billing refund 三步无事务边界 (billing.Refund -> audit ledger -> refunded receipt 三步必须原子)" |
| Scope | In: `backend/internal/audit/refund_worker.go`, billing settler tx entrypoint if needed, audit ledger tx append if needed, receipt PGX storage tx append, focused Go tests for refund atomicity. Out: reference reverse-proxy source, frontend, Rust, `vendor/boring`, `backend/internal/gatewayhttp/cost_receipt_handler.go`, `backend/internal/audit/mismatch_detector.go`, production schema changes, secrets. |
| Success criteria | Refund worker performs refund event, audit ledger append, and refunded receipt append inside one PostgreSQL transaction; step-3 failure rolls back step 1 and step 2; all-success path commits all three rows and marks DLQ pending completed; requested build and tests pass or any blocker is recorded honestly. |
| Time estimate | 45-90 minutes wall clock; one Codex executor lane. |
| Blast radius | Billing refund worker, audit ledger append path, receipt storage append path, focused audit/billing/auditledger tests. |
| Failure modes | Interface mismatch between production and test doubles: mitigate with minimal tx-capable interfaces near worker. Existing storage lacks tx abstraction: add narrow PGX `AppendInTx`. Race tests may expose unrelated package issues: report separately without hiding refund result. |
| Decision points | Stop for Owner only if a high-risk change becomes required: DB schema, auth core, billing ledger semantics beyond transaction plumbing, quota enforcement, deployment scripts, dependency addition, or touching prohibited files. |
| Pre-execution checklist | 1. Read refund worker process path. 2. Read billing settler refund tx support. 3. Read audit ledger PGX append tx support. 4. Read receipt PGX append path. 5. Implement tx composition with minimal interface changes. 6. Add AT_AUDIT_001_047 and AT_AUDIT_001_048 tests. 7. Run `cd backend && GOCACHE=/tmp/go-cache go build ./...`. 8. Run `cd backend && GOCACHE=/tmp/go-cache go test ./internal/audit/... ./internal/billing/... ./internal/auditledger/... -race -count=1 -timeout 180s`. |
| Concrete execution order | Inspect first, patch only the narrow worker/storage/settler/ledger surface needed, then run focused tests before broader requested commands. |
