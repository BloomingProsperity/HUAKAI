# 2026-05-15 rust-core-gateway module split
| Owner directive | "按职责拆分两个过大的 Rust 文件 (behavior-preserving refactor)" |
| Scope | In: `crates/core_gateway/src/proxy_engine.rs` -> `proxy_engine/` directory, `crates/core_gateway/src/attempt_reporter.rs` -> `attempt_reporter/` directory, visibility/use path fixes, inline unit test relocation, cargo verification. Out: logic changes, signature behavior changes, dependency additions, `src/lib.rs` module declarations, reference-project source reading. |
| Success criteria | Existing public paths continue to compile; `cargo test` passes; old flat files are replaced by directory `mod.rs` modules; requested submodule responsibilities exist; line counts are reported. |
| Time estimate | 60-90 minutes wall clock, one Codex execution lane. |
| Blast radius | Rust crate compile surface for proxy forwarding, attempt reporting, listener imports, and integration tests. A bad move can break module visibility or tests, but should not affect runtime behavior if code is moved mechanically. |
| Failure modes | Missing re-export breaks external imports; private helper visibility breaks sibling modules; moved tests lose helper imports; rustfmt uncovers ordering or module path issues; dirty worktree already contains unrelated edits. Mitigation: keep old `pub mod` declarations unchanged, add `pub use` in each `mod.rs`, use `pub(super)`/`pub(crate)` only for sibling access, run targeted compile/tests, and avoid reverting unrelated changes. |
| Decision points | No high-risk file is planned. If a fix requires touching auth core semantics, quota/billing logic, database schema, deployment scripts, real secrets, or adding dependencies, stop for Owner confirmation. Parallel Claude plan/synthesis is not available inside this single-agent execution; this plan records Codex's independent plan before mutation. |
| Pre-execution checklist | 1. Read current file outlines and external imports. 2. Rename flat files to module directories with `mod.rs`. 3. Split `proxy_engine` into `error`, `http_client`, `auth`, `headers`, and `relay` while preserving `ProxyEngine` orchestration in `mod.rs`. 4. Split `attempt_reporter` into `types`, `metrics`, and `idempotency` while preserving worker/reporter orchestration in `mod.rs`. 5. Add transparent `pub use` re-exports for externally used items. 6. Run `cargo fmt`. 7. Run `cargo test 2>&1 | tail -25`. 8. Report changed files, line counts, test summary, and path-preservation result. |
