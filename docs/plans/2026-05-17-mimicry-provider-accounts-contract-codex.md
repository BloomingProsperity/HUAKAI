# 2026-05-17 mimicry template + Provider Accounts contract fix
| Owner directive | "任务 = mimicry template + Provider Accounts 三层契约修复 (P0+P0)" |
| Scope | In: mimicry template placement/recursive registry, backend admin provider-account route and handler contract, OpenAPI sync, focused backend tests. Out: frontend, LICENSE, billing, Rust core_gateway, pool dispatcher, Email SMTP, spec wave, reference project source. |
| Success criteria | Mimicry mode registration tests pass; provider-account backend paths align with `/v1/admin/provider-accounts` while keeping temporary pool-account alias; create derives tenant from admin session; list/get/update/clear-rate-limit endpoints exist and are covered; OpenAPI matches backend; requested Go tests/build pass. |
| Time estimate | 1-2 hours wall clock; single Codex executor lane. |
| Blast radius | Backend HTTP admin routes and mimicry template loading. Incorrect changes could break admin provider-account operations or template registration. |
| Failure modes | Route mismatch remains; tenant derivation breaks tests without auth context; OpenAPI drifts from handler; recursive loader double-registers names; full backend test/build exposes pre-existing unrelated failures. Mitigation: inspect existing handler/store tests first, keep changes scoped, add focused contract tests before full checks. |
| Decision points | Stop for Owner confirmation before touching frozen areas, adding runtime dependencies, schema migrations, billing/quota/auth-core changes, or destructive file operations beyond the requested template move. |
| Pre-execution checklist | Read mimicry registry/tests; read gateway route mount and admin pool account handler/tests; grep OpenAPI for old/new paths; move template; patch recursive load; patch backend route/handler/tests/OpenAPI; run focused mimicry test; run focused/admin tests as needed; run requested backend test/build commands. |
| Concrete execution order | 1. Inspect target files only. 2. Move template and make registry recursive. 3. Run mimicry focused test. 4. Inspect provider-account handler/store shape. 5. Patch routes/handler/tests/OpenAPI. 6. Run focused provider-account tests. 7. Run full backend test summary and build. 8. Report concise Chinese summary. |
