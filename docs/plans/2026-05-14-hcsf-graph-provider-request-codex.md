# 2026-05-14 HCSF graph to provider request
| Owner directive | "HUAKAI L3 — HCSF graph→provider-request 完整 + default 开启 (Owner 2026-05-14 issue #6 #7)" |
| Scope | In: HCSF capability graph marshal to `anthropic_messages` / `openai_chat` / `openai_responses`, request controls overlay, default-on HCSF dispatch, endpoint family metadata, focused gateway/http tests, `/tmp` evidence files. Out: L1 admin/main wiring, L2 credentialworker, auth/billing/quota/schema/auditledger/sign/trust-chain interfaces, non-MIT reference source reading. |
| Success criteria | `MarshalToProviderRequest` covers text/system/tool_use/tool_result/image/thinking/cache_control/unknown with explicit loss for unsupported projection; dispatcher uses it instead of OpenAI-only hardcode; handler defaults HCSF on unless `HUAKAI_DISPATCH_HCSF=0`; tests cover requested endpoint families and default toggle; `go test ./internal/gateway/... ./internal/gatewayhttp/...` passes. |
| Time estimate | 90-120 minutes wall clock, one Codex implementation lane. |
| Blast radius | Non-streaming provider body construction and handler dispatch selection. Stream forwarding, account selection, credential refresh, billing ledger, quota, DB schema, deployment, and trust-chain signatures are not edited. |
| Failure modes | Body schema drift causing provider rejection: mitigate with family-specific tests and official public API examples. Silent capability loss: every unsupported node path appends `ProtocolLossEntry`. Existing raw fallback tests expecting default-off may fail: update assertions to explicit env-off. File-size budget overrun: keep helpers local and table-driven tests compact. |
| Decision points | No Owner sign-off needed for requested low/medium-risk code/test changes. High-risk areas remain out of scope. Parallel Claude/Codex synthesized plan is not available inside this direct Codex execution; proceeding because Owner explicitly supplied the work unit and said not to ask Owner. |
| Pre-execution checklist | 1. Confirm baseline tests pass. 2. Inspect HCSF node structs and handler dispatch path. 3. Add `RequestMeta.EndpointFamily`. 4. Add marshal layer and tests. 5. Wire dispatcher and handler. 6. Run gofmt, targeted tests, vet/test evidence capture. 7. Write `/tmp/codex-l3-hcsf-graph-final.txt`. |
| Concrete execution order | Proto metadata -> gateway marshal -> dispatcher overlay -> gatewayhttp default/metadata -> tests -> gofmt -> go test -> final evidence. |

Clean-room note: this task uses official public API contracts only and does not read Sub2API, LiteLLM, Portkey, or any other non-MIT reference source.
