# 2026-05-16 R-E-A transport baseline burn-the-boats Codex plan

| Owner directive | "R-3 R-E Phase R-E-A (transport baseline switch + burn-the-boats fallback 删除)" |
| Scope | In: `exploratory/rust-core-gateway/merged/crates/core_gateway/src/route_client.rs`, focused Rust tests/fixtures in the same crate, evidence commands. **R-E-A code review fix (PM OCAW 2026-05-16 follow-up)** 后扩为含 `src/lib.rs` (GatewayState transport wire) + `src/proxy_engine/http_client.rs` (HTTPS hyper-rustls 临时恢复) + `Cargo.toml` (阶段性注释, 非 dep 变更). Out: reference-project source, **真新增 Cargo dep** (R-E-A+1 才动 tonic tls feature), `backend/`, `LICENSE`, auth core, billing, quota, database schema, deployment scripts, R-D smoke scaffold, device fingerprint binding (R-E+1). |
| Success criteria | `TransportBaseline` supports default UDS and fully specified mTLS config; UDS endpoint construction validates 0600 plus same UID/GID; mTLS endpoint construction uses tonic TLS config where the crate already supports it and fails fast if any cert/key/CA path is missing; **dispatch fallback (HyperRustlsClient / fallback_client) 删除** (122 行 fallback 删除 = 31 src + 91 test); **HTTPS forward client 临时保留 hyper-rustls** (`proxy_engine/http_client.rs` 用 `HttpsConnector<HttpConnector>`, 加 `forward_to_https_endpoint_uses_tls` 回归测试, Cargo.toml hyper-rustls 加阶段性保留注释); **PM OCAW 2026-05-16** = R-E+1 切 rquest 后真删 hyper-rustls dep; requested cargo test command run with workspace-local `CARGO_TARGET_DIR`; final report includes `git status`, `git diff --stat`, test result, and required lane tail. |
| Time estimate | 90-150 minutes wall clock for this lane, depending on existing tonic TLS feature availability and current test health. |
| Blast radius | Rust exploratory core gateway route-client construction and its tests only. No Go control-plane behavior, deployment routing, database, billing, quota, or auth core changes. |
| Failure modes | Existing crate may not compile tonic TLS APIs without a pre-existing feature; mitigation is to stop before dependency edits and report the OCAW need. UDS UID/GID checks may need Unix-only metadata extensions; mitigation is to keep checks behind Unix cfg because the target path is Unix socket deployment. Existing unrelated test failures may block a clean run; mitigation is to report exact failures without masking them. |
| Decision points | Stop for Owner OCAW if mTLS activation requires `Cargo.toml` dependency or feature changes, if implementation must touch high-risk files, or if deleting fallback code would remove the mimicry path rather than only the backup path. |
| Pre-execution checklist | Confirm no reference-project source is read; read D1-D5 decision doc and prior transport plan; inspect route-client code/tests; grep for fallback identifiers in `exploratory/rust-core-gateway/`; implement only within allowed Rust exploratory scope; run formatter if code changes; run requested cargo test with target dir outside `/tmp`. |
| Concrete execution order | 1. Inspect existing route transport helpers and current tests. 2. Search for `hyper.*rustls`, `HyperRustlsClient`, and `fallback_client`. 3. Replace the baseline model with `TransportBaseline::{Uds, Mtls}` carrying concrete paths and validation. 4. Add UDS permission/ownership validation and mTLS fail-fast helper tests. 5. Remove fallback code or stale tests while leaving a D3 burn-the-boats comment at the deletion boundary. 6. Run `cargo fmt` and the requested cargo test. 7. Report status, diff stat, test output summary, and required source tail. |

## Clean-room note

This lane reads HUAKAI-owned files only. It must not read `sub2api`, `new-api`, `portkey`, `helicone`, `litellm`, `all-api-hub`, or `envoy-ai-gateway` source.

## Owner-facing risk note

The only anticipated Owner decision is if real `tonic::transport::ClientTlsConfig` construction is unavailable behind current crate features. The hard constraint forbids `Cargo.toml` edits, so this lane will stop rather than add a dependency or feature flag.
