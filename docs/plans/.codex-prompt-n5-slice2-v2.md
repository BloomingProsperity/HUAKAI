# Codex Round-2 Plan Refinement — N+5 Slice 2 (Model Registry)

## Context

Round 1 produced two independent plans (Claude + Codex). Owner reviewed and asked for a SECOND round that **integrates reference-project (借鉴项目) feature-implementation patterns** on top of the round-1 work.

Round 1 surfaced 4 material conflicts: D2 (cache), D3 (tenant scoping), D4 (HTTP code on disabled/unknown), D6 (snapshot version shape). Round 2's job is to re-arbitrate those USING evidence from reference projects (one-api / litellm / portkey / envoy-ai-gateway / + already-mined evidence on sub2api / new-api / all-api-hub / helicone), AND surface new decision points the round-1 plans missed.

## Lane declaration (per CLAUDE.md #11)

- **Lane**: implementer (round-2 reads only MIT references and already-mined evidence rows; no fresh non-MIT source reads).
- **Counterpart**: Claude is drafting `docs/plans/2026-04-30-n5-model-registry-claude-v2.md` IN PARALLEL — do NOT read it.
- **Output target**: `docs/plans/2026-04-30-n5-model-registry-codex-v2.md` (relative to repo root `c:\HUAKAI\repo`).
- **Round-1 lane**: you were specifier; round-2 stays implementer per HUAKAI clean-room (MIT-only fresh reads + behavior-only paraphrase from prior evidence rows for non-MIT).

## Hard prohibitions (clean-room)

- MIT projects you MAY read source for: songquanpeng/one-api, BerriAI/litellm (excluding `enterprise/`), Portkey-AI/gateway, envoyproxy/ai-gateway.
- non-MIT projects you MUST NOT freshly read: sub2api (LGPL), QuantumNous/new-api (AGPL), qixing-jk/all-api-hub (AGPL), Helicone/ai-gateway (GPL). For these, USE ONLY existing evidence rows in `docs/07_REFERENCE_EVIDENCE_LEDGER.md` (E-NAI-*, E-OAI-*, etc).
- NEVER copy verbatim function names, struct fields, schema column names, comments, file paths from any reference (MIT or non-MIT).
- NEVER do line-by-line algorithmic translation.
- ALWAYS paraphrase behavior in your own sentence structure.
- For each MIT source file you read: cite the file path + commit/tag at the closing requirement.

## Closing requirement

End your file with:

```
Source files read:
  HUAKAI repo: <list>
  Reference (MIT): <list with repo + path + commit/tag>
  Reference (already-mined evidence rows): E-OAI-XXX, E-LM-XXX, E-PK-XXX, E-NAI-XXX (only IDs)
Clean-room declaration: "I read MIT reference source for behavior extraction only.
  I did NOT read source from sub2api / new-api / all-api-hub / helicone.
  I did not copy verbatim names, schemas, comments, or algorithms."
Lane: implementer (round 2)
Agent: Codex
UTC timestamp: <RFC3339>
```

## Required deliverables

### 1. Reference-pattern grid (table)

For each decision point from round 1 (D1–D8) + any new decision points you propose, fill a row:

| DX | round-1 Claude pick | round-1 Codex pick | one-api pattern | litellm pattern | portkey pattern | envoy-ai-gateway pattern | sub2api/new-api evidence (id only) | round-2 pick + 1-line reasoning grounded in patterns |

The "pattern" cells should be 1–2 sentences each, citing source file paths or evidence IDs. Empty cells when pattern doesn't apply (state "n/a").

### 2. Behavior obligations missed by round-1 plans

For each MIT reference (one-api / litellm / portkey / envoy-ai-gateway), list 3–5 specific behavior obligations relevant to a Model Registry that round-1 plans didn't address. Examples:

- one-api: per-channel model rename — `gpt-4` from public surface maps to `gpt-4-turbo` upstream-side.
- litellm: per-deployment rpm/tpm caps live at the registry layer, not at the pool layer.
- portkey: target-list with weights for active load-balance vs ordered fallback.
- envoy-ai-gateway: declarative LLMRoute CRD as upstream of routing decisions.

For each obligation: describe how HUAKAI's Slice-2 schema would absorb it (or defer it). Cite source path + commit/tag.

### 3. Re-arbitrate the 4 conflict points with reference evidence

For each of D2/D3/D4/D6: state your round-2 pick AND cite the reference patterns that pushed you that way. If you change from your round-1 pick, say so explicitly.

### 4. New decision points uncovered by reference-pattern review

At least 3 new decision points (D9, D10, D11+). Examples to consider (not mandatory — choose what reference patterns actually surface):

- Per-deployment rpm/tpm caps: store at registry layer (model_registry side) or at pool layer (provider_accounts side)?
- Traffic split weights vs strict priority ordering for pool candidates: which mode is default?
- Per-binding model rename (`public_model` → `provider_model`): allow override at binding level vs only at model level?
- Health-aware degradation: when a binding's pool is `failed`, should ResolveModel still surface it (and let Router skip), or filter at registry?
- Auto-disable threshold (E-OAI-009/013-derived): does registry track per-model success rates or stay pure metadata?
- Cache invalidation event source for D2-cache: scheduler_outbox row vs registry-snapshots-version-bump?

For each: state default + reasoning + reference citation.

### 5. Schema impact

Show the schema deltas to your round-1 5-table proposal that the new decision points imply. Use SQL DDL fragments. Be explicit about WHICH new columns / tables / indexes / FK constraints are added.

### 6. CMB invariant audit (CMB-1, CMB-2, CMB-7)

Walk each CMB invariant and prove no violation. Special attention: rpm/tpm caps if added — does that violate "Pool does not compute cost" (CMB-2) or stay safely in metadata-only territory?

### 7. Risk matrix expansion

Add 4+ new risks from reference-pattern absorption. Examples to seed:

- Two MIT references describe per-model rate limits at different layers (deployment vs account); HUAKAI must pick one or unify, else dual-source-of-truth bug.
- Reference patterns assume runtime config reload (litellm hot-reload); HUAKAI L0 doesn't have one.
- portkey "weighted target" pattern requires deterministic hashing for stickiness; HUAKAI's existing sticky binding lives in pool layer.

For each risk: probability (low/med/high), blast radius (per-tenant / global), mitigation.

### 8. Final synthesis recommendation

The "if Owner says by-you-decide, here's what I'd ship" version. Concrete code structure, schema, sequencing, and what gets deferred to N+6 / Phase E.

### 9. What feature-parity items now land in PARITY MATRIX

Slice 2 will absorb some reference-derived parity items. List them: which `F-*` capability ids in `docs/03_FEATURE_PARITY_MATRIX.md` get marked `Designed` or `Implemented` after Slice 2 ships. (Do NOT modify the parity matrix file itself; Claude/synthesis will do that as a separate commit.)

## Plan length target

~15–20 KB markdown. Be deep, don't summarize. If a section is short, you missed something.

## Inputs you must read (in this order)

1. `c:\HUAKAI\repo\docs\plans\2026-04-30-n5-model-registry-claude.md` — Claude round 1
2. `c:\HUAKAI\repo\docs\plans\2026-04-30-n5-model-registry-codex.md` — your round 1
3. `c:\HUAKAI\repo\docs\07_REFERENCE_EVIDENCE_LEDGER.md` — pre-mined evidence (esp. E-OAI-001 to E-OAI-015, E-LM-001 to E-LM-006, E-PK-001 to E-PK-008, E-NAI-001+)
4. `c:\HUAKAI\repo\docs\06_REFERENCE_PROJECTS.md` — license tier table
5. `c:\HUAKAI\repo\docs\05_CLEAN_ROOM_POLICY.md` — lane discipline
6. `c:\HUAKAI\repo\docs\02_HUAKAI_FUSION_ARCHITECTURE.md` — 3-tier blueprint, 8-souls fusion table
7. `c:\HUAKAI\repo\docs\specs\_invariants\cross-module-boundaries.md` — CMB-1/2/7

For MIT-source reads: use Web search / git clone / GitHub raw fetch as needed. Anchor on these areas:

- one-api: `model/channel.go`, `model/ability.go`, `relay/channel/*` — channel-model mapping, model rename
- litellm: `litellm/router.py`, `litellm/proxy/_types.py` — model_alias_map, model_list, fallbacks
- portkey: `src/middlewares/cache/`, `src/handlers/handlerUtils.ts` — config schema, target/weight, fallback
- envoy-ai-gateway: `api/v1alpha1/*` CRD types, `internal/extproc/*` — declarative route → backend resolution

If GitHub fetches fail, prefer existing evidence rows from the ledger.

## Process

1. Read v1 plans + ledger + clean-room policy.
2. Read MIT reference source as needed (Web / fetch / clone).
3. Write `docs/plans/2026-04-30-n5-model-registry-codex-v2.md` (path relative to repo root, NOT to current cwd).
4. Do NOT git commit.
5. Do NOT read `2026-04-30-n5-model-registry-claude-v2.md`.

## Output path note

The repo lives at `c:\HUAKAI\repo`. Your CWD may default to `c:\HUAKAI` (parent). Write the file to `c:\HUAKAI\repo\docs\plans\2026-04-30-n5-model-registry-codex-v2.md` (use absolute path to avoid the same path-resolution surprise as round 1).
