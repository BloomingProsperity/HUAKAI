This file is agent-facing and authoritative.

# Acceptance Test Matrix

## Purpose

Acceptance tests prove that feature parity, safety, and operational workflows are real.

## Mined Test Directions — Phase 1 L1 MVP

First Codex-authored batch (2026-04-28). Each row covers normal path, failure path, and operator recovery in the Steps + Expected Result columns. Source: Codex via `omc ask codex`, raw artifact retained under `.omc/artifacts/ask/` (gitignored). Codex notes captured in [the Phase 1 follow-ups](#phase-1-codex-notes-and-follow-ups) below.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-GW-001 | Client OpenAI-compatible request routes via Route -> Channel -> Provider Account. | F-GW-001 (E-OAI-001) | A User has an active API Key, available Quota, a matching Route, an enabled Channel, and an active Provider Account; an operator can change Route or Channel state. | Normal: send one OpenAI-compatible request with the API Key. Failure: disable the selected Provider Account or remove the model from the Channel allow-list and repeat. Recovery: operator restores the Provider Account or repairs the Route and Channel mapping, then repeats the request. | Normal: request resolves API Key -> User -> User Group -> Quota -> Route -> Channel -> Provider Account and creates one Usage Record. Failure: request is not sent to the wrong Provider Account and returns an actionable error without exposing upstream credentials. Recovery: request succeeds after operator change and the change has an Audit Event. | Misrouting, disabled Provider Account routing, model exposure error, missing request traceability. | Planned |
| AT-GW-002 | Streaming and non-streaming both produce one final Usage Record with consistent token accounting. | F-GW-002 (E-OAI-002) | Same User, API Key, Route, Channel, Provider Account, model, and prompt are available for streaming and non-streaming requests. | Normal: send equivalent non-streaming and streaming requests. Failure: interrupt a streaming response after partial completion. Recovery: operator investigates the request id and reruns the same request after the interruption source is cleared. | Normal: each request creates exactly one final Usage Record with consistent request token and completion token accounting. Failure: interrupted stream creates one final failed Usage Record with partial usage evidence and no duplicate Billing Ledger charge. Recovery: rerun creates a new Usage Record and does not mutate the earlier Usage Record. | Streaming accounting drift, duplicate billing, mutable usage history. | Planned |
| AT-GW-004 | Failed request retries with exponential backoff and emits operator-visible fallback reason. | F-GW-004 (E-OAI-002, E-LM-002, E-PK-001, E-PK-002) | Route has a primary Channel and fallback Channel; primary Provider Account can be forced to fail; fallback Provider Account is active; User Quota is available. | Normal: send a request while primary Channel is healthy. Failure: force primary failure and observe retry, backoff, and fallback. Recovery: operator reviews fallback reason, pauses or repairs the failing Channel or Provider Account, then repeats the request. | Normal: request uses primary Channel and creates one Usage Record. Failure: retries follow configured backoff, fallback reason is operator-visible, no duplicate Billing Ledger charge is created, and final Usage Record shows final Channel and Provider Account. Recovery: repaired path succeeds and operator state change has an Audit Event. | Retry cost amplification, silent fallback, fallback to exhausted Provider Account, duplicate usage. | Planned |
| AT-AUTH-001 | Email/password login and one OAuth identity source authenticate the User against the same User entity. | F-AUTH-001 (E-OAI-003) | One User exists with email/password credentials and one linked OAuth identity source; duplicate unverified identity data can be simulated. | Normal: sign in with email/password, sign out, then sign in with OAuth. Failure: attempt wrong password and attempt OAuth login with unverified or unlinked identity data. Recovery: User completes verified linking or password reset and signs in again. | Normal: both login paths resolve to the same User and do not create a duplicate User. Failure: invalid credentials fail without merging identities or exposing account state beyond safe error text. Recovery: verified linking or reset succeeds and any operator-assisted change has an Audit Event. | Duplicate User creation, account takeover through OAuth collision, hardcoded auth path. | Planned |
| AT-AUTH-002 | Session survives server restart and works across two instances when SESSION_SECRET is shared and operator-supplied. | F-AUTH-002 (E-OAI-004) | Two instances share the same operator-supplied SESSION_SECRET and session store; a third instance can be started with missing or different SESSION_SECRET. | Normal: User signs in on instance A, accesses instance B, restart both instances, and access again. Failure: access the same session through instance C with missing or different SESSION_SECRET. Recovery: operator supplies the shared SESSION_SECRET to instance C and restarts it. | Normal: session remains valid across restart and both shared-secret instances. Failure: instance with missing or different SESSION_SECRET rejects the session and exposes an operator-visible configuration error; no default secret is generated. Recovery: instance C accepts the existing session after configuration is corrected. | Default secret generation, broken horizontal scale, session loss on restart. | Planned |
| AT-KEY-001 | API Key lifecycle: issue, rotate, disable, expire; per-key Quota reconciles against User account balance. | F-KEY-001 (E-OAI-005) | User has available balance from Billing Ledger, one active API Key can be issued with expiration and per-key Quota, and a working Route exists. | Normal: issue API Key, use it, rotate it, and use the rotated API Key. Failure: use old rotated value, disabled value, expired value, and active value after per-key Quota is exhausted. Recovery: operator issues replacement or raises per-key Quota and reconciles against User balance. | Normal: only active unexpired API Key values work and each successful request creates one Usage Record. Failure: old, disabled, expired, or exhausted API Key is rejected before Provider Account use and without spending User balance. Recovery: replacement or Quota change works, reconciles with Billing Ledger, and lifecycle changes have Audit Events. | Leaked old API Key reuse, balance OK but key out confusion, quota overspend, missing audit. | Planned |
| AT-CH-001 | Channel CRUD plus per-channel model allow-list plus bulk-create produces one audit per Channel and one bulk summary. | F-CH-001 (E-OAI-008) | Operator has permission; Provider Accounts exist; valid and invalid model allow-list inputs are prepared. | Normal: create, read, update, delete, and bulk-create Channels with model allow-lists. Failure: include invalid model allow-list entries or disabled Provider Account references in a bulk create. Recovery: operator corrects invalid rows and reruns only the failed subset. | Normal: Channel state and model allow-lists match requested changes, allowed models route, and disallowed models are blocked. Failure: invalid rows fail with actionable errors while valid handling is deterministic. Recovery: corrected subset succeeds; every Channel mutation has one Audit Event and each bulk action has one bulk-summary Audit Event. | Bulk action without audit, accidental model exposure, invalid Channel configuration. | Planned |
| AT-TIMEOUT-001 | Per-request timeout cuts long requests; cut emits Usage Record with timeout cause and alert. | F-TIMEOUT-001 (E-PK-007) | Operator-configured timeout exists for the model; Route, Channel, Provider Account, User, API Key, and Quota are available; alert surface is enabled. | Normal: send a request that completes before timeout. Failure: send a request that exceeds the configured timeout. Recovery: operator increases model timeout or routes to a faster Channel and retries. | Normal: request succeeds and creates one Usage Record. Failure: long request is cut within configured bound, returns actionable timeout error, creates one Usage Record with timeout cause, reconciles Quota and Billing Ledger without overcharge, and raises an operator-visible alert. Recovery: adjusted policy or Route succeeds and operator change has an Audit Event. | Hung requests, aggressive timeout cutting valid work, untracked timeout spend, missing alert. | Planned |
| AT-SEC-001 | Per-IP rate limit blocks bursts beyond operator-configured threshold. | F-SEC-001 (E-OAI-014) | Operator-configured per-IP threshold is set; User and API Key are valid; at least two source IPs are available for comparison. | Normal: send requests below threshold from one IP and from a second IP. Failure: burst beyond threshold from the first IP. Recovery: operator reviews evidence, changes threshold or waits for the window to clear, then retries. | Normal: below-threshold requests proceed normally. Failure: excess requests from the first IP are blocked before Provider Account use, do not consume Quota, and produce operator-visible evidence; second IP is not blocked by the first IP. Recovery: request succeeds after threshold change or window clear, and operator threshold change has an Audit Event. | Abuse bursts, global over-blocking, quota burn from blocked traffic, unobservable security events. | Planned |
| AT-SEC-002 | First-run admin bootstrap forces credential change before any other privileged operation succeeds. | F-SEC-002 (E-OAI-015) | Fresh installation is in first-run bootstrap state; privileged Channel or User operation is available for verification. | Normal: bootstrap User signs in and attempts credential change before any other privileged operation. Failure: attempt privileged operation before credential change and attempt any known default credential. Recovery: bootstrap User changes credential and repeats the privileged operation. | Normal: credential change is allowed as the first privileged action. Failure: all other privileged operations are blocked until change completes and no hardcoded default credential works. Recovery: new credential works, old credential is invalid, privileged operation succeeds, and bootstrap plus operation changes have Audit Events. | Default credential exposure, first-run takeover, unaudited privileged operation. | Planned |
| AT-POOL-001 | One Pooling Group aggregates multiple Provider Accounts into one logical capacity; routing selects an Account inside the Pool; depletion and disablement propagate cleanly. | F-POOL-001 (E-S2A-001) | At least two Provider Accounts with valid credentials are members of one Pooling Group; one Route resolves to that Pool; one User has active API Key and Quota; one Account inside the Pool can be forced to a depleted or disabled state. | Normal: send a request that resolves to the Pool. Failure: disable one Account, then exhaust the remaining Account, then send a request. Recovery: operator re-enables an Account or adds a new Account to the Pool, then repeats the request. | Normal: Pool selects an Account per policy and creates one Usage Record carrying Pool id, chosen Account id, and routing reason. Failure: disabled Account is excluded from selection; full Pool depletion returns an actionable error without leaking upstream credentials and creates a Usage Record marked depleted. Recovery: repaired Pool succeeds and every Pool/Account state change has an Audit Event. | Pool routing surprise, account depletion mishandling, missing audit, upstream credential leak. | Planned |
| AT-MODE-001 | Edition flag toggles SaaS-only features off in Personal Edition while keeping the cost-ceiling alert active in every Edition. | F-MODE-001 (E-S2A-005) | Two deployments share one configuration baseline; one runs in Personal Edition, one in SaaS Edition; an Edition switch path exists; cost-ceiling threshold is configurable in both. | Normal: send a request to Personal Edition that uses only Personal-Edition features. Failure: attempt a SaaS-only operation (payment, tenant onboarding) in Personal Edition; then run Personal Edition without payment and exceed the cost-ceiling threshold. Recovery: operator switches the deployment to SaaS Edition; SaaS-only operations become available. | Normal: Personal Edition deployment does not expose SaaS-only endpoints or surfaces. Failure: SaaS-only call in Personal Edition returns an actionable rejection naming the Edition required; cost-ceiling breach raises an operator-visible alert in either Edition. Recovery: Edition switch is permitted, audited, and emits an Audit Event; SaaS-only endpoints become reachable after switch. | SaaS-feature leakage in Personal Edition, missing cost guardrail in Personal mode, silent Edition drift, unaudited Edition switch. | Planned |
| AT-SEC-005 | Operator-configurable allow/block list governs which upstream response headers are forwarded to clients; defaults strip sensitive headers. | F-SEC-005 (E-S2A-008) | One Channel and Provider Account that returns at least one known sensitive upstream header (e.g. upstream auth-token residue, internal-routing header); operator can edit the header allow/block list; a "currently exposing N headers" diagnostic surface is reachable. | Normal: send a request through the default-configured Channel. Failure: operator over-permits the allow list to forward all upstream headers; send a request; then revert. Recovery: operator restores secure defaults and re-verifies. | Normal: default policy strips known sensitive headers; client response carries only safe headers. Failure: over-permissive policy leaks headers but is surfaced by the operator-visible diagnostic. Recovery: secure defaults are restored; subsequent request strips again; every header-policy change emits an Audit Event. | Upstream auth-token leak, internal-routing-header leak, undetected over-permissive header policy, unaudited policy change. | Planned |

## Phase 1 Codex Notes and Follow-ups

Codex flagged four items while drafting the L1 directions above:

1. **AT-GW-004 sits closest to the Option C carve-out.** Retry / fallback paths intersect provider health, account-pool routing, and provider failover — all carve-out areas in [DR-000](decisions/DR-000-clean-room-methodology.md). Phase 2 must lock the local fields (fallback reason, retry-attempt count, account-health snapshot) before any test code is written, and the spec for these fields must use Option C lane discipline.
2. **AT-GW-002, AT-KEY-001, AT-TIMEOUT-001 require Phase 2 schema lock first.** All three depend on Usage Record, Quota, and Billing Ledger field commitments that have not been made yet. Acceptance test code for these can only be written after Phase 2.
3. **Glossary cleanup needed.** The original AT-AUTH-001 wording ("OAuth provider") collides with this project's `Provider` term ([18_GLOSSARY.md](18_GLOSSARY.md), upstream LLM service operator). The row above has been corrected to use "OAuth identity source". Apply the same convention everywhere downstream.
4. **Capability anchors missing in [02_CAPABILITY_CONTRACT.md](02_CAPABILITY_CONTRACT.md).** Three capabilities are implied by L1 MVP rows but not anchored explicitly: auth-provider abstraction (F-AUTH-001), operator-supplied SESSION_SECRET as a deployment requirement (F-AUTH-002), and first-run bootstrap credential rotation (F-SEC-002). These should be added to docs/02 in the next governance pass.

## Test Template

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-TBD | TBD | TBD | TBD | TBD | TBD | TBD | Planned |

## Required Test Groups

- Gateway routing and fallback.
- Protocol compatibility.
- Provider account lifecycle.
- Credential rotation and disablement.
- User key lifecycle.
- Quota enforcement.
- Billing and usage recording.
- Admin audit logs.
- Secret redaction.
- Observability and investigation workflow.
- Feature flags and plugin boundaries.
- UI operations workflows.

## F-CH-002 Channel Health Auto-Disable — Round 3 Spec Wave

Source: `docs/specs/channel-health-auto-disable.md`, `docs/decompositions/_cross-cutting/channel-health.md`, and `docs/plans/2026-05-16-f-ch-002-channel-health-codex.md`. These are acceptance outlines only; this docs wave does not implement backend code, schema, OpenAPI, or admin UI.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-CH-002-001 | F-CRED-001 finalization creates an active channel health subject. | F-CH-002 / F-CRED-001 | F-CRED-001 finalizes one credential for `(vendor, account_credential_id, credential_version)`; health policy exists; admin did not create it as manual-paused. | Finalize credential, query channel health state, send one normal request through selector. | Channel health subject exists for the credential version, starts `active`, is eligible for selection, and emits no false degradation. | Acquisition blocked by health module, missing default active state, credential-version ambiguity. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:43) |
| AT-CH-002-002 | Rolling error rate above policy threshold triggers cooldown. | F-CH-002 | One active channel has enough samples to satisfy `min_sample_count`; policy sets a test error-rate threshold and cooldown duration; at least one alternate healthy channel exists. | Send successes and failures so `error_rate > error_rate_threshold_pct` over the policy window. | Channel transitions to `cooling_down`, `cooldown_until` is set, selector excludes it, `channel_health_degraded` and `channel_disabled` audit events are emitted. | Silent failure accumulation, overuse of failing channel, missing audit on auto-disable. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:61) |
| AT-CH-002-003 | Upstream 5xx spike degrades first and cools down only under repeated policy breach. | F-CH-002 / F-RATE-001 | Active channel has upstream 5xx outcomes normalized as upstream failures; local gateway 5xx can also be simulated. | Produce one threshold-crossing upstream 5xx window, then a second threshold-crossing window; compare with local gateway 5xx. | First upstream 5xx breach moves channel to `degraded`; repeated breach moves it to `cooling_down`; local gateway 5xx does not count as upstream 5xx. | Permanent disable from ambiguous transient failure, local/upstream failure conflation. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:80) |
| AT-CH-002-004 | Rate-limit hit rate triggers cooldown until the next rate window. | F-CH-002 / F-RATE-001 | F-RATE-001 emits normalized 429/403 rate-limit outcomes and one concrete reset timestamp; health policy has rate-limit threshold. | Send attempts until `rate_limit_hit_rate` crosses threshold. | Channel enters `cooling_down`; `cooldown_until` follows the F-RATE reset timestamp when present, otherwise default policy cooldown; selector fails over. | Under-cooldown burns upstream quota, over-cooldown wastes capacity, duplicated rate-limit parsing. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:115) |
| AT-CH-002-005 | Ban signal triggers 24-72 hour disabled state and admin alert. | F-CH-002 / F-AUTH-005 | Active channel receives a safe normalized ban-signal class such as `account_suspended` or `token_revoked`; policy cooldown range is 24-72 hours. | Emit ban-signal event, inspect health state, audit, and alert surfaces. | Channel transitions to `disabled`, cooldown is inside the 24-72 hour policy range, admin alert is created, audit payload includes reason class but no raw upstream text or token material. | Raw upstream text leakage, serious account failure hidden as transient cooldown, missing operator alert. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:131) |
| AT-CH-002-006 | Multi-account pool failover skips a cooled channel and selects the next healthy channel. | F-CH-002 / F-POOL-001 | Pool has at least two eligible channels; primary channel is moved to `cooling_down`; secondary remains `active`. | Send request through normal route selection. | Selector excludes cooled primary, selects healthy secondary, Usage Record/routing reason identifies failover reason, no disabled channel receives traffic. | Single channel ban taking down pool, routing to disabled channel, invisible failover. | PASS (commit 7770e8c + backend/internal/channelhealth/failover_test.go:27) |
| AT-CH-002-007 | Cooldown expiry starts gradual recovery ramp through 1%, 10%, 50%, and 100%. | F-CH-002 | Channel is `cooling_down`; `cooldown_until` has elapsed; policy stage minimums are satisfied by test traffic/probes. | Advance time and feed clean signals through each ramp stage. | State becomes `ramping` at 1%, then 10%, 50%, 100%, then `active`; `channel_ramp_started` and `channel_recovered` are audited. | Immediate full re-enable causing re-flap, missing recovery audit. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:156) |
| AT-CH-002-008 | Ramp-stage failure rolls back to cooldown. | F-CH-002 | Channel is `ramping` at 10% or 50%; policy has ramp rollback threshold. | Feed enough failures or rate-limit outcomes to exceed the ramp threshold. | Channel returns to `cooling_down`, cooldown backoff is applied, selector excludes it, ramp failure reason is visible in audit/admin state. | Flapping channel restored too quickly, failed ramp hidden from operators. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:156) |
| AT-CH-002-009 | Health state is isolated across vendors and credential versions. | F-CH-002 | Two vendors and two credential versions exist under the same tenant; one old credential version is degraded. | Degrade `(vendor A, credential v1)`, then inspect `(vendor A, credential v2)` and `(vendor B, credential v1)` states and selection eligibility. | Only the targeted channel subject changes state; other vendor/version pairs remain eligible; audit references the exact credential version. | Cross-vendor contamination, new credential poisoned by old version history. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:211) |
| AT-CH-002-010 | Admin manual pause, resume into ramp, and force-active are audited and do not mutate credentials. | F-CH-002 / F-AUTH-005 | Operator has permission; channel is active; credential row exists. | Manual pause with reason, resume into ramp with reason, attempt force-active as authorized and unauthorized actors. | Manual pause sets `manual_paused` and blocks selection; resume starts ramp at 1%; unauthorized force-active is rejected; authorized force-active emits high-severity audit; credential bytes are unchanged. | Unaudited override, automatic clearing of manual pause, health module altering secrets. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:237 + backend/internal/gatewayhttp/channel_health_admin_handler_test.go:46) |
| AT-CH-002-011 | F-TRUST audit chain records degraded, disabled, and recovered without secrets. | F-CH-002 / F-TRUST | Test inputs include token-shaped strings in raw upstream error text and normal channel identifiers. | Trigger degradation, disable, ramp, recovery, and manual override; inspect audit payloads. | Audit contains tenant id, channel id, vendor, credential id/version, state transition, reason class, policy version, and window summary; no token/API key/cookie/raw upstream body appears. | Secret leakage, missing chain of custody, non-replayable operator investigation. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:131 + backend/internal/channelhealth/service_test.go:156) |
| AT-CH-002-012 | Sample floor prevents low-traffic flapping and all-unhealthy state surfaces explicitly. | F-CH-002 / F-POOL-001 | One low-traffic channel has fewer than `min_sample_count` attempts; another test pool has all channels degraded/cooling. | Send a single failure below sample floor; separately put every channel in a pool into degraded/cooling states and send a request. | Single low-volume failure does not auto-cooldown; all-unhealthy pool returns operator-visible `all_channels_degraded` or `pool_exhausted` rather than silently routing to disabled channel. | One-off failures disabling scarce accounts, hidden total pool outage, last-resort ambiguity. | PASS (commit 7770e8c + backend/internal/channelhealth/failover_test.go:27 + backend/internal/channelhealth/failover_test.go:90) |
| AT-CH-002-013 | Latency P99 spike triggers degradation cooldown and recovers when latency normalizes (not permanently disabled). | F-CH-002 | One active channel with stable success traffic; policy sets `latency_p99_threshold_ms` and cooldown duration; alternate healthy channel exists. | Inject high-latency successful responses crossing `latency_p99_threshold_ms` over policy window; verify state + audit; then return latency to normal across the cooldown window. | Channel transitions to `cooling_down` with `cooldown_until` set due to latency policy; `channel_health_degraded` audit includes `reason_class=latency_p99`; latency-only triggered cooldown does not escalate to `disabled`; after latency normalizes channel re-enters ramp and recovers. | Permanent disable from transient latency spike, missing latency-specific cooldown reason, latency conflated with error rate or ban signal. | PASS (commit 7770e8c + backend/internal/channelhealth/service_test.go:264) |

## F-CRED-001 Credential Acquisition — Phase A

Source: `docs/specs/credential-acquisition.md`, `docs/plans/2026-05-15-f-cred-001-acquisition-codex.md`, and `docs/plans/2026-05-15-f-cred-001-synthesis-codex.md`. These are acceptance outlines; Phase A Go tests are mock-only scaffolds.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-CRED-001-001 | Browser OAuth happy path for `openai/chatgpt_oauth`. | F-CRED-001 | Admin is authorized; target Provider Account exists; OAuth client identity source is available; fake exchange succeeds. | Start OAuth flow, receive callback with matching state, exchange code, preview redacted metadata, finalize. | One F-AUTH-005 credential is created; flow is finalized once; started/completed audit events contain no token bytes. | Broken OAuth acquisition, missing finalizer handoff, token leakage. | Planned |
| AT-CRED-001-002 | OAuth callback state mismatch is rejected and audited. | F-CRED-001 | OAuth flow exists with hashed state and 10-minute TTL. | Send callback with wrong state. | Code exchange does not run; no credential row is created; failed audit reason is `state_mismatch`. | CSRF/callback forgery, wrong account finalization. | Planned |
| AT-CRED-001-003 | `anthropic/claude_ai_oauth` cookie bootstrap never stores raw cookie in flow state. | F-CRED-001 | Admin provides browser-session bootstrap input. | Start bootstrap, validate metadata, inspect flow and audit payloads. | Flow context stores only redacted hashes/labels; raw cookie/session value is absent from flow and audit. | Cookie leakage, long-lived secret persistence. | Planned |
| AT-CRED-001-004 | `openai/codex_cli_oauth` import from uploaded CLI auth content creates encrypted credential. | F-CRED-001 | Admin has uploaded or pasted CLI auth content; server has no local file read permission. | Submit CLI import, parse content, preview redacted identity hints, finalize. | F-AUTH-005 accepts payload; audit records upload/import outcome; no workstation path is read. | Unsafe local file access, parser gap, secret leakage. | Planned |
| AT-CRED-001-005 | `anthropic/claude_code` CLI import uses the same parser boundary and does not read local path. | F-CRED-001 | Admin provides uploaded/pasted content and a path hint only for operator guidance. | Submit import using path hint plus content. | Server ignores path as a read target; only request body is parsed; credential finalizes or fails with redacted parser error. | Ambient filesystem authority, hidden local-agent behavior. | Planned |
| AT-CRED-001-006 | Bedrock manual SigV4 save stays supported. | F-CRED-001 | Admin has AWS access key id, secret key, and region for `anthropic/bedrock`. | Submit paste/manual flow and finalize. | Payload validates through F-AUTH-005 Bedrock handler; audit shows `credentials_present=true` only. | Manual path regression, AWS secret logging. | Planned |
| AT-CRED-001-007 | Bedrock STS bootstrap mock produces short-lived payload and refresh window. | F-CRED-001 | STS bootstrap is enabled in test config; fake STS returns temporary material. | Submit role/external-id bootstrap input, run fake exchange, finalize. | Temporary payload includes expiry/refresh-before metadata; raw AWS material is encrypted only by F-AUTH-005. | Cloud bootstrap drift, stale temporary credentials. | Planned |
| AT-CRED-001-008 | Vertex service-account upload validates and ignores uploaded token endpoint. | F-CRED-001 | Admin uploads service-account JSON with project/client fields and a malicious token endpoint field. | Submit JSON import, validate, finalize. | Server uses HUAKAI-controlled token endpoint policy; uploaded endpoint is ignored; audit is redacted. | SSRF/token endpoint injection, service-account private key leak. | Planned |
| AT-CRED-001-009 | `gemini/code_assist` stores project metadata only in redacted context/audit. | F-CRED-001 | Fake OAuth exchange returns project metadata and token material. | Start flow, callback, preview, finalize. | Project id/status may appear redacted or hashed; token/session values do not appear in audit or flow context. | Project metadata leakage, token leakage. | Planned |
| AT-CRED-001-010 | `gemini/google_one` stores tier metadata without exposing token bytes. | F-CRED-001 | Fake exchange returns subscription tier and credential material. | Complete flow and inspect final audit. | Tier metadata is normalized/redacted; audit contains no credential bytes. | Tier drift, secret leakage in metadata. | Planned |
| AT-CRED-001-011 | `gemini/antigravity` dedicated metadata failure does not leak secrets and marks operator attention. | F-CRED-001 | Fake Antigravity exchange succeeds but project/plan probe fails transiently. | Complete acquisition through dedicated mode adapter. | Credential may finalize per policy; metadata is marked stale/operator-attention; failure audit is token-free. | Dedicated mode collapse into generic Gemini, metadata blind success. | Planned |
| AT-CRED-001-012 | API key paste emits credential-created audit with presence flag only. | F-CRED-001 | Admin submits API key for a supported `api_key` mode. | Submit paste flow and finalize. | Audit payload uses `credentials_present=true` and omits the key value and key-shaped fragments. | API key exposure in logs/audit. | Planned |
| AT-CRED-001-013 | Token endpoint exchange preserves refresh-token rotation rule after F-AUTH-005 refresh. | F-CRED-001 / F-AUTH-005 | Admin submits refresh-token flow; fake future refresh response may omit replacement refresh token. | Finalize initial token exchange, then run refresh adapter test. | Existing refresh token is preserved when replacement is absent; new token replaces only when present. | Losing refresh token, breaking subsequent refresh. | Planned |
| AT-CRED-001-014 | Concurrent finalize replay creates exactly one credential. | F-CRED-001 | Validated flow exists; two finalize attempts share one idempotency key. | Race two goroutines against the same finalize flow with a shared start barrier. | Exactly one `credentialstore.Create` call succeeds; both callers receive same redacted result/status. | Duplicate credential rows, callback replay spend. | Planned |
| AT-CRED-001-015 | All 15 mode cells have implemented acquisition path or explicit manual-first path. | F-CRED-001 / F-AUTH-005 | F-AUTH-005 handler registry is available. | Enumerate registry modes and compare against F-CRED ModePlan table. | Every mode maps to a flow kind, client identity policy, and finalizer expectation. | Silent mode drop, partial parity claim. | Planned |
| AT-CRED-001-016 | ChatGPT OAuth finalization performs account metadata enrichment and privacy action according to tenant policy. | F-CRED-001 | Tenant policy is non-blocking by default; fake metadata/privacy calls are configured. | Complete OAuth, run metadata enrichment and privacy action. | Credential finalizes; metadata is redacted; privacy action outcome is audited; strict policy may block in future config. | Hidden privacy side effect, finalization blocked by optional mutation. | Planned |
| AT-CRED-001-017 | Gemini acquisition/refresh receives 8 canonical and 7 legacy tier labels. | F-CRED-001 / F-AUTH-005 | Tier normalizer test table is available. | Feed all known tier labels and one unknown. | Known labels map to HUAKAI canonical tiers; unknown becomes explicit `unknown` and never higher privilege. | Rate-limit tier drift, unsafe default tier. | Planned |
| AT-CRED-001-018 | Gemini refresh gets a client-mismatch error for the stored client family. | F-CRED-001 / F-AUTH-005 | Compatibility matrix allows exactly one alternate family. | Trigger mismatch, run bounded fallback. | One approved alternate attempt is audited; disallowed pairs fail with reauthorization-needed status. | Unbounded fallback, silent wrong-client refresh. | Planned |
| AT-CRED-001-019 | Gemini background refresh runs twice within 24 hours after tier probe. | F-CRED-001 / F-AUTH-005 | Tier cache timestamp exists. | Run refresh twice inside TTL, then once after TTL. | Fresh cache suppresses tier probe; stale cache re-probes; audit records cache decision. | Excess upstream calls, stale tier metadata. | Planned |
| AT-CRED-001-020 | Antigravity background refresh succeeds while project metadata lookup fails transiently. | F-CRED-001 / F-AUTH-005 | Existing credential has previous project metadata. | Run refresh with successful token refresh and failing metadata probe. | Access material updates; previous metadata remains; account is metadata-stale/operator-attention. | Metadata loss, false healthy status. | Planned |
| AT-CRED-001-021 | Admin imports Antigravity refresh token without browser OAuth. | F-CRED-001 | Admin submits refresh token; fake dry-run validates token and metadata. | Submit paste/token flow, dry-run, preview, finalize. | Dry-run validates required pieces; finalizer stores encrypted payload; audit is redacted. | Raw refresh-token endpoint exposure, no preview. | Planned |
| AT-CRED-001-022 | Long-lived Anthropic automation-token feature flag is disabled by default. | F-CRED-001 / future F-AUTH-006 | Admin requests setup-token flow while flag is off. | Submit `setup_token` acquisition request. | Request is rejected with feature-flag/roadmap message; no credential row; future enabled path must verify warning/expiry/audit. | Long-lived token theft, accidental ToS-risk enablement. | Planned |
| AT-CRED-001-023 | OpenAI-style OAuth account has missing expiry metadata and rate-limit account state. | F-CRED-001 / F-AUTH-005 | Credential exists with missing expiry and rate-limit signal. | Run refresh eligibility check. | One bounded refresh attempt is scheduled through storm controller; static API-key mode remains no-op. | Refresh storm, static key misclassification. | Planned |
| AT-CRED-001-024 | Claude bootstrap returns multiple org candidates including team and personal contexts. | F-CRED-001 | Fake bootstrap returns multiple org candidates. | Preview candidates and attempt finalize without confirmation, then with confirmation. | Finalize requires explicit selection; selected org metadata is stored redacted and audited. | Silent wrong-org selection. | Planned |
| AT-CRED-001-025 | Gemini project discovery cannot find project metadata through the first lookup path. | F-CRED-001 | Approved fallback list is configured. | Run project discovery with first path failing and later paths succeeding/failing. | Fallback attempts follow configured order; unresolved metadata returns operator-action status with no silent success. | Missing fallback matrix, hidden side effects. | Planned |
| AT-CRED-001-026 | S8 advisory-lock refresh race on the same OAuth credential. | F-CRED-001 / F-AUTH-005 | Two refresh workers target the same `account_credential_id`; F-AUTH-005 refresh lock is enabled. | Race two refresh attempts on the same account under `pg_advisory_xact_lock(hashtext('credential_refresh:' || account_credential_id::text))`. | Exactly one upstream refresh call occurs; the second worker waits, rereads the credential row after lock release, and uses the new token/version instead of refreshing again. | Refresh storm, duplicate upstream refresh, credential regression. | Planned |
| AT-AUTH-SESSION-001 | Project-level user auth roadmap checks refresh-token family invalidation and OAuth email local-account recovery. | F-AUTH-007 / F-SESSION-001 | User-auth/session slice is not part of F-CRED-001. | Review roadmap/spec before claiming parity. | F-CRED marks this out of scope with Mandatory Roadmap ownership; detailed coverage is split into AT-AUTH-007-001..010 and AT-SESSION-001-001..008. | Silent mixing of HUAKAI user login with upstream credential acquisition. | Superseded by detailed rows |

## F-AUTH-007 User Authentication — Phase 6 Commercial Foundation

Source: `docs/specs/user-authentication.md` and `docs/decompositions/_cross-cutting/user-auth-session.md`. These are acceptance outlines only; no backend/schema implementation is part of this docs wave.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-AUTH-007-001 | Email/password registration with required invite. | F-AUTH-007 | Tenant policy requires invite; one unexpired invite targets the submitted email; email delivery is configured. | Normal: register with matching invite. Failure: register with missing, expired, reused, or wrong-email invite. Recovery: admin issues a fresh invite and user repeats registration. | Normal creates pending User, consumes invite, sends verification, and emits user/invite audit. Failure creates no active User and no binding. Recovery succeeds with new invite and old failed code remains unusable. | Invite abuse, duplicate user creation, unaudited referral binding. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:14) |
| AT-AUTH-007-002 | Open registration without invite still records no false invite binding. | F-AUTH-007 | Tenant policy allows open registration; no invite is supplied. | Normal: register with email/password. Failure: attempt duplicate normalized email. Recovery: user verifies existing email or starts reset. | Normal creates one pending User with no invite binding and sends verification. Failure does not reveal whether the email exists beyond safe text and does not create a duplicate. | Account enumeration, phantom invite linkage, duplicate local User. | PASS (commit 7770e8c + backend/internal/gatewayhttp/auth_session_handler_test.go:21) |
| AT-AUTH-007-003 | Email verification activates pending User and rejects replay. | F-AUTH-007 | Pending User has one unexpired verification challenge. | Normal: submit valid verification token. Failure: submit same token again or an expired token. Recovery: request a new verification email. | Normal marks email verified and consumes challenge. Failure does not change state. Recovery issues a new token and audit trail links attempts without raw token. | Verification replay, stale pending users, token leakage. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:14 + backend/internal/gatewayhttp/auth_session_handler_test.go:21) |
| AT-AUTH-007-004 | Email/password login succeeds only for verified active User. | F-AUTH-007 / F-SESSION-001 | Verified active User has password credential; unverified and disabled Users also exist. | Normal: login with correct password. Failure: wrong password, unverified User, disabled User. Recovery: verify email, unlock/enable User, or reset password. | Normal returns platform session through F-SESSION-001. Failure creates no session and emits reason-class audit with safe User-facing text. Recovery path succeeds after the required state change. | Account takeover, unsafe errors, login before verification. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:14 + backend/internal/gatewayhttp/auth_session_handler_test.go:21) |
| AT-AUTH-007-005 | Login lockout and reset-required state are enforced. | F-AUTH-007 | User has configurable failed-login threshold and reset-required flag can be set. | Normal: login before threshold. Failure: exceed threshold and try correct password; set reset-required and try login. Recovery: password reset clears counters/state. | Lockout or reset-required state blocks session creation until recovery; audit records reason without password data. | Brute force, stale compromised password, missing audit. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:72) |
| AT-AUTH-007-006 | Google social login links only verified identity claims. | F-AUTH-007 / F-SESSION-001 | Google identity-source config exists; one existing User has matching verified email; one callback has unverified email. | Normal: callback with valid state/nonce and verified email links or logs into User per policy. Failure: unverified email or state mismatch. Recovery: user logs in with password and links identity from authenticated session. | Normal maps to exactly one HUAKAI User and creates platform session. Failure creates no User/link/session. Recovery creates audited link after local proof. | OAuth collision takeover, callback replay, duplicate User. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:113) |
| AT-AUTH-007-007 | GitHub social signup creates local User only when registration policy allows it. | F-AUTH-007 / F-SESSION-001 | GitHub identity-source config exists; social signup is enabled for one tenant and disabled for another. | Normal: new verified identity signs up in enabled tenant. Failure: same flow in disabled tenant. Recovery: admin invite enables signup or user uses existing account. | Enabled tenant creates verified/local User and session; disabled tenant rejects without storing upstream tokens. | Unauthorized signup, storing identity-source tokens, tenant policy bypass. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:113) |
| AT-AUTH-007-008 | Password reset consumes one token and revokes existing sessions. | F-AUTH-007 / F-SESSION-001 | User has two active session families and one reset challenge. | Normal: complete reset with valid token. Failure: replay token or use expired token. Recovery: request new reset and complete. | Normal changes password, consumes token, increments password/session guard, and calls F-SESSION-001 to revoke sessions. Failure leaves password unchanged. | Reset replay, old-session survival, account takeover. | PASS (commit 7770e8c + backend/internal/userauth/service_test.go:14 + backend/internal/usersession/rotation_test.go:251) |
| AT-AUTH-007-009 | Auth-sensitive social identity change triggers session policy. | F-AUTH-007 / F-SESSION-001 | User signs in through a linked Google identity and receives an active platform session; a tenant-scoped admin webhook reports provider-side identity change/disable. | Normal: allowed tenant operator posts `/v1/auth/social/identity-changed`. Failure: wrong-tenant operator posts the same webhook. Recovery: user signs in again after the provider-side issue is resolved. | Normal revokes the old session family with safe reason class `social_identity_provider_disabled`; old session and refresh token fail. Failure preserves the session and returns tenant-scope denial. | Stale trusted sessions, cross-tenant admin action, social identity takeover. | PASS (working tree + backend/internal/gatewayhttp/auth_session_handler_test.go:333) |
| AT-AUTH-007-010 | Auth audit and logs redact all secret material. | F-AUTH-007 / F-SESSION-001 / F-PRIV-001 | Test inputs include sentinel password strings, wrong password, cookie, verification token, reset token, session token, and refresh token. | Run register, verify, login success/failure, reset request/success/failure, cross-user refresh failure, and backend error echo; scan response audit surface, system log, UserAction-style event sink, F-TRUST decision_ref-style sink, and channel-health-style audit payload. | All structured sinks contain reason classes, ids, provider enums, and session policy only; no sentinel password, cookie, raw verification/reset/session/refresh token, or backend raw error appears. | Secret leakage, audit overcollection, raw backend error echo. | PASS (working tree + backend/internal/gatewayhttp/auth_session_handler_test.go:185) |
| AT-AUTH-007-011 | Same-tenant cross-user refresh-token use is rejected and revokes the stolen-token family. | F-AUTH-007 / F-SESSION-001 | User A and User B both have active sessions in the same tenant. User A has a valid bearer session token and submits User B's refresh token to `/v1/sessions/refresh`. | Failure: call refresh with User A bearer and User B refresh token. Recovery: User B logs in again after the revoked family is blocked. | Failure returns 401, emits `refresh_token_cross_user_attempt` without raw token material, and revokes User B's refresh family so it cannot keep rotating. Recovery creates a new family through normal login. | Account takeover, stolen refresh-token replay, missing audit after blocked abuse. | PASS (commit 158c421 + backend/internal/usersession/rotation_test.go:72 + backend/internal/gatewayhttp/auth_session_handler_test.go:133) |

## F-SESSION-001 Platform Session Management — Phase 6 Commercial Foundation

Source: `docs/specs/session-management.md` and `docs/decompositions/_cross-cutting/user-auth-session.md`. These tests target HUAKAI platform User sessions, not Provider Account sticky routing.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-SESSION-001-001 | Session create after successful local login. | F-SESSION-001 / F-AUTH-007 | Verified User authenticates through F-AUTH-007; PostgreSQL session store is available. | Login, create session family, receive session token and refresh token, inspect stored records. | Session family is active; only token hashes/fingerprints are stored; audit emits `session_created`; raw tokens are absent from logs. | Raw token storage, missing session audit, tenant leakage. | PASS (commit 7770e8c + backend/internal/gatewayhttp/auth_session_handler_test.go:21) |
| AT-SESSION-001-002 | Session token expiry and auto-refresh. | F-SESSION-001 | User has active session token near expiry and valid refresh token. | Validate before expiry, call refresh, then validate new session token after old token expires. | Refresh succeeds before expiry, new token works, old expired token fails, and refresh event is audited. | Session loss, accepting expired tokens, missing refresh audit. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:109) |
| AT-SESSION-001-003 | Refresh-token rotation rejects replay. | F-SESSION-001 | User has active family generation N. | Refresh once with current token, then replay the consumed token while also trying new token. | First refresh rotates to generation N+1; replayed old token triggers replay policy and no new token; new token behavior follows family state. | Token theft, replay acceptance, family race. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:13) |
| AT-SESSION-001-004 | Per-token, per-family, and per-User invalidation. | F-SESSION-001 | User has two devices and multiple active tokens. | Revoke one token, then one family, then all User sessions. | Token revoke affects only that token; family revoke affects one device family; User-wide revoke rejects all active sessions and corrects cache. | Over-broad revocation, under-broad revocation, stale cache. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:138 + backend/internal/gatewayhttp/auth_session_handler_test.go:85) |
| AT-SESSION-001-005 | Refresh-token cache falls back to PostgreSQL and corrects stale cache. | F-SESSION-001 | In-memory cache is enabled; PostgreSQL has authoritative family state. | Populate positive cache, revoke family in PostgreSQL, then validate through stale cache path. | PostgreSQL denial wins, cache is invalidated, request is rejected, and stale-cache metric/audit is emitted. | Cache-only session truth, revoked session accepted after restart. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:219 + backend/internal/usersession/rotation_test.go:279) |
| AT-SESSION-001-006 | Multi-device login policy is enforced. | F-SESSION-001 | Tenant max active device families is configured; User already has the maximum active families. | Attempt new login under deny policy, revoke-oldest policy, and confirmation policy. | Deny blocks new family; revoke-oldest removes oldest family and creates new one; confirmation policy requires explicit step before session creation. | Unlimited device sprawl, surprise logout, account sharing abuse. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:179) |
| AT-SESSION-001-007 | Password reset revokes or steps up active session families. | F-SESSION-001 / F-AUTH-007 | User has active sessions and completes password reset. | Complete reset, then try old session token and old refresh token; then log in again. | Old sessions are revoked or step-up required according to policy; old refresh tokens cannot rotate; new login creates clean family. | Compromised old sessions surviving reset. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:251) |
| AT-SESSION-001-008 | Abnormal session detection handles IP/UA drift. | F-SESSION-001 | User has established device baseline; test can vary IP class and User-Agent fingerprint. | Validate/refresh from normal context, then medium-risk drift, then high-risk drift. | Normal context stays active; medium risk marks family suspicious or requires step-up; high risk revokes family or all sessions per policy; audit contains no raw token. | Session hijack, false positives without recovery, raw IP/UA overexposure. | PASS (commit 7770e8c + backend/internal/usersession/rotation_test.go:49) |

## AT-MIMICRY-001 Anthropic TLS Mimicry — Rust Wave R-1

Source: `docs/plans/2026-05-17-rust-core-closure-roadmap-plan-claude.md` Wave R-1 and `tools/fingerprint-collector/templates/anthropic-claude-code.json`. This test targets the feature-gated Rust OpenSSL adapter only; no real `anthropic.com` network call is required in CI.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-MIMICRY-001 | Anthropic Claude Code TLS profile loads and routes to OpenSSL adapter; local mock TLS handshake runs with sampled JA3 fields applied where OpenSSL public APIs can express them. | Rust mimicry / Anthropic Lane 2b | `mimicry-openssl` feature enabled; HUAKAI collector template promoted to builtin; local TLS mock server available. | Load `BuiltinProfile::AnthropicClaudeCode`; assert sampled JA3/JA4/cipher/extensions fields; resolve backend; run OpenSSL adapter against local TLS server; inspect preflight extras. | Resolver returns `MimicryBackend::Openssl`; local TLS handshake succeeds; sampled JA3 hash stays recorded as `de88744b20558d50f03a5f0ea176ee98`; OpenSSL-native extras are surfaced instead of hidden. | Anthropic remaining KnownGapBlocked, silent fallback, or false exact-JA3 claim. | COVERED-WEAK (Wave R-1 local mock; full exact JA3 match remains gated by OpenSSL native extra fields and Owner R-D capture). |

## F-FP-001 Device Fingerprint Binding — Phase R-E+1 Spec Wave

Source: `docs/specs/device-fingerprint-binding.md`. These tests target the L3 device fingerprint binding (Phase R-E+1, after R-3 R-E rquest integration); implementation is deferred.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-FP-001-001 | account_credential 添加触发 device fingerprint binding 自动建立, profile 来自对应 vendor pool, deterministic (同 account_credential_id 永远同 profile) | F-FP-001 / F-CRED-001 | F-CRED-001 Phase B 已完成; F-FP-001 Phase R-E+1 已实施; `device_fingerprint_bindings` 表存在 | F-CRED-001 finalize 一 credential, 查 `device_fingerprint_bindings` 表, 检查 `fingerprint_profile_id` 跟 vendor profile pool registry 一致 | binding 自动建立; profile 来自该 vendor 注册 pool; 同 `account_credential_id` 重 query 返回同 profile | binding 漏建立, profile 来自错 vendor pool, hash collision 不可重现 | Planned |
| AT-FP-001-002 | 同 account_credential_id 多 outbound request → 同 fingerprint header 注入 (Sec-Ch-UA + User-Agent + Accept-Language 不变) | F-FP-001 | binding 已存在 | 跑 N 个 outbound request 通过 HUAKAI, 抓 outbound HTTP header | 所有 N request 同 Sec-Ch-UA + User-Agent + Accept-Language | 同 account 多 request fingerprint header 漂移, 上游识别为多设备 | Planned |
| AT-FP-001-003 | 不同 account_credential_id → 不同 fingerprint (collision rate <5% per vendor pool) | F-FP-001 | 同 vendor 多 account 已绑定 fingerprint | 跑 collision check 工具: 同 vendor 内 N 个 account 的 `fingerprint_profile_id` 唯一比例 | profile_id 唯一比例 ≥ 95% (collision <5%) per vendor pool | collision >5%, pool 太小或 hash skew | Planned |
| AT-FP-001-004 | account_credential 删除时 binding cascade 删除 | F-FP-001 / F-CRED-001 | binding 存在 | 删 `account_credentials.id`, 查 `device_fingerprint_bindings` 同 account_credential_id row | row 不存在 (CASCADE 生效); FK 约束保持一致 | binding orphan row, FK 约束失效 | Planned |
| AT-FP-001-005 | F-CH-002 ban_signal 检测 → fingerprint marked "burned", 不再 bind 给新 account | F-FP-001 / F-CH-002 | F-CH-002 已 active; binding pool 有 N profile | F-CH-002 emit ban_signal for 1 account; 新 account 添加 trigger binding selection | burned `profile_id` 不出现在新 account 的 binding; 新 binding 用另一 profile | burned profile 仍 bind 给新 account, 上游识别"同设备多次 ban" 链式 ban | Planned |
| AT-FP-001-006 | 3 个 burned account → pool 全 burned, admin alert 触发 | F-FP-001 / F-CH-002 | pool 5 profile; 3 已 burned | 第 4 个 account 添加 trigger binding | pool 全 burned → 用 fallback "low-risk default profile" + 写 admin alert + F-TRUST event `fingerprint_pool_exhausted` | 静默 fallback 无 alert, Owner 不感知 | Planned |
| AT-FP-001-007 | self_test cron 跑跟真 Chrome 137 采样比对, 漂移 <5% 阈值 | F-FP-001 | self_test cron 已配; Chrome 137 真采样 fixture 已落档 | 触发 self_test, 拿 HUAKAI inject 的 Sec-Ch-UA 串 vs 真 Chrome 137 串 比对 | 漂移 <5% 阈值; 漂移 ≥5% 触 admin alert + F-TRUST event `fingerprint_drift_detected` | 静默漂移 (Chrome 升级后 HUAKAI 落后), Owner 不感知 | Planned |
| AT-FP-001-008 | outbound HTTP request inject 正确 Sec-Ch-UA + User-Agent + Sec-Ch-UA-Platform 等 header (用 fingerproxy 类工具 verify) | F-FP-001 | binding 存在; outbound 走 R-3 R-E Rust 数据面 | 跑 outbound request 经 HUAKAI → fingerproxy mirror; 比对实际 header vs profile data | HTTP-layer 注入维度 (维度 1-3 + 部分 8, 实际 4-5 个 outbound header 含 User-Agent / Sec-Ch-UA / Sec-Ch-UA-Platform / Sec-Ch-UA-Mobile / Accept-Language) 跟 profile data 一致; JS-runtime 维度 (4-12) 不注入 HTTP layer (留 L6 主动对抗 phase) | header 漏字段或不一致, 上游识别异常 | Planned |
| AT-FP-001-009 | AWS Bedrock / Vertex / Azure path **不**走 fingerprint inject (企业 SaaS 不需) | F-FP-001 | binding 表内 `anthropic/bedrock` + `gemini/vertex_sa` account_credential 也有 row | 跑 Bedrock outbound request, 抓 header | header **不**含 inject 的 Sec-Ch-UA / fake User-Agent; binding 表 row 标 `inject_enabled=false` | Bedrock outbound 带 fake browser fingerprint, AWS 上游识别异常 | Planned |
| AT-FP-001-010 | L1 TLS profile 跟 L3 device profile 同 family (Chrome 137 Win11 一致, 不出现 Chrome 123 TLS + Chrome 137 Sec-Ch-UA 异常组合) | F-FP-001 / R-E+1 | rquest integration 已 commit; L1 + L3 profile pool 已设计 cross-layer family map | 同 account_credential_id 跑 outbound request, 抓 TLS handshake (JA3/JA4) + HTTP header (Sec-Ch-UA), 比对 family | JA3/JA4 暗示 Chrome 137; Sec-Ch-UA 也 declare Chrome 137; cross-layer 一致 | cross-layer 不一致 (例 JA3 Chrome 123 + Sec-Ch-UA Chrome 137), 上游异常 combo 触发风控 | Planned |

## F-ADV-001 Active Anti-Detection (L6) — Phase R-E+1 Spec Wave

Source: `docs/specs/active-anti-detection.md` (Claude-authored 反代 sensitive spec). 7 detection class × 7 action class active anti-detection orchestrator. Tests target the L6 active counter-measure layer (after L1 TLS / L2 HTTP/2 / L3 device fingerprint baseline is shipped); implementation deferred to Phase ADV-1 (7-10 天 codex). Schema migration 0023 `active_detection_events` table is referenced but not yet applied.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-ADV-001-001 | Detector 真识别 Cloudflare Turnstile challenge HTML → emit `challenge_injected` event. | F-ADV-001 | `active_detection_events` 表存在; response classifier 已 wire 到 outbound response path. | Inject 上游 response 含 Cloudflare Turnstile challenge HTML/iframe; 让 outbound response 通过 HUAKAI gateway. | `active_detection_events` row 写入 detection_class=`challenge_injected`, action_taken 走配置策略 (layer_rotate 或 channel_cooldown), audit_payload redacted (无 raw HTML body). | Challenge 漏识别, 上游主动 challenge HUAKAI 持续无应对; raw challenge body 入审计. | Planned |
| AT-ADV-001-002 | 突然 403 spike (5 min 内 N+) → trigger F-CH-002 channel cooldown + L3 fingerprint marked burned. | F-ADV-001 / F-CH-002 / F-FP-001 | F-CH-002 ban_signal 通路接 L6; F-FP-001 device_fingerprint_bindings row 存在. | 让同 channel 5 min 内连续 403 N 次. | active_detection_events row class=`sudden_403`, action=`channel_cooldown` + `fingerprint_burn`; channel_health_state.status=cooling_down; device_fingerprint_bindings.status=burned; F-TRUST audit chain 完整. | 误触发 (单次 403 触发); 关联 layer 漏处理. | Planned |
| AT-ADV-001-003 | CAPTCHA injected response → 立刻停 channel + admin alert + write audit. | F-ADV-001 | Admin alert sink 已配置 (`channel_health_admin_alerts` 表 reusable). | 让 outbound response 含 hCaptcha / reCaptcha site key. | channel 立即 status=disabled (manual_paused), admin alert row 写入, F-TRUST `active_detection_triggered { class: captcha_detected }` audit. | CAPTCHA 漏处理上游持续注入; admin 不感知. | Planned |
| AT-ADV-001-004 | Unusual response shape (vendor 突加 `X-Detected-Bot` header) → log + alert + no action. | F-ADV-001 | response shape monitor 已 wire baseline schema. | Inject response header `X-Detected-Bot: true` + 比对 baseline schema. | active_detection_events row class=`unusual_shape`, action=`admin_alert`, NOT layer_rotate / channel_cooldown (避免误触发); log entry + admin alert 都 emit. | 误把 unusual shape 升级到激进 rotate; vendor 加新 header 不感知. | Planned |
| AT-ADV-001-005 | JA4+ drift (HUAKAI 当前 Chrome profile vs Chrome 真采样最新) → profile pool update + alert. | F-ADV-001 / R-3 R-E+1 | self_test cron 已配; Chrome 真采样 fixture 存在; profile pool 可 update. | 触发 drift_monitor; 比对 HUAKAI outbound JA4+ vs Chrome 真采样, 漂移 > 阈值. | profile pool 自动 update 到新 Chrome version 数据; admin alert + F-TRUST `fingerprint_drift_detected` audit; 仅 drift > 严重阈值 alert (非严重静默 update). | Chrome 升级后 HUAKAI fingerprint 落后; 漂移误报刷屏 alert. | Planned |
| AT-ADV-001-006 | POL-1 alert "Anthropic banned 3rd-party tools" → L6 policy_listener trigger L1 升 Chrome + L3 加 Canvas 噪声 + F-CH cooldown all Anthropic channels. | F-ADV-001 / POL-1 / F-CH-002 | POL-1 已 cron 跑; L0→L6 联动 channel 已实施. | 注入 POL-1 alert event class="upstream_policy_change" vendor=Anthropic. | L1 profile pool 升 Chrome 最新 version; L3 device profile pool 加 Canvas 噪声 layer; F-CH 把 all Anthropic channels 切 cooling_down 24h; F-TRUST 多 audit event 链路完整. | POL-1 alert 误报触发大规模 layer rotate; 跨层联动不一致. | Planned |
| AT-ADV-001-007 | 跨账号 ban 关联 (同 fingerprint pool 内 3+ account 短期 ban) → 全 pool cooldown 24-72h. | F-ADV-001 / F-FP-001 / F-CH-002 | 3+ account 共 fingerprint pool 已绑定 binding. | 让 3 account 在 30 min 内全 ban_signal. | active_detection_events class=`cross_account_ban` 写入; pool 内全 fingerprint binding status=burned; pool 内全 account channel cooldown 24-72h (配置驱动); admin alert. | 误隔离健康 account; pool 全 burned 无可用; admin 不感知. | Planned |
| AT-ADV-001-008 | 误触发 rollback (admin override) → fingerprint un-burn + channel resume. | F-ADV-001 | Admin override API 已实施; F-TRUST audit 可写 rollback event. | Admin 调 rollback endpoint 指定 specific active_detection_events row. | device_fingerprint_bindings.status=active (un-burn); channel_health_state.status=active; F-TRUST `active_detection_rollback` audit event 含 admin user id + reason. | Rollback 漏一致性 (fingerprint resume 但 channel 没 resume); rollback 无审计. | Planned |
| AT-ADV-001-009 | detection_evidence JSONB 严格 redacted (无 raw cookie / raw upstream body / token / user prompt / credential bytes). | F-ADV-001 / F-TRUST | Test detection event 含上游 raw response 含 sensitive material. | 触发 detection → 查 active_detection_events.detection_evidence JSONB column. | JSONB 内仅含 enum class + 计数 + header name 白名单 + body length + body shape signature hash; 不含 raw cookie / raw body text / token / prompt. | 审计含敏感 raw material (上游 cookie / token / user prompt 漏出); 信任链泄漏. | Planned |
| AT-ADV-001-010 | Cross-vendor isolation (Anthropic ban detection 不影响 OpenAI channels). | F-ADV-001 | 多 vendor channel 同时 active. | 让 Anthropic 触发 sudden_403 / cross_account_ban event. | 仅 Anthropic channel + Anthropic fingerprint pool affected; OpenAI / Gemini channel + pool 完全不变; affected_layer 数组只含 Anthropic vendor scope. | 跨 vendor 误隔离 (Anthropic ban 把 OpenAI 一起 cooldown); admin 看到全平台异常. | Planned |
| AT-ADV-001-011 | Rule engine config hot reload (新 detection class 加进 HUAKAI native config, 不重启 gateway). | F-ADV-001 | HUAKAI native config 已支持 hot reload; rule engine 有 config 监听 channel. | Admin push 新 config 含 new detection_class; rule engine SIGHUP 或 watch trigger reload. | 新 detection_class 立即 active (无需重启); existing config snapshot 仍生效 直到 reload 完成; reload event 写 F-TRUST audit. | Hot reload 失败但无 fallback; 部分 reload 致 rule 不一致. | Planned |
| AT-ADV-001-012 | F-TRUST audit 含完整 detection chain (triggered + executed + correlated, 含 affected_layer + policy_version). | F-ADV-001 / F-TRUST | active_detection_events 表已 wire append-only audit. | 触发任意 detection class → 走完 detection + action + correlated; query active_detection_events 表 + correlation by detection_class + detected_at + policy_version. | 单 detection event 产生完整 chain row (triggered → executed → correlated), 每 row 含 affected_layer 数组 + policy_version 字段; 缺任一 row 算 audit gap; admin dashboard 可显示 chain timeline. | Audit chain 断裂 (triggered 写但 executed 漏); policy_version 漂移 audit 不可追溯. | Planned |

## F-PACE-001 Request Pacing Mimicry (L4) — Phase R-E+2 Spec Wave

Source: `docs/specs/request-pacing-mimicry.md` (Claude-authored 反代 sensitive spec). 7 节奏维度 × 7 vendor profile request pacing mimicry. Tests target the L4 request pacing layer (after L3 device fingerprint baseline ships, before L6 active anti-detection runtime); implementation deferred to Phase PACE-1 (5-7 天 codex). Schema migration for `pacing_profile_definitions` + `pacing_session_traces` is referenced but not yet applied.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-PACE-001-001 | profile_registry 加载 7 vendor profile + hot reload 新 profile 不重启 gateway. | F-PACE-001 | `pacing_profile_definitions` 表已存在; 7 默认 profile 已 seed; native config watch channel 已 wire. | Push 新 vendor_mode 进 native config + 触发 reload; 老 profile 改 μ/σ 然后 reload. | profile registry 重新加载, 新 vendor_mode 立即 active, 老 profile 改动生效; reload event 写 F-TRUST audit. | hot reload 失败但无 fallback; 部分 reload 致 profile 不一致. | Planned |
| AT-PACE-001-002 | 同 session 内连续 N request 时间分布跟 profile μ/σ 一致 (Kolmogorov-Smirnov 检验). | F-PACE-001 | profile registry 已加载; pacing_planner 已 wire. | 跑 100+ request 单 session, 收集 inter-request interval 分布. | KS 检验 p-value > 0.05 通过 (分布不显著偏离 profile); 实际 μ/σ 跟 profile target 比 < 10% 偏差. | profile sampling 实现错; 系统 bias 致分布偏离. | Planned |
| AT-PACE-001-003 | streaming response chunk 消费间隔跟 profile streaming_consume_seconds_per_chunk 一致. | F-PACE-001 | profile 含 streaming_consume_seconds_per_chunk; streaming_consumer 已 wire. | 跑 streaming response, 收集 chunk 消费 timestamp. | 平均 chunk interval ≈ profile target ± 10%; max < profile target × 3. | streaming consumer 不限速致 chunk 全 forward burst; 限速过严致客户端 timeout. | Planned |
| AT-PACE-001-004 | thinking pause 在 prompt 后真插入 N-M 秒 idle, 跟 vendor profile 一致. | F-PACE-001 | profile 含 thinking_pause_min/max_seconds; session_tracker 已 wire. | 提交大 prompt → 检查 prompt 提交完成后到下个 request 间隔. | 间隔 ≥ thinking_pause_min 且 ≤ thinking_pause_max (with jitter). | thinking pause 漏注入致节奏看着像 bot; 注入过长致 user 不耐烦. | Planned |
| AT-PACE-001-005 | burst tolerance 内 N request 不延迟; 超 burst 立刻触发 cooldown. | F-PACE-001 | profile 含 burst_tolerance; burst_controller 已 wire. | Same session 内连发 burst_tolerance + 2 request 不 sleep. | 前 burst_tolerance 个不 delay; 第 burst_tolerance+1 个开始进 burst block (delay X 秒); burst_block_count incremented. | burst block 误触发 (合法 burst 被 block); 不 block 致 vendor 看到突 burst. | Planned |
| AT-PACE-001-006 | diurnal modulation 深夜 (vendor 时区) 自动延长 interval × 1.5-2.0. | F-PACE-001 | profile 启 diurnal_modulation; tenant 时区配置 + vendor home TZ 配置. | Force 深夜时段 (2-6 AM tenant TZ) 跑同样 request 序列, 跟白天对比. | 深夜 inter-request interval 平均比白天长 1.5-2.0×; 透明 X-Huakai-Pacing-Diurnal-Active: true header. | diurnal 误判时区; 不调致深夜节奏跟白天一样可疑. | Planned |
| AT-PACE-001-007 | pacing_session_traces row 创建 + request_count + burst_block_count + ended_at 准确. | F-PACE-001 | session_tracker 已 wire; pacing_session_traces 表存在. | 跑 1 session 含 10 request + 1 burst block, then close session. | row 创建 + request_count=10 + burst_block_count=1 + ended_at 填; session 关闭前 last_activity_at update. | session_tracker 漏 update; ended_at 不填致 row 永远未 close. | Planned |
| AT-PACE-001-008 | pacing drift score (实际 vs profile) 在 admin UI 可见 + > 阈值触发 admin alert. | F-PACE-001 | drift_score 计算逻辑 + admin alert sink 已 wire. | 故意跑跟 profile 不一致的节奏 (e.g. 强 burst over tolerance + 漏 thinking pause). | pacing_drift_score 计算 + > 阈值; admin alert 写入; admin UI 展示 score timeline. | drift score 漏计算; alert 漏触发致 drift 持续不感知. | Planned |
| AT-PACE-001-009 | tenant_id NOT NULL FK enforced; 跨 tenant 不互见 pacing trace. | F-PACE-001 | 2 tenant 各自 session active. | tenant A 查 session_traces, tenant B 同 query. | tenant A 仅见自己 row; tenant B 同; 跨 tenant query 0 row. | tenant 隔离 fail 致 cross-tenant 数据泄漏. | Planned |
| AT-PACE-001-010 | pacing module 加 latency overhead p99 < 20% (compared to no-pacing baseline). | F-PACE-001 | benchmark harness 已配; baseline (无 pacing) 已 measure. | Load test 1000 request 有/无 pacing 对比 p99 latency. | p99 latency 加重 < 20%; admin UI 显示 pacing overhead 指标. | pacing module 性能瓶颈致 gateway 整体 slow; 关 pacing 不实际还跑致 fake disable. | Planned |

## F-NET-001 Outbound IP Pool (L5) — Phase R-E+2 Spec Wave

Source: `docs/specs/outbound-ip-pool.md` (Claude-authored 反代 sensitive spec). 4 IP 来源类型 × 4 binding 策略 outbound IP pool with per-account stable binding + cross-layer burn correlation. Tests target the L5 IP pool layer (after L4 pacing baseline ships); implementation deferred to Phase NET-1 (5-8 天 codex). Schema migration for `outbound_ip_pool_sources` + `outbound_ip_bindings` + `outbound_ip_burn_events` is referenced but not yet applied.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-NET-001-001 | account 启用时自动从 active pool 分配 1 IP + binding row 创建 + status=active. | F-NET-001 / F-CRED-001 | F-CRED-001 finalize 一 credential; outbound_ip_pool_sources 含 active source with quota. | F-CRED-001 finalize 一 credential, 触发 binding_allocator. | outbound_ip_bindings row 创建 + source_id 对应 active source + assigned_ip 填 + status=active; source.used_count +1. | binding 漏建立; 选错 source (inactive 或 quota 满). | Planned |
| AT-NET-001-002 | 同 account 多 outbound request → 全部走同 IP (stable_per_account 策略生效). | F-NET-001 | binding 存在 + strategy=stable_per_account. | 跑 N outbound request 通过 HUAKAI, 抓 outbound connection 源 IP. | 所有 N request 同 assigned_ip. | binding 漂移 (request 间换 IP) 致 vendor 看到多 IP 多 device. | Planned |
| AT-NET-001-003 | 不同 account → 不同 IP (collision <5% per pool). | F-NET-001 | 同 source 内 N+ account 已 binding. | 跑 collision check: 同 source 内 N account 的 assigned_ip 唯一比例. | 唯一比例 ≥ 95% (collision <5%). | collision >5% 致同 IP 多 account 被 vendor 关联. | Planned |
| AT-NET-001-004 | F-CH-002 ban_signal 触发 → binding.status=burned + burn_events row 写入 + 新 binding 不复用此 IP. | F-NET-001 / F-CH-002 | binding 已 active. | F-CH-002 emit ban_signal for 1 account. | binding.status=burned + burn_events row + 新 account 添加 trigger binding 不会得到此 IP (即便仍在 pool 中 marked unavailable). | burned IP 仍 reassign 致链式 ban. | Planned |
| AT-NET-001-005 | 跨层 burn 同 tx 原子 — IP burn + device burn + channel cooldown 一起 commit/rollback. | F-NET-001 / F-FP-001 / F-CH-002 | F-CH-002 ban_signal trigger 3 层联动. | trigger ban_signal + 注入 burn_events insert fail. | 3 层状态全 rollback (binding active / device active / channel not cooling); burn_events 无 row. | 半残: device burned 但 IP active 致下个 request 仍走 ban IP. | Planned |
| AT-NET-001-006 | account 删除时 binding cascade 删除. | F-NET-001 / F-CRED-001 | binding 存在. | 删 account_credential. | outbound_ip_bindings cascade 删除; pool source.used_count -1. | orphan binding row; quota 永不释放. | Planned |
| AT-NET-001-007 | health_probe 检测 pool IP 不可达 → 标 inactive + 不再分配 + admin alert. | F-NET-001 | health_probe cron 已配. | 注入 proxy endpoint 返 timeout/503. | source.active=false; 该 source 不再分配新 binding; admin alert 写入. | 不可达 source 仍分配致 request 全 fail. | Planned |
| AT-NET-001-008 | rotate_per_request 策略 — 每 request 不同 IP (按 pool 分布). | F-NET-001 | binding.strategy=rotate_per_request. | 跑 100 request, 收集 outbound source IP. | 各 IP 出现频率约等 pool 内 quota 比例 (Chi-square 检验通过). | rotate 漏触发致仍走同 IP; rotate 失衡致部分 IP 过载. | Planned |
| AT-NET-001-009 | manual_pin 策略 — 指定 IP 后 binding 不自动 rotate, ban 时仍触发 alert 但 status 不变 burned (除非 admin manual). | F-NET-001 | binding.strategy=manual_pin + assigned_ip 锁定. | F-CH-002 ban_signal trigger. | binding.status 仍 manual_pinned (不 auto burned); admin alert 写入提示 "manual_pin IP ban, 需 admin 决策"; admin manual API 改 status 后才 burn. | manual_pin 自动 burn 致 admin 配置丢失. | Planned |
| AT-NET-001-010 | cross-account 同 IP 检测 — 同 IP 绑 N+ account 时 L6 触发 cross_account_ban 关联. | F-NET-001 / F-ADV-001 | 多 account 共享同 manual_pin IP (违反默认 1:1). | trigger 任意一 account ban_signal. | L6 cross_account_ban 关联 → 全 N account 同 cooldown + 同 IP marked burned. | 漏关联致单 account ban 不影响其他, vendor 后续仍 ban. | Planned |
| AT-NET-001-011 | tenant_id NOT NULL FK enforced; 跨 tenant 不互见 pool / binding. | F-NET-001 | 2 tenant 各自 pool source 和 binding. | tenant A 查 sources / bindings, tenant B 同. | tenant A 仅见自己; 跨 tenant 0 row. | tenant 隔离 fail 致 cross-tenant pool 数据泄漏. | Planned |
| AT-NET-001-012 | outbound_ip_burn_events 严格 redacted (无 raw cookie / body / token); proxy_endpoint encrypted at rest 不 leak. | F-NET-001 / F-TRUST | burn 触发含 sensitive upstream raw material. | trigger burn → query burn_events.evidence_redacted JSONB + pool_sources.proxy_endpoint. | evidence_redacted 仅含 enum + 计数 + ID; proxy_endpoint 在 SELECT * 时 encrypted, decrypt 仅在 outbound forwarder 内 (with KeyProvider). | 审计 leak 上游 raw material; proxy 凭证泄漏致 pool 被反向利用. | Planned |

## F-TRUST-001 Trust Chain User-Verifiable Ledger — Phase 6 Commercial Foundation

Source: `docs/specs/trust-chain-user-verifiable-ledger.md` (PM synthesis, commit 158c421). HUAKAI 6 大差异化 1+3+4 实施 (链路公开 + 模型校验 + 商家不能做假). Phase TRUST-1-A 已完成 (audit_ledger_entries schema + writer, commit 0013); TRUST-1-B/C/D/E 待 codex 派.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-TRUST-001-001 | 每 gateway request 创建 1 row in audit_ledger_entries; request_id UNIQUE; tenant_id 一致. | F-TRUST-001 | audit_ledger_entries 表存在 (0013 migration); writer pipeline wired. | 发 N request → query ledger; cross-tenant query 验隔离. | row 创建 + request_id UNIQUE + tenant_id 跟 request 一致. | ledger 漏写 / cross-tenant leak / request_id 冲突. | PASS (working tree + backend/internal/auditledger/acceptance_test.go:42 + backend/internal/auditledger/postgres_test.go:216) |
| AT-TRUST-001-002 | hop_chain JSON array 含完整 hop (ingress_auth → response_finalize), 每 hop 有 decision_ref + safe schema. | F-TRUST-001 / F-PRIV-001 | hop emitter wired at 7 hop_kind points; F-PRIV Redactor enforced. | 发 request → query hop_chain. | 6+ hop entries 含 hop_kind/actor/started_at/decision_ref; 严禁 prompt/completion/token/cookie. | hop 漏 emit / prompt 误入 decision_ref. | PASS (working tree + backend/internal/auditledger/acceptance_test.go:15 + backend/internal/auditledger/acceptance_test.go:75) |
| AT-TRUST-001-003 | model_chain 3 字段 + verdict 4 分类; mismatch 时 client 收 X-Huakai-Mismatch header. | F-TRUST-001 / F-AUDIT-001 | model_chain emit at response_finalize hop. | 发 match / allowed_alias / mismatch / unknown 4 case request. | 4 verdict 正确分类; mismatch 时 client header 透明告知 + F-AUDIT validation_state=mismatch_pending. | substitution 不透明 / verdict 漏分类. | PASS (working tree + backend/internal/auditledger/acceptance_test.go:97 + backend/internal/proto/trust_chain_types.go:89) |
| AT-TRUST-001-004 | prev_merkle_root + merkle_root 32-byte; chain 连续 (N prev == N-1). | F-TRUST-001 | writer 单线程 + advisory lock per tenant. | 写连续 100 entries → query 验 chain 连续. | 100 entries 全连续, 任 1 中断 → chain 检测 fail. | chain 断裂 / writer race. | PASS (working tree + backend/internal/auditledger/acceptance_test.go:113 + backend/internal/auditledger/acceptance_test.go:143 + backend/internal/auditledger/postgres_test.go:120) |
| AT-TRUST-001-005 | ed25519 signature 用 pubkey verify PASS; tamper any byte → fail. | F-TRUST-001 | KMS / KeyProvider ed25519 keypair active; pubkey 发布到 /.well-known/. | sign 1 entry → client verify PASS; tamper byte → verify FAIL. | Verify pass without tamper; fail with tamper. | sign 实现错 / tamper 不检测. | PASS (working tree + backend/internal/auditledger/acceptance_test.go:152 + backend/internal/auditledger/signer_test.go:18) |
| AT-TRUST-001-006 | UPDATE audit_ledger_entries trigger 拒绝; DELETE 拒绝. | F-TRUST-001 | Append-only trigger enforce_ledger_append_only 已 install. | UPDATE row → 拒; DELETE row → 拒. | Both 操作 trigger raise exception. | append-only 失效 / operator 篡改. | PASS (working tree + backend/sql/migrations/0027_ledger_append_only_trigger.up.sql:3 + backend/internal/auditledger/acceptance_test.go:192 + backend/internal/auditledger/postgres_test.go:182) |
| AT-TRUST-001-007 | GET /v1/ledger/entries/{request_id} 返完整 row + client detached verify PASS. | F-TRUST-001 / F-SESSION-001 | session middleware 接入; pubkey registry public. | tenant user query own request_id; cross-tenant query → 404. | 完整 row + canonical_payload + signature; detached verify PASS. | endpoint 漏 / cross-tenant leak. | Planned |
| AT-TRUST-001-008 | pubkey rotation — 老 pubkey rotated/revoked 仍可 verify 历史 entry; 新 entry 用新 pubkey. | F-TRUST-001 | rotation worker 已配 (90 天周期). | Trigger rotation → 老 entries 仍 verify PASS; 新 entries 用新 pubkey. | 双 pubkey window 内全部 verify PASS; rotation 后老 entry 用 stored pubkey_fingerprint. | rotation 致历史 entry 失效 / 新 entry 用错 pubkey. | PASS (working tree + backend/internal/auditledger/signer_test.go:80) |
| AT-TRUST-001-009 | cross-tenant query — tenant A 查 tenant B request_id → 404 (RLS + tenant_scope_ref 不暴露). | F-TRUST-001 / F-PRIV-001 | tenant scope query 强制. | tenant A user query tenant B request_id (或猜测). | 404 不暴露 row 存在; tenant_scope_ref 在 receipt 中不直接是裸 tenant_id. | tenant 隔离 fail / 枚举 attack. | Planned |
| AT-TRUST-001-010 | hop_chain.decision_ref / model_chain 严禁含 raw prompt / completion / token / cookie / PII. | F-TRUST-001 / F-PRIV-001 | F-PRIV Redactor enforce. | 发请求含 sentinel prompt/cookie → query ledger 验内容. | hop/model 仅 SAFE_METADATA; sentinel 不存在. | redaction 漏 / 隐私 leak. | Planned |

## F-PRIV-001 Privacy / No User Data Logs — Phase 6 Commercial Foundation

Source: `docs/specs/privacy-no-user-data-logs.md` (PM synthesis). HUAKAI 6 大差异化 2+5 实施 (无用户数据日志 + 日志只系统报错). Phase PRIV-1 5 sub-phase 5-7 天 codex 待派.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-PRIV-001-001 | prompt/completion sentinel 全 sink 搜索. | F-PRIV-001 | Redactor + 3 channel logger 接入. | 发含 sentinel request; 搜索 audit/log/usage/trust sinks. | 仅 metadata; sentinel 不存在. | 默认内容留存. | PASS (working tree + `backend/internal/privacy/integration_test.go`) |
| AT-PRIV-001-002 | streaming failure partial chunk. | F-PRIV-001 | streaming path 可注入 disconnect. | 前 2 chunk 含 sentinel 后断流; query log/audit/usage. | usage 含 terminal class + token estimate; log 无 chunk text. | partial stream 泄漏. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-003 | tool I/O redaction. | F-PRIV-001 | tool call path 可用; args/result 含 sentinel. | trigger tool call 成功 + 失败. | audit 仅 class/status/latency/count; 无 args/result. | tool payload 泄漏. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-004 | cookie/token/API key 不进日志. | F-PRIV-001 | header + upstream credential 含 sentinel. | trigger auth fail / upstream fail / retry fail. | system log 仅 fingerprint/ref; 无 secret value/prefix/suffix. | secret logging. | PASS (working tree + `backend/internal/privacy/redactor_test.go` + `backend/internal/obs/dlq/outbox_test.go`) |
| AT-PRIV-001-005 | raw upstream error body normalized. | F-PRIV-001 | upstream 返 4xx/5xx body 含 user text + diagnostic. | trigger upstream error; 查 log/audit/client response. | HUAKAI 记 normalized error_class/status_class; 无 raw body 持久化. | upstream body 泄漏. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-006 | system log freeform interpolation 拒绝. | F-PRIV-001 | logger test fixture 尝试 user interpolation. | 执行 log write. | pre-write guard block; fallback event `redaction_result=blocked`. | freeform log 拼接. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-007 | F-TRUST hop/model chain 无 content. | F-PRIV-001 / F-TRUST-001 | F-TRUST ledger writer 可用. | 发 sentinel request; 查 ledger entry. | hop_chain/model_chain 仅 safe metadata; sentinel 不存在. | trust ledger 泄漏. | PASS (working tree + `backend/internal/privacy/integration_test.go`) |
| AT-PRIV-001-008 | usage/billing 仅消费 metadata. | F-PRIV-001 / F-BILL-001 | usage writer 可用. | 完成 success/retry/timeout/refund path. | usage/billing 含 token/cost/pricing/settlement; 无 prompt preview. | 消费透明变内容留存. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-009 | cross-tenant log isolation. | F-PRIV-001 | tenant A/B 各有 request. | tenant A query tenant B request_id / trace_id / ledger_id. | 404 或 empty; 不暴露存在性. | cross-tenant data leak. | PASS (working tree + `backend/internal/gatewayhttp/audit_verify_handler_test.go`) |
| AT-PRIV-001-010 | metadata/details/evidence JSON pre-write guard. | F-PRIV-001 | audit payload 含 forbidden keys + secret-like values. | call audit writer. | writer fail-closed 或 drop redacted + alert emitted. | raw dump container. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-011 | panic recovery 不 dump request. | F-PRIV-001 | handler 注入 panic; body/header 含 sentinel. | trigger panic path. | system log 含 panic class/component/request_id; 无 body/header/locals. | panic dump 泄漏. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-012 | external log sink 仅 redacted event. | F-PRIV-001 | external sink test double 可见 payload. | trigger success / failure / panic / security event. | sink payload 跟 internal redacted schema 一致; 无 sentinel. | sink bypass. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-013 | content binding hash default OFF. | F-PRIV-001 / F-TRUST-001 | F-TRUST enabled; no explicit opt-in. | 发 request; 查 receipt. | receipt 不含 content hash; no server-side content digest. | 默认 hash 泄漏/可关联. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |
| AT-PRIV-001-014 | opt-in content binding hash 仅不可逆 proof. | F-PRIV-001 | Owner/test config 显式 enable; client 提供 nonce/salt. | 发 request; 查 receipt/logs. | 仅 proof field; raw content 不存在; hash 不可作 search/debug key. | hash 被滥用为内容索引. | PASS (working tree + `backend/internal/privacy/redactor_test.go`) |

## F-AUDIT-001 User Consumption Transparency — Phase 6 Commercial Foundation

Source: `docs/specs/user-consumption-transparency.md` (PM synthesis). HUAKAI 6 大差异化 6 实施 (用户消费透明). Phase AUDIT-1 5 sub-phase 7-11 天 codex 待派. derived-first 架构 + ed25519 signed snapshot.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-AUDIT-001-001 | Happy-path receipt validates exact cost. | F-AUDIT-001 / F-BILL-001 / F-TRUST-001 | One committed claim + billing event + usage record + F-TRUST row + rate version for tenant/user. | Fetch receipt + recompute from token tuple + rates. | API `valid`; recomputed cost = final cost = balance delta. | Hidden overcharge / wrong rate version. | Planned |
| AT-AUDIT-001-002 | Cross-tenant request_id no leak. | F-AUDIT-001 / F-PRIV-001 | Tenant A/B same/guessed request ids. | Tenant A query tenant B. | Safe 404; no timing/message hint. | DR-001 breach. | Planned |
| AT-AUDIT-001-003 | Receipt redaction-safe. | F-AUDIT-001 / F-PRIV-001 | usage/routing metadata 含 sensitive-like fields. | Generate receipt + scan payload. | 无 prompt/completion/tool body/upstream body/credential/cookie/raw voucher code. | Privacy breach. | Planned |
| AT-AUDIT-001-004 | F-TRUST mismatch → refund. | F-AUDIT-001 / F-TRUST-001 | F-TRUST verdict=mismatch + billing 已 charge. | Generate receipt + run mismatch closure. | `mismatch_pending` → `mismatch_refunded` + adjustment_refs. | Silent model substitution charge. | Planned |
| AT-AUDIT-001-005 | Allowed cheaper substitution lower rate. | F-AUDIT-001 | Route uses allowed alias with lower rate. | Settle + fetch receipt. | alias + lower rate + no hidden higher charge. | Cheap model sold as expensive. | Planned |
| AT-AUDIT-001-006 | Cache-hit charge explicit. | F-AUDIT-001 | request 含 cache read/create token. | Fetch receipt. | Cache tokens/rates separate cost lines; no blended hidden. | Cache overbilling. | Planned |
| AT-AUDIT-001-007 | Retry attempts no double charge. | F-AUDIT-001 / F-BILL-001 | 1 failed pre-output attempt + 1 successful final. | Settle + fetch receipt. | Failed listed non-charged; final once. | Duplicate billing on retry. | Planned |
| AT-AUDIT-001-008 | Aborted claim not billable. | F-AUDIT-001 / F-BILL-001 | Upstream terminal failure after Tx1, claim aborted. | Fetch receipt. | `not_billable`, cost=0, balance delta=0. | Charging failed requests. | Planned |
| AT-AUDIT-001-009 | Provisional inferred usage visible. | F-AUDIT-001 | Stream ends without authoritative; usage source inferred/partial. | Fetch receipt before + after reconciliation. | Initial `provisional`; later `valid` or adjustment/refund. | Estimate disguised as truth. | Planned |
| AT-AUDIT-001-010 | Late authoritative usage appends correction only. | F-AUDIT-001 | Original usage row exists; late report differs. | Run reconciliation + fetch receipt. | Original usage row unchanged; reconciliation + adjustment refs listed; receipt hash/version 变 predictably. | Mutating money history. | Planned |
| AT-AUDIT-001-011 | Rate version historical, not live. | F-AUDIT-001 | Rate changes after request settles. | Fetch old receipt + current rate endpoint. | Old receipt still validates against stored historical version. | Repricing old requests. | Planned |
| AT-AUDIT-001-012 | Voucher top-up vs usage 不混淆. | F-AUDIT-001 / F-BILL-002 | User has voucher redemption + later usage charge. | Fetch usage receipt. | Receipt refs usage billing event; voucher ref 仅 balance source when relevant; no raw code. | Balance-source confusion / voucher leakage. | Planned |
| AT-AUDIT-001-013 | Detached verify no request oracle. | F-AUDIT-001 | Attacker no full receipt payload. | Call detached verify with guessed request_id only. | API refuses or returns non-oracle error; no DB existence lookup. | Privacy oracle. | Planned |
| AT-AUDIT-001-014 | Billing event survives usage-record gap. | F-AUDIT-001 / F-OBS-005 | Billing event 存 + usage record DLQ/replay. | Fetch receipt. | `receipt_unavailable` 或 `provisional` with durable billing event ref; no false valid. | User sees unverifiable charge as verified. | Planned |
| AT-AUDIT-001-015 | Decimal precision exact. | F-AUDIT-001 | Many tiny token charges across categories. | Recompute over large sample. | Sum matches numeric precision; no float drift. | Penny drift / rounding abuse. | Planned |
| AT-AUDIT-001-016 | Receipt generation idempotent. | F-AUDIT-001 | Same request fetched repeatedly before new adjustment. | Fetch receipt multiple times. | Same canonical hash + payload; after adjustment, new state appends refs without changing original. | Non-deterministic proof. | Planned |
| AT-AUDIT-001-028 | Refund reconciliation event is replicated. | F-AUDIT-001 / F-BILL-001 / F-OBS-005 | Settled claim exists; settler has `WithReplicaTarget` configured. | Append refund via billing `Refund`; inspect DLQ replica intent for the refund billing event. | `reconciliation_appended` billing event enqueue produces one pending `billing_event_replica` row in the same transaction. | DR/replica losing refund adjustments while primary billing history shows refund. | PASS (working tree + backend/internal/billing/settler_integration_test.go:348; requires `integration_pg`) |
| AT-AUDIT-001-029 | Detached verify degrades when derivation is unavailable. | F-AUDIT-001 / F-TRUST-001 | User submits a receipt with valid ed25519 signature; derived receipt lookup fails or is temporarily unavailable. | POST verify payload and force derivation error. | HTTP 200 with `valid=true` and `verdict=unknown`; no mismatch refund enqueue. | Historical valid receipt blocked by transient derivation outage. | PASS (working tree + backend/internal/gatewayhttp/cost_receipt_handler_test.go:286) |
| AT-AUDIT-001-032 | Refunded receipt sequence verifies. | F-AUDIT-001 / F-TRUST-001 | A sequence 1 refunded receipt exists with `mismatch_refunded` state, adjustment refs, canonical hash, and signature. | GET the receipt, then POST the returned public payload to detached verify. | Response includes `receipt_sequence=1`; detached verify returns `valid=true` with the same sequence. | Refunded receipt signed over sequence 1 but verified as sequence 0. | PASS (working tree + backend/internal/gatewayhttp/cost_receipt_handler_test.go:92) |
| AT-AUDIT-001-033 | Refund ledger append duplicate retry completes. | F-AUDIT-001 / F-BILL-001 | DLQ retry sees the refund ledger row already written; append returns PostgreSQL unique violation. | Run refund worker and force ledger append duplicate, then read existing ledger entry by refund request id. | Worker uses the existing ledger ref and marks refund pending completed. | Refund DLQ stuck failed after post-append crash. | PASS (working tree + backend/internal/audit/refund_worker_test.go:118) |
| AT-AUDIT-001-034 | Refund receipt append duplicate retry completes. | F-AUDIT-001 / F-BILL-001 | DLQ retry sees the sequence 1 refunded receipt already written; append returns duplicate. | Run refund worker and force receipt append duplicate, then read existing sequence 1 receipt. | Worker accepts the existing refunded receipt and marks refund pending completed. | Refund DLQ stuck failed after receipt append succeeded but completion mark crashed. | PASS (working tree + backend/internal/audit/refund_worker_test.go:144) |
| AT-AUDIT-001-050 | Historical signer key verifies after rotation. | F-AUDIT-001 / F-TRUST-001 | Old signer key is registered, an audit ledger entry is signed, then registry rotates to a new signer key. | Query audit verify for the old entry after rotation. | `signature_valid=true` and `key_status=rotated`; old proof remains verifiable with its historical public key. | Key rotation making historical proofs unverifiable. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:18) |
| AT-AUDIT-001-051 | Unknown signer fingerprint does not verify as active. | F-AUDIT-001 / F-TRUST-001 | Audit verify receives an entry whose signer fingerprint is not in the registry. | Query audit verify with an empty registry. | `signature_valid=false`, `key_status=unknown`, and `reason=unknown_signer`. | Unknown key accepted or misreported as current. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:57) |
| AT-AUDIT-001-052 | Public key registry lists current and historical keys. | F-AUDIT-001 / F-TRUST-001 | Registry contains one rotated key and one current key. | GET `/v1/audit/pubkeys` and GET `/v1/audit/pubkey/{fingerprint_hex}` for the rotated key. | List includes both keys; current key is `active`, old key is `rotated`, and historical lookup includes `effective_to`. | Pubkey endpoint exposing only current key, making old receipts unverifiable. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:82 + backend/internal/gatewayhttp/audit_key_rotation_test.go:115) |
| AT-AUDIT-001-053 | Receipt verify uses historical signer key after rotation. | F-AUDIT-001 / F-TRUST-001 | A receipt is signed by the old key, then registry rotates to a new active key. | POST the detached receipt to `/v1/receipts/{request_id}/verify` while the active signer is the new key. | `valid=true`, `key_status=rotated`, and no unknown-signer reason. | Historical cost receipts rejected after key rotation. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:145) |
| AT-AUDIT-001-054 | Ledger verify rejects signatures outside key validity window. | F-AUDIT-001 / F-TRUST-001 | Old key has `effective_to`, but an entry timestamp after that window is signed with the old key. | Query `/v1/audit/verify` for the entry. | `signature_valid=false` and `reason=signature_outside_key_window`. | Rotated keys remaining valid for future entries. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:177) |
| AT-AUDIT-001-055 | Public key list timestamps are RFC3339 date-time strings. | F-AUDIT-001 / F-TRUST-001 | Registry contains active and rotated keys with fixed effective timestamps. | GET `/v1/audit/pubkeys`. | `effective_from` and `effective_to` parse with `time.RFC3339` and match exact UTC values. | OpenAPI `date-time` contract violated by RFC1123 strings. | PASS (working tree + backend/internal/gatewayhttp/audit_key_rotation_test.go:216) |

## F-BILL-002 Voucher System — Phase 6 Commercial Foundation

Source: `docs/specs/voucher-system.md` and `docs/decompositions/_cross-cutting/voucher-system.md`. These are acceptance outlines only; no backend/schema implementation is part of this docs wave.

| Test ID | Scenario | Capability | Preconditions | Steps | Expected Result | Risk Covered | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AT-BILL-002-001 | Admin creates one active voucher. | F-BILL-002 | Admin is authorized; tenant context is resolved; no voucher exists with the submitted code; F-TRUST audit chain is available. | Normal: create voucher with code, value in cents, valid window, max redemptions, and single-use-per-user flag. Failure: create duplicate code, zero/negative value, or invalid time window. Recovery: admin corrects input or regenerates code and retries. | Normal creates one voucher and emits `voucher_created` without raw code in routine logs. Failure creates no voucher and records safe validation evidence. Recovery succeeds with one audit event for the final voucher. | Duplicate code, invalid commercial value, unaudited admin creation, code leakage. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:13 + backend/internal/gatewayhttp/voucher_handler_test.go:20) |
| AT-BILL-002-002 | User redeems a valid voucher and receives balance/quota credit. | F-BILL-002 / F-AUTH-007 / F-SESSION-001 / F-BILL-001 | User is authenticated; voucher is active, within validity window, eligible for the User, and has remaining redemption capacity; balance/quota target exists or can be created atomically. | Normal: User submits code once. Failure: simulate storage error before commit. Recovery: retry after storage recovers. | Normal writes voucher redemption, `billing_events { type: voucher_redeemed, value, voucher_id }`, balance/quota increment, and `voucher_redeemed` audit in one committed result. Failure rolls back with no partial credit or success event. Recovery can complete exactly once. | Missing credit, billing drift, partial transaction, audit gap. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:39 + backend/internal/gatewayhttp/voucher_handler_test.go:20) |
| AT-BILL-002-003 | Concurrent redemption race is serialized for same voucher and same User. | F-BILL-002 | Voucher has `max_redemptions = 1` or `single_use_per_user = true`; test can start concurrent redeem attempts with a barrier; PostgreSQL advisory-lock behavior is enabled in implementation wave. | Race many redeem requests for the same voucher and User, then for same voucher with multiple Users. | At most one global redemption succeeds when capacity is one; at most one redemption per User succeeds when single-use applies; losing requests reread state and reject safely without extra balance. | Double credit, exhausted voucher overrun, same-User replay under race. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:72) |
| AT-BILL-002-004 | Expired and not-yet-valid vouchers reject without balance mutation. | F-BILL-002 | One voucher is before `valid_from`; another is past `valid_until`; User is authenticated. | Try redeem before start time and after expiry; run expiry transition path when applicable. | Both attempts reject; no redemption row with success, no balance/quota change, no `voucher_redeemed` billing event; `voucher_expired` appears when status transitions. | Expired-code spend, premature campaign use, missing expiry audit. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:109) |
| AT-BILL-002-005 | Wrong User or tenant cannot redeem targeted voucher. | F-BILL-002 / F-AUTH-007 | Voucher has eligibility policy for another User/email hash/cohort or tenant; authenticated User is outside the policy. | Attempt redemption from wrong User and from mismatched tenant context. Recovery: intended User redeems or admin issues replacement. | Wrong caller receives safe rejection; no balance change; no leakage of intended recipient beyond policy-safe text; failed audit reason is `wrong_user` or `tenant_mismatch`. | Targeted credit theft, cross-tenant leakage, recipient enumeration. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:109) |
| AT-BILL-002-006 | Revoked voucher rejects future redemption and does not refund prior successful redemption. | F-BILL-002 | Voucher has one successful prior redemption and remaining capacity; admin is authorized to revoke. | Admin revokes voucher; previous redeemer balance is checked; another User attempts redemption. Recovery: admin creates replacement voucher if needed. | `voucher_revoked` audit is emitted; previous credit remains; future redemption rejects; no automatic negative adjustment or refund occurs. | Surprise credit removal, revoke without audit, revoked-code reuse. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:109) |
| AT-BILL-002-007 | Voucher audit and billing event chain is complete and redacted. | F-BILL-002 / F-TRUST | Admin can create/revoke; User can redeem; audit query and billing event query are available. | Create voucher, redeem it, expire or revoke another voucher, and inspect audit/billing payloads. | Required events `voucher_created`, `voucher_redeemed`, `voucher_expired`, and `voucher_revoked` are present where applicable; redeem has matching billing event; raw voucher code is absent from routine audit/log payloads. | Audit gaps, commercial event mismatch, raw code exposure. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:13 + backend/internal/voucher/service_test.go:39) |
| AT-BILL-002-008 | Anti-fraud blocks per User+IP burst and alerts on burst pattern. | F-BILL-002 | Tenant anti-fraud threshold `N` is configured; test can vary User and IP class/hash; alert sink is enabled. | Same User+IP submits more than `N` attempts in one minute; then many Users from one IP attempt many voucher codes. Recovery: operator reviews alert and tightens/revokes if needed. | Excess attempts block before balance mutation; failed attempts do not consume voucher capacity; burst alert includes aggregate counts and redacted User/IP/voucher evidence. | Brute-force code guessing, global overblocking, capacity burn from failed attempts, missing ops alert. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:157) |
| AT-BILL-002-009 | Redemption idempotency retry returns prior result without double credit. | F-BILL-002 | Voucher is active; User sends an idempotency key; first redeem can be replayed after client timeout. | Redeem once and lose client response; retry with same idempotency key; then retry with conflicting key/fingerprint if supported by implementation. | Same-key retry returns prior success/new balance without adding credit again; conflicting replay is rejected or treated as a new request only when policy permits and capacity remains. | Double credit on retry, ambiguous client timeout, replay conflict. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:183) |
| AT-BILL-002-010 | Admin batch creates vouchers with deterministic all-or-nothing and explicit partial mode. | F-BILL-002 | Admin is authorized; batch request includes count, value, validity, redemption policy, and generated/imported codes; one code conflict can be simulated. | Normal: create valid batch. Failure: include one duplicate/invalid code under all-or-nothing mode. Recovery: rerun corrected batch or choose explicit partial mode. | Normal creates requested vouchers and one batch summary plus per-voucher audit. All-or-nothing failure creates none. Explicit partial mode creates valid subset and reports failed rows with stable ordinals and audit. | Partial silent creation, batch audit gap, duplicate-code drift, operator confusion. | PASS (commit 158c421 + backend/internal/voucher/service_test.go:211) |

## Release Rule

No capability group may be marked release-ready without acceptance tests covering normal path, failure path, and operator recovery path.
