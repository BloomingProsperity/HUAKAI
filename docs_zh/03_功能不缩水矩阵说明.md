本文件仅供 Owner 快速理解；现行执行规则以根目录 `AGENTS.md` 和 `docs/RULES.md` 为准。

# 功能不缩水矩阵说明

这个矩阵用于保证参考项目里的功能不会被悄悄丢掉。

每个参考功能都必须被标记为以下状态之一：

- 已实现。
- 更好地实现。
- 合并等价实现。
- 安全等价实现。
- 插件实现。
- 功能开关控制。
- 强制路线图。

不允许出现“忽略”“删掉”“不需要”“风险太高所以不做”这类结论。
