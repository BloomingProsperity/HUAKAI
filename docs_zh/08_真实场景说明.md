本文件仅供项目 Owner 理解，正式规则以英文文件为准。

# 真实场景说明

必须保持功能不缩水，真实场景用于证明功能对齐不是口头承诺。

真实场景用于描述系统在生产中必须能处理的问题。

重点场景包括：

- 供应商正常时按规则路由。
- 供应商异常、账号禁用、额度耗尽时自动切换。
- 凭据轮换不中断服务。
- 账号泄露后能立即禁用并从路由中移除。
- 请求、用户、key、路由、供应商账号、计费记录之间能串起来排障。
- 日志和 UI 中不能泄露密钥。

每个重要功能上线前都应该有至少一个真实场景。
