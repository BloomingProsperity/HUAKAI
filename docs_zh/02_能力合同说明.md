本文件仅供 Owner 快速理解；现行执行规则以根目录 `AGENTS.md` 和 `docs/RULES.md` 为准。

# 能力合同说明

必须保持功能不缩水，目标是功能对齐或更强。

能力合同定义这个系统必须具备哪些能力。

这些能力包括网关、协议、账号中心、渠道、供应商、配额、计费、用量、可靠性、安全、可观测性、管理后台等。

“合同”的意思是：这些能力是产品义务，不能因为麻烦就删。如果某个能力来自非 MIT 参考项目，也不能复制代码，但必须用独立方式实现同等或更好的用户结果。
