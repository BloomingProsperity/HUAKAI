本文件仅供 Owner 快速理解；现行执行规则以根目录 `AGENTS.md` 和 `docs/RULES.md` 为准。

# 验收测试说明

必须保持功能不缩水，验收测试用于证明功能对齐真实存在。

验收测试用于证明功能真的可用，不只是文档上存在。

必须覆盖：

- 网关路由和故障转移。
- 协议兼容。
- 供应商账号生命周期。
- 凭据轮换和禁用。
- 用户 key。
- 配额、计费、用量。
- 审计日志。
- 密钥脱敏。
- 运维排障流程。
- 插件和功能开关。
- 管理后台操作。

每个能力组都要覆盖正常路径、失败路径和运营恢复路径。
