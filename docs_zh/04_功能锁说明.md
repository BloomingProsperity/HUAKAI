本文件仅供 Owner 快速理解；现行执行规则以根目录 `AGENTS.md` 和 `docs/RULES.md` 为准。

# 功能锁说明

必须保持功能不缩水，目标是功能对齐或更强。

功能锁用于防止开发过程中产品缩水。

被锁定的范围包括：

- 网关和供应商管理。
- 账号和凭据中心。
- 渠道和路由。
- 协议兼容和转换。
- 配额、用量、计费。
- 管理后台。
- 可靠性、安全、日志、审计。
- 插件、功能开关、验收测试。

如果要调整这些功能，只能做增强、合并等价、安全等价、插件化、功能开关或强制路线图，不能直接删除。
