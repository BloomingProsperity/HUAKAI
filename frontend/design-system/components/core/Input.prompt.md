A text input on the console's inset surface, with a teal focus ring. Pair with `Label`.

```jsx
<div>
  <Label>API Key</Label>
  <Input mono placeholder="hk_live_…" />
</div>
```

Props: `invalid` (red border), `mono` (monospace, for keys/IDs/tokens).
